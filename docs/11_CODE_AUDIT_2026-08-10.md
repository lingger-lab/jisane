# 코드 축 전수 검수 2026-08-10 — scope=code set=diff(7/16↓) model=fable
> 기준: `~/.claude/rules` (golden-principles·quality-gates·coding-style·quality-model) + `/audit` 스킬
> 범위: docs/07(7/16 감사) 이후 변경분 **183파일 / 20,403라인** — 병렬 대규모 머지(partner→owner/expert 개명·파트너공간) + 결제 작업 전부
> coverage: 183/183 판독(24그룹) · confirmed **P1 22 · P2 70 · P3 120** · refuted 9 · 미독 0
> quote-back: **212/212 OK, 허위 인용 0** (기계 검증)
> 검수자 직접 재확인: 마이그레이션 갭 클러스터(#14·18·19·20·21) — 아래 0절

## 0. 🔴 최우선 — 저장소 스키마 재현 불가 (검수자 직접 확인)

대규모 머지가 DB 스키마를 **라이브 Supabase에만** 만들고 마이그레이션을 커밋하지 않았다. `git ls-files`로 직접 확인:

- 마이그레이션이 **0018 → 0024로 점프** — `0019`~`0023` 5개가 통째로 없음
- **`owner`(코드 21곳 참조) · `invitation`(8곳) · `provider`(4곳)** 테이블: 커밋된 마이그레이션에 CREATE **전무**
- **RPC `increment_completed_deals`**: 정의 없음(호출 2곳)
- `0024_provider_member`·`0025_service_package`·`0026_...`이 존재하지 않는 `provider` 테이블/컬럼을 FK·인덱스로 참조 → 마이그레이션 체인 자체가 실행 불가

**영향:** `supabase db reset`·신규 환경 배포·스테이징 구성 시 DB가 깨진다. 현재 프로덕션이 도는 건 라이브 DB에 수동 반영돼 있기 때문이며, 저장소만으론 복구 불가. **누락 마이그레이션(0019~0023 + provider/owner/invitation/RPC)을 커밋하는 것이 다른 모든 수정에 선행해야 한다.**

## 1. P1 — 22건 (전건 quote-back OK + 적대적 검증 통과)

클러스터로 묶으면:
- **스키마 미커밋 (0절):** #14 #18 #20 #21 (+ #19 #22 RLS)
- **동시성 — 금전/상태 compare-and-set 부재:** #3 #4 #7 #8 #9 #10 #13 #16 #17 — 매칭·정산해제·환불·리뷰·수락 전반. 읽고-검사-쓰기 사이 race, matching(request_id) UNIQUE 없음, 정산해제가 원장 이중적립·카운터 이중증가
- **관리자 인가 fail-open:** #1(대시보드 page가 인증만 확인, admin 게이트는 layout에만 — Next는 layout을 authz 경계로 쓰지 말라고 명시) #2(ADMIN_EMAILS 빈 세그먼트 → 이메일 없는 계정이 admin) #5(신규자 검수 게이트가 죽은 라우트에만)
- **수수료/익명화 노출:** #12 settlements GET이 work_fee/match_fee·expert_id 노출
- **복원력 fail-open:** #15 분쟁 확인 쿼리 에러 시 분쟁 정산을 그대로 해제
- **기타:** #6 프로필 필드 어휘 불일치(저장 필드가 편집 불가) #11 로그아웃 초빙 CTA가 404

### P1-1. Dashboard page checks authentication only; admin gate lives solely in the layout
- `apps/admin/app/(protected)/dashboard/page.tsx:36` · security · quote-back OK · ☐ untaken
- The page verifies only that a user is logged in, then reads the entire business dataset (all owner/expert emails, phone contacts, deals, settlements, ledger) via the service-role adminClient, which bypasses RLS. The ADMIN_EMAILS allowlist check exists only in apps/admin/app/(protected)/layout.tsx. Next.js explicitly documents that layouts are not a reliable auth boundary: layouts and pages render in parallel, and layouts do not re-render on partial/soft navigations, so the page's service-role qu

### P1-2. ADMIN_EMAILS check fails open: empty-email user becomes admin when env has an empty segment
- `apps/admin/app/docs/statement/[dealId]/page.tsx:34` · security · quote-back OK · ☐ untaken
- If ADMIN_EMAILS is unset, empty, or contains an empty segment (e.g. trailing comma 'a@x.com,'), the parsed array contains '' — and a logged-in user whose auth account has no email (Kakao OAuth email consent is optional, so this is attacker-controllable) yields (user.email || '').toLowerCase() === '', so [''].includes('') === true and isAdmin becomes true. isAdmin fully bypasses the owner/expert ownership check on this page, letting such a user read ANY deal's transaction statement (fees, settlem

### P1-3. selectCandidate/createMatching: TOCTOU check-then-insert can create duplicate matchings for one request
- `apps/admin/lib/admin/actions.ts:219` · concurrency & data integrity · quote-back OK · ☐ untaken
- Both selectCandidate (lines 212-251) and createMatching (lines 352-389) read request.status, check it is 'open', then insert into matching and update request.status — three separate statements, no transaction, and the final request update (lines 245-249, 383-387) has NO .eq('status','open') compare-and-set guard. The matching table has no UNIQUE on request_id (verified: supabase/migrations/0001_init.sql lines 72-80, no later migration adds one), so two concurrent invocations (admin double-click,

### P1-4. releaseSettlement: no compare-and-set on escrow_status and non-atomic multi-step release → double ledger accrual / double counter increment
- `apps/admin/lib/admin/actions.ts:505` · concurrency & data integrity · quote-back OK · ☐ untaken
- releaseSettlement reads escrow_status (line 470-479), then updates settlement to 'released' (lines 502-508) WITHOUT .eq('escrow_status','deposited')/.eq('escrow_status','reviewing') on the update — unlike confirmDepositManual directly above it (line 457) which does add the pending guard, so the omission is an inconsistency, not a pattern. Two concurrent invocations (double-click on the dashboard release button, or dashboard-load runAutoRelease racing a manual release) both pass the read check an

### P1-5. Newbie review gate and transition validation live ONLY on a dead API route — live path bypasses both
- `apps/expert/app/api/deals/[id]/workflow/[step]/route.ts:86` · security / dead-code / structure-duplication · quote-back OK · ☐ untaken
- Nothing in the repo calls this route: grep for 'api/deals' across all apps finds zero fetch callers; the UI (workflow-form.tsx) calls the server action updateWorkflowStep in apps/expert/lib/workflow/actions.ts instead. That server action has NO pending→in_progress→done transition validation, NO deal.status check, and critically NO is_newbie gate — it fetches deal.status in verifyDealExpertOwnership and never uses it, then updates deal_workflow unconditionally. Consequence today: a newbie expert 

### P1-6. FIELD_CHIPS vocabulary diverges from register page's category-synced FIELD_GROUPS — stored fields become invisible and unremovable
- `apps/expert/components/profile-editor.tsx:7` · structure-state / correctness · quote-back OK · ☐ untaken
- Registration (apps/expert/app/(main)/register/page.tsx FIELD_GROUPS, commented '대분류별 중분류 그룹 (category 테이블과 동기)') offers 28 category-table-synced fields such as '업무자동화', '품질관리', 'R&D 기획', '콘텐츠제작'. This editor renders a different flat 13-chip list that omits most of them and adds '기타', which has no depth-1 category counterpart. Consequences: (1) an expert whose stored field contains e.g. '업무자동화' opens this editor, selectedFields is seeded from profile.field.split(',') (line 44) but no chip renders

### P1-7. acceptMatching: status update has no compare-and-set — double-accept race corrupts winner's state
- `apps/expert/lib/matching/actions.ts:78` · concurrency · quote-back OK · ☐ untaken
- The action reads matching.status, checks it is 'proposed', then updates to 'accepted' with only .eq('id', matchingId) — no .eq('status', 'proposed') guard. Two concurrent accepts (double-click, two tabs) both pass the check. The loser's deal insert then fails on the UNIQUE(matching_id) constraint (database.types.ts: deal_matching_id_fkey isOneToOne), and its rollback path sets matching.status back to 'proposed' — while the winner's deal, settlement, and workflow rows exist. Result: a live deal w

### P1-8. acceptInvitation: check-then-update without CAS — losing racer's rollback() wipes the winner's invitation state
- `apps/expert/lib/invitation/actions.ts:122` · concurrency · quote-back OK · ☐ untaken
- Same pattern as acceptMatching: status read ('invited' check at line 68), then update to 'accepted' with no .eq('status','invited') guard. Under a concurrent double-accept, the loser fails at deal insert (UNIQUE deal.invitation_id, isOneToOne) and runs rollback(), which resets the invitation to status 'invited' and nulls est_hours/est_amount/cap_amount (line 113) — destroying the winner's committed state while the winner's deal+settlement+workflow rows remain. The invitation then appears accepta

### P1-9. Refund read-modify-write on refunded_amt with no compare-and-set — lost update on money
- `apps/owner/app/api/settlements/[id]/refund/route.ts:68` · concurrency-data-integrity · quote-back OK · ☐ untaken
- The route SELECTs settlement.refunded_amt, computes newRefundedAmt in app code, calls Toss cancel, then UPDATEs refunded_amt unconditionally (.eq('id', settlementId) only, no .eq('refunded_amt', old) CAS and no version column). Two concurrent refund requests with different amounts both read the stale refunded_amt (e.g. 0), both pass the over-refund guard at line 69 against the stale value, both get distinct idempotency keys (refund_{id}_{newAmt}), so BOTH cancels execute at Toss — while the DB e

### P1-10. submitReview: SELECT-then-INSERT duplicate check with no UNIQUE constraint — duplicate reviews under concurrency
- `apps/owner/lib/deal/actions.ts:63` · concurrency-data-integrity · quote-back OK · ☐ untaken
- The duplicate-review guard is a SELECT on review(deal_id, author_type) followed by an INSERT (lines 63-81). There is no UNIQUE constraint backing it: migration 0016_composite_indexes.sql line 10 creates `CREATE INDEX IF NOT EXISTS idx_review_deal_author ON review(deal_id, author_type)` — a plain index, even though its own comment says '중복 리뷰 확인 유니크 체크' (unique check for duplicate reviews). Two concurrent submissions (double-tap of the submit button, retried request) both see `existing` = null an

### P1-11. Logged-out invite CTA links to /api/auth/login, a route that does not exist — 404 now
- `apps/owner/components/invite-button.tsx:18` · correctness · quote-back OK · ☐ untaken
- The logged-out variant of the expert-detail primary action is `<a href="/api/auth/login">`. A repo-wide grep finds no app/api/auth/** handler in the owner app (or anywhere), no middleware rewrite, and proxy.ts only refreshes the Supabase session. Every logged-out visitor who clicks '로그인 후 초빙하기' lands on a 404 — the invite conversion path is broken in production. The rest of the app initiates login via the signInWithKakao/signInWithGoogle server actions (AppHeader, home CTA forms); this button sh

### P1-12. GET /api/settlements/[id] leaks work_fee/match_fee split and expert_id to the owner, breaking the documented anonymization design
- `apps/owner/app/api/settlements/[id]/route.ts:24` · security · quote-back OK · ☐ untaken
- The endpoint returns the settlement with an embedded deal containing work_fee, match_fee, total_pay and expert_id, and serves that identical payload to BOTH parties (owner path returns the same `{ settlement }` at line 62 as the expert path at line 44). quote-section.tsx documents a product invariant that the owner must only ever see total_pay — exposing the internal fee split '직거래 방지 (direct-deal prevention) 설계를 깨뜨린다' — and the status page carefully passes only total_pay. The same defect was fl

### P1-13. Webhook retry cannot repair a failed deal-status write — idempotency guard short-circuits first
- `apps/owner/lib/payments/confirm-deposit.ts:42` · concurrency-data-integrity · quote-back OK · ☐ untaken
- The documented recovery contract (header comment: DB failure after capture returns ok:false so the caller responds non-2xx and Toss re-sends the webhook) is broken for the second write. Sequence: settlement update (line 58) succeeds → deal update (line 78) fails → ok:false 500 → Toss retries → the retry now sees escrow_status='deposited' and the guard at line 42 returns ok:true alreadyProcessed BEFORE ever reaching the deal update. The deal stays 'quoted' forever: the owner paid, escrow shows de

### P1-14. Code targets 'owner'/'invitation' schema that no checked-in migration defines — migrations 0019-0023 missing
- `apps/owner/lib/invitation/actions.ts:71` · honesty · quote-back OK · ☐ untaken
- This action (and message/profile/request/services actions) reads/writes tables 'owner' and 'invitation' and inserts sender_type/author_type 'owner', but the checked-in migrations define only 'client'/'partner' (0001_init.sql line 26: create table client; deal_message CHECK allows 'partner','admin','client' in 0007) and create no 'invitation' table at all. The migration sequence jumps 0018 → 0024, and git history (git log --all on 'supabase/migrations/0019*..0023*') shows the gap files never exis

### P1-15. Dispute check fails OPEN: query error releases disputed settlements
- `packages/shared/lib/automation/auto-settlement.ts:58` · correctness / resilience / money · quote-back OK · ☐ untaken
- The open-dispute query result destructures only { data: disputes } and never checks the error. On any transient DB/network failure, disputes is null, `(disputes || [])` yields an empty Set, every reviewing settlement passes the dispute filter, and step 4 unconditionally releases them — money is released to experts on deals that have an open dispute. The guard whose entire purpose is to block release silently degrades to no-op on error. Fix: check the query error and abort the run (fail closed) w

### P1-16. Batch release UPDATE has no compare-and-set on escrow_status
- `packages/shared/lib/automation/auto-settlement.ts:81` · concurrency & data integrity · quote-back OK · ☐ untaken
- Between the SELECT (escrow_status='reviewing') and the batch UPDATE, a settlement can be refunded (payments/refund flow sets escrow_status='refunded') or otherwise transitioned. The update matches only `.in('id', eligibleIds)` with no `.eq('escrow_status', 'reviewing')`, so it overwrites that concurrent transition back to 'released' (lost update on money state). The same missing predicate means two overlapping cron invocations can both select the same rows and both proceed to steps 5-8 (double g

### P1-17. Multi-step money write is not one transaction; ledger accrual failure swallowed
- `packages/shared/lib/automation/auto-settlement.ts:100` · concurrency & data integrity / honesty · quote-back OK · ☐ untaken
- Steps 4-8 (release settlements, insert guarantee_fund_ledger accruals, audit sampling, score recalc, completed_deals increments) run as separate statements with no transaction. If the ledger insert fails, settlements are already 'released', the failure is only console.error'd, the function still reports released=N success, and there is no retry or reconciliation — the guarantee fund ledger permanently drifts from released settlements with guarantee_fee > 0. Because release already happened, the 

### P1-18. RPC increment_completed_deals (and other automation schema objects) defined nowhere in the repo
- `packages/shared/lib/automation/auto-settlement.ts:139` · correctness / honesty / portability · quote-back OK · ☐ untaken
- `increment_completed_deals` is called here and in apps/admin/lib/admin/actions.ts:542, but no migration defines it — the only SQL function in supabase/migrations is set_updated_at (0001_init.sql:7). Likewise platform_config (audit-sampling.ts), expert_activity table, and the queue_status/audit_sampled/auto_processed/completed_deals/activity_points/is_newbie columns appear in NO migration; the migrations directory jumps 0018 → 0024 and git history (all branches) never contained 0019-0023. If thes

### P1-19. provider_self_update allows updating ALL columns including status — self-approval path
- `supabase/migrations/0024_provider_member.sql:46` · security · quote-back OK · ☐ untaken
- The UPDATE policy has USING (auth_user_id = auth.uid()) and no explicit WITH CHECK, so the USING expression is reused as WITH CHECK — meaning a provider may update any column of their own row, including `status` (the admin-approval gate: pending → active) and `kind`. If RLS is enforced on the provider table, any pending provider with the anon key can call PostgREST directly (`update provider set status='active' where auth_user_id=uid`) and bypass the admin approval process entirely ("관리자 승인제" pe

### P1-20. FK references `provider` table that no committed migration creates — chain unreproducible
- `supabase/migrations/0025_service_package.sql:12` · correctness · quote-back OK · ☐ untaken
- No migration in the repo creates a `provider` table (0001_init.sql creates `client` and `partner`; grep across all migrations finds no CREATE TABLE provider and no RENAME). Migrations 0019-0023 are absent and `git log --all --diff-filter=AD` shows they never existed in git history, yet 0024 comments reference '0019의 테이블 루프' and ALTERs `provider`. A fresh `supabase db reset` / CI / staging rebuild / disaster-recovery restore from this repo fails at 0024/0025 with 'relation provider does not exist

### P1-21. Index on `service_order.provider_id` — column never added by any committed migration
- `supabase/migrations/0026_service_order_package_fk.sql:19` · correctness · quote-back OK · ☐ untaken
- `service_order` as created in 0005_service_orders.sql has `client_id` and `partner_id` but no `provider_id`, and no committed migration adds one (grep across all migrations). The column must have been added by the missing 0019-0023 migrations, so on a rebuilt-from-repo database this CREATE INDEX fails with 'column provider_id does not exist'. Same root cause as the 0025 finding (missing migrations 0019-0023); also the header comment's snapshot list '스냅샷(package_slug/name/price/is_free)' names an

### P1-22. RLS FOR ALL policy lets a partner self-publish via direct PostgREST, bypassing the admin gate
- `supabase/migrations/0025_service_package.sql:43` · security · quote-back OK · ☐ untaken
- The file header states the design: partners register drafts and the admin publishes ('파트너가 직접 서비스를 등록(draft)하고 관리자가 publish'). But `service_package_owner_all` is FOR ALL with no column/status restriction, so any authenticated user whose auth.uid matches a provider row can INSERT/UPDATE/DELETE their own rows directly through Supabase's REST API (a browser client with the publishable key exists in packages/shared/lib/supabase/client.ts, and partners get real auth sessions via the partner OAuth cal

## 2. P2 — 70건

| # | file:line | 차원 | 제목 |
|---|---|---|---|
| 1 | `apps/admin/app/(protected)/dashboard/matching-tab.tsx:82` | correctness/race | Candidate fetch has no response-to-request correlation: stale response renders under the w |
| 2 | `apps/admin/app/(protected)/dashboard/dispute-tab.tsx:58` | honesty | resolveDispute result discarded — failures are silently swallowed and look like success |
| 3 | `apps/admin/app/(protected)/dashboard/page.tsx:118` | performance | expert_interest fetched as full table (no filter, no limit) just to count per request |
| 4 | `apps/admin/app/(protected)/dashboard/page.tsx:56` | performance | Guarantee fund balance computed by fetching every ledger row and summing in JS |
| 5 | `apps/admin/app/(protected)/dashboard/progress-tab.tsx:69` | correctness/concurrency | Message thread fetch has no stale-response guard; one shared messages state for all deals |
| 6 | `apps/admin/app/(protected)/dashboard/service-tab.tsx:56` | structure/state | Props copied into useState — server refresh never reaches the rendered list (two sources o |
| 7 | `apps/admin/app/(protected)/review-input/[id]/page.tsx:21` | security | Service-role data reads gated only by login check; admin allowlist lives in layout only |
| 8 | `apps/admin/app/api/admin/matching/route.ts:48` | concurrency-data-integrity / | Matching creation is non-atomic and the second write's failure is silently swallowed |
| 9 | `apps/admin/app/api/admin/matching/route.ts:28` | concurrency-data-integrity | SELECT-then-INSERT race: status guard has no DB-level backing (no UNIQUE, no compare-and-s |
| 10 | `apps/admin/app/api/reviews/route.ts:22` | concurrency & data integrity | Duplicate-review guard is SELECT-then-INSERT with no UNIQUE constraint backing it |
| 11 | `apps/admin/app/api/expert-activity/route.ts:51` | concurrency & data integrity | Activity insert + points recalc is two steps, not atomic; failure leaves stored derived va |
| 12 | `apps/admin/app/page.tsx:13` | structure/state | Admin-email predicate duplicated inline in 5 files with no shared helper or pinning test |
| 13 | `apps/admin/app/docs/statement/[dealId]/page.tsx:96` | correctness | Transaction-statement dates rendered in server timezone, not KST |
| 14 | `apps/admin/app/partner/dashboard/order-labels.ts:1` | structure/state | ORDER_STATUS_LABELS duplicated verbatim from shared labels with no pinning test |
| 15 | `apps/admin/app/partner/dashboard/services/[id]/page.tsx:37` | correctness | Post-publication edits bypass the admin review gate |
| 16 | `apps/admin/lib/admin/actions.ts:151` | concurrency & data integrity | generateAiCandidates: SELECT-then-INSERT existence check with no UNIQUE constraint backing |
| 17 | `apps/admin/lib/admin/actions.ts:582` | concurrency & data integrity | submitReview: duplicate-review guard is check-then-insert; review table has no UNIQUE(deal |
| 18 | `apps/admin/proxy.ts:16` | correctness | Supabase SSR cookie pattern deviation: refreshed tokens written only to the response, not  |
| 19 | `apps/expert/app/(browse)/requests/page.tsx:55` | correctness | Depth-2 or unknown category param silently applies NO filter, showing all requests |
| 20 | `apps/expert/app/(main)/invitations/[id]/invitation-actions.tsx:22` | honesty / swallowed error | declineInvitation error result ignored — UI stuck on '거절 중...' forever with no message |
| 21 | `apps/expert/app/(browse)/requests/page.tsx:75` | correctness / honesty | Search sanitizer strips only % and , — other PostgREST .or() metacharacters can break the  |
| 22 | `apps/expert/app/(browse)/requests/[id]/page.tsx:66` | structure / performance | Breadcrumb reimplemented with an uncached full category-table fetch, duplicating shared ge |
| 23 | `apps/expert/app/(main)/matching/[id]/page.tsx:71` | correctness/honesty | Fee hidden when budget_hope is null/0, yet accepting still creates a deal with a 100,000원  |
| 24 | `apps/expert/app/(main)/work/[id]/message-thread.tsx:40` | honesty / correctness | sendDealMessage awaited without try/catch — network failure leaves optimistic message show |
| 25 | `apps/expert/app/(main)/work/[id]/message-thread.tsx:21` | structure/state — stale prop | Thread state seeded from props and never re-synced; the action's revalidatePath refresh is |
| 26 | `apps/expert/app/api/disputes/route.ts:54` | concurrency-data-integrity | Duplicate-dispute check is SELECT-then-INSERT with no UNIQUE idempotency key |
| 27 | `apps/expert/app/opengraph-image.tsx:3` | correctness / deprecation | Edge runtime export is deprecated in this Next 16 version |
| 28 | `apps/expert/app/api/disputes/route.ts:49` | test-gaps | Ownership/authz and duplicate-guard logic has zero tests (apps/expert has no test files at |
| 29 | `apps/expert/lib/expert/actions.ts:77` | correctness | expert_category sync: unchecked delete-then-insert — a failed insert silently strips the e |
| 30 | `apps/expert/lib/expert/actions.ts:76` | structure/state | expert.field and expert_category are two sources of truth that drift when no labels match |
| 31 | `apps/expert/lib/matching/actions.ts:56` | correctness | acceptMatching never checks request.status — a request already 'dealt' can receive a secon |
| 32 | `apps/expert/lib/interest/actions.ts:42` | concurrency | expressInterest limit: count-then-insert race, and the count includes interests on already |
| 33 | `apps/expert/lib/workflow/actions.ts:42` | security/correctness | updateWorkflowStep ignores deal.status — expert can rewrite workflow steps after inspectio |
| 34 | `apps/expert/lib/matching/actions.ts:32` | test-gaps | Zero tests over the money-path deal creation and its manual rollback logic |
| 35 | `apps/owner/app/(browse)/experts/page.tsx:65` | correctness/honesty | '__none__' sentinel against a uuid column relies on a swallowed 400 error |
| 36 | `apps/owner/app/(browse)/experts/page.tsx:74` | correctness/security | Search string interpolated into PostgREST .or() — parens break the query, commas silently  |
| 37 | `apps/owner/app/(auth)/callback/route.ts:37` | concurrency & data integrity | SELECT-then-INSERT for owner record instead of upsert on the UNIQUE key |
| 38 | `apps/owner/app/(browse)/experts/page.tsx:24` | performance | Full-table fetch of expert_category on every request plus O(n·m) find loop |
| 39 | `apps/owner/app/(browse)/services/page.tsx:5` | performance / structure-stat | Same DB query executed twice per page load; providers derivable from packages already held |
| 40 | `apps/owner/app/(main)/request/request-form.tsx:9` | structure-state / duplicated | Category tree hardcoded in form; DB category table is the source, matched by exact label w |
| 41 | `apps/owner/app/(main)/mypage/page.tsx:98` | honesty / swallowed errors | All six dashboard queries swallow errors — DB failure renders as fake empty state |
| 42 | `apps/owner/app/(main)/status/[id]/message-thread.tsx:27` | structure/state | Message list state captured once from props — revalidated server data never reaches the re |
| 43 | `apps/owner/app/(main)/status/page.tsx:74` | honesty | DB query error rendered as a fake empty state ('아직 등록한 의뢰가 없습니다') |
| 44 | `apps/owner/app/(main)/status/[id]/page.tsx:51` | honesty | All query errors on the detail page swallowed — infra failure masquerades as not-found/no- |
| 45 | `apps/owner/app/api/settlements/[id]/refund/route.ts:121` | correctness | Guarantee-fund ledger cap applied per refund call, not cumulatively — over-records fund pa |
| 46 | `apps/owner/app/api/disputes/route.ts:62` | concurrency-data-integrity | Duplicate-dispute guard is SELECT-then-INSERT with no UNIQUE constraint backing it |
| 47 | `apps/owner/app/api/settlements/[id]/refund/route.ts:13` | test-gaps | Zero tests on any money path — refund cap, confirm idempotency, webhook signature, orderId |
| 48 | `apps/owner/lib/deal/actions.ts:28` | honesty | requestRevision: deal_message insert result ignored — notification can silently fail after |
| 49 | `apps/owner/lib/deal/actions.ts:41` | test-gaps | Zero tests over owner deal actions — money-adjacent authz/state flows (approve, confirm, r |
| 50 | `apps/owner/lib/payments/confirm-deposit.ts:81` | concurrency-data-integrity | Deal status write is read-then-write with no compare-and-set across a 15s external call |
| 51 | `apps/owner/lib/invitation/actions.ts:88` | concurrency-data-integrity | Invitation cap and duplicate guard are SELECT-count-then-INSERT races |
| 52 | `apps/owner/proxy.ts:16` | correctness | Refreshed auth cookies not synced back to the request — stale token reaches server compone |
| 53 | `apps/owner/lib/invitation/actions.ts:38` | structure-state | Owner auto-create block duplicated verbatim in 3 files, with insert error swallowed |
| 54 | `apps/owner/lib/payments/confirm-deposit.ts:16` | test-gaps | Escrow money path has zero automated tests despite a documented idempotency contract |
| 55 | `packages/shared/lib/automation/auto-settlement.ts:45` | structure/state design / cor | 3-day release timer derived from mutable updated_at, not a stored reviewing-transition fac |
| 56 | `packages/shared/lib/automation/audit-sampling.ts:23` | resilience / honesty | Non-numeric audit_sample_rate silently disables all auditing for 5 minutes at a time |
| 57 | `packages/shared/lib/automation/audit-sampling.ts:71` | honesty | Returns sampledIds as 'audited' even when the marking UPDATE failed |
| 58 | `packages/shared/lib/deal/deal-operations.ts:24` | concurrency & data integrity | Deal status transitions use SELECT-then-UPDATE with no compare-and-set |
| 59 | `packages/shared/lib/deal/deal-operations.ts:112` | honesty / correctness | requestRevisionOp ignores its UPDATE result entirely and returns success |
| 60 | `packages/shared/lib/cap-pricing.ts:18` | test-gaps | Zero tests anywhere in the repo for the money-math and scoring pure functions |
| 61 | `packages/shared/lib/landing-stats.ts:75` | honesty | Query errors swallowed as zeros, then cached for 10 minutes |
| 62 | `packages/shared/lib/landing-stats.ts:69` | performance | Full-table row fetches for averages/counts, silently capped at PostgREST's 1000-row limit |
| 63 | `packages/shared/lib/payment.ts:173` | resilience | cancelPayment throws on timeout/network failure, breaking its own {success:false} contract |
| 64 | `packages/shared/lib/query-types.ts:8` | structure/state | Hand-maintained mirror of DB query shapes with no pinning test — drift already happened on |
| 65 | `packages/shared/lib/pricing.ts:6` | test-gaps | Zero tests on money-critical pure functions (fee bands, VAT, orderId round-trip) |
| 66 | `packages/shared/lib/service-package/queries.ts:78` | honesty | getPackageBySlug swallows DB errors entirely — outage rendered as 404 |
| 67 | `packages/shared/lib/service-package/queries.ts:67` | structure/state | Hand-written DB row mirror + double cast defeats generated types — drift has no pinning te |
| 68 | `supabase/migrations/0007_owner_engagement.sql:18` | security | client_send_message RLS policy does not bind sender_id — attribution spoofing |
| 69 | `supabase/migrations/0024_provider_member.sql:12` | correctness | Migration alters table `provider` that no migration in the repo creates; RLS enable state  |
| 70 | `supabase/migrations/0025_service_package.sql:13` | data-integrity | Slug immutability is comment-only policy — nothing in the DB enforces it |

## 3. P3 — 120건 (원본: 워크플로 출력)

| # | file:line | 차원 | 제목 |
|---|---|---|---|
| 1 | `apps/admin/app/(protected)/dashboard/page.tsx:242` | performance | All deal_message rows fetched to derive per-deal counts |
| 2 | `apps/admin/app/(protected)/dashboard/page.tsx:39` | concurrency/design | State-mutating automation runs as a side effect of a GET page render |
| 3 | `apps/admin/app/(protected)/dashboard/dashboard-tabs.tsx:72` | correctness | '즉시 처리' banner counts disputes + overdue settlements but navigates only to the d |
| 4 | `apps/admin/app/(protected)/dashboard/invitation-tab.tsx:95` | correctness | est_hours rendered under a cap_amount-only null guard — can print 'nullh' |
| 5 | `apps/admin/app/(protected)/dashboard/inquiry-tab.tsx:39` | honesty | closeInquiry failure is silently dropped — no user-visible error |
| 6 | `apps/admin/app/(protected)/dashboard/matching-tab.tsx:266` | correctness | Assign button has no in-flight disable — double-click fires the action twice |
| 7 | `apps/admin/app/(protected)/dashboard/matching-tab.tsx:85` | structure | autoAssignAt read via type-assertion escape — action return type doesn't declare |
| 8 | `apps/admin/app/(protected)/dashboard/page.tsx:329` | structure | Query→component shape bridged with 'as unknown as' double cast, no pinning |
| 9 | `apps/admin/app/(protected)/review-input/[id]/review-form.tsx:61` | structure/state | AI suggestion auto-seeded into form state, making the explicit 'AI 제안 적용' button |
| 10 | `apps/admin/app/(protected)/dashboard/progress-tab.tsx:110` | structure/state | Message-count badge is a server snapshot that goes stale after the optimistic re |
| 11 | `apps/admin/app/(protected)/review-input/[id]/page.tsx:35` | test-gaps/correctness | Reviewable-state invariant (all 5 workflow steps done) enforced only by the dash |
| 12 | `apps/admin/app/api/admin/matching/route.ts:33` | structure-state / correc | API route duplicates the server-action matching flow but skips matching_candidat |
| 13 | `apps/admin/app/api/admin/matching/route.ts:10` | correctness / boundary-v | Unguarded request.json() and no schema validation of body |
| 14 | `apps/admin/app/api/admin/deals/route.ts:24` | security / honesty | Raw Supabase error.message returned to the client |
| 15 | `apps/admin/app/api/admin/deals/route.ts:6` | security | Timing-unsafe admin secret comparison (===/!== instead of constant-time compare) |
| 16 | `apps/admin/app/api/admin/deals/route.ts:21` | performance | Unbounded queries — no limit/pagination on deals or workflows |
| 17 | `apps/admin/app/(public)/ax-process/page.tsx:81` | honesty / structure-stat | Silent fallback to hardcoded production URLs when NEXT_PUBLIC_* env is unset |
| 18 | `apps/admin/app/docs/quote/[dealId]/page.tsx:40` | security (row authz) | Owner ownership check skipped entirely when deal.request_id is null |
| 19 | `apps/admin/app/api/reviews/route.ts:17` | correctness (boundary va | rating accepts non-integers and numeric strings; DB error leaks via error.messag |
| 20 | `apps/admin/app/api/expert-activity/route.ts:18` | correctness (boundary va | req.json() outside the try block; malformed body crashes to an unhandled 500 |
| 21 | `apps/admin/app/api/inquiry/route.ts:22` | correctness (boundary va | No length cap on content/category; unguarded JSON parse; sequential profile look |
| 22 | `apps/admin/app/api/admin/requests/route.ts:13` | performance | Unbounded query — no .limit() on the requests list |
| 23 | `apps/admin/app/api/admin/requests/route.ts:6` | structure/state (two sou | Two parallel admin-auth mechanisms across sibling admin endpoints; non-constant- |
| 24 | `apps/admin/app/docs/quote/[dealId]/page.tsx:54` | performance | request row fetched twice and six sequential round-trips for one page |
| 25 | `apps/admin/app/layout.tsx:79` | security | Third-party chatbot script loaded on every admin route, including the protected  |
| 26 | `apps/admin/app/docs/statement/[dealId]/page.tsx:50` | performance | Three independent lookups (settlement, request title, expert) serialized instead |
| 27 | `apps/admin/app/partner/dashboard/orders/page.tsx:16` | security | Dashboard pages check provider existence only; active-status gate lives solely i |
| 28 | `apps/admin/app/partner/dashboard/profile/page.tsx:11` | honesty | Save-success signal (?success=saved) is emitted but never consumed — silent succ |
| 29 | `apps/admin/app/partner/dashboard/page.tsx:16` | performance | Provider row fetched twice per request (layout + page) with no request-level ded |
| 30 | `apps/admin/app/partner/dashboard/orders/page.tsx:23` | correctness | Search only covers the 100 most recent orders; truncation is invisible |
| 31 | `apps/admin/app/partner/dashboard/services/page.tsx:12` | dead-code | ?success= redirect param has no consumer — success feedback silently dropped |
| 32 | `apps/admin/app/partner/dashboard/services/page.tsx:18` | security | Page guard checks provider existence, not active status — leans on layout guard  |
| 33 | `apps/expert/app/(auth)/callback/route.ts:37` | correctness | OAuth callback: check-then-insert of expert row treats a unique-violation race a |
| 34 | `apps/expert/app/(auth)/callback/route.ts:40` | correctness | user.email! non-null assertion: OAuth account without email makes signup permane |
| 35 | `apps/admin/lib/admin/actions.ts:73` | performance | Unbounded full-table fetch of expert_category (and all active experts with selec |
| 36 | `apps/expert/app/(browse)/requests/[id]/page.tsx:17` | security / information d | generateMetadata fetches the title WITHOUT the status='open' filter the page bod |
| 37 | `apps/expert/app/(browse)/requests/[id]/interest-button.tsx:21` | resilience | Server-action awaits have no try/finally — a rejected action leaves the button p |
| 38 | `apps/expert/app/(browse)/requests/request-list.tsx:73` | resilience | Same missing try/finally as interest-button — rejected action leaves that row's  |
| 39 | `apps/expert/app/(main)/invitations/[id]/invitation-actions.tsx:64` | correctness / test-gap | est_hours submits via hidden input, so the visible number input's min/max HTML v |
| 40 | `apps/expert/app/(browse)/requests/request-list.tsx:46` | structure / state | interested Set seeded from props into useState and never re-synced — two sources |
| 41 | `apps/expert/app/(browse)/education/education-view.tsx:134` | honesty / configuration | Silent fallback to hardcoded production URL when NEXT_PUBLIC_ADMIN_URL is unset |
| 42 | `apps/expert/app/(browse)/requests/page.tsx:51` | correctness / UX | Hard limit(50) with no pagination or truncation indicator |
| 43 | `apps/expert/app/(main)/matching/opportunity-section.tsx:22` | structure/state | Interest state copied from prop and never re-synced; server revalidation targets |
| 44 | `apps/expert/app/(main)/invitations/[id]/page.tsx:17` | security | generateMetadata resolves owner company/CEO name via adminClient with no auth or |
| 45 | `apps/expert/app/(main)/invitations/[id]/page.tsx:55` | performance/security | Ownership verified by a redundant second query after the full row was already fe |
| 46 | `apps/expert/app/(main)/matching/opportunity-section.tsx:59` | concurrency | Single shared `loading` id: a second concurrent click clears the first's complet |
| 47 | `apps/expert/app/(main)/invitations/page.tsx:41` | correctness/test-gap | Hard limit(20) with no pagination or truncation indicator |
| 48 | `apps/expert/app/(main)/matching/loading.tsx:1` | dead-code/structure | Stale loading skeleton: mirrors the removed /matching list page, but the segment |
| 49 | `apps/expert/app/(main)/work/[id]/workflow-form.tsx:55` | honesty / fake success | Missing deal_workflow row renders as toggleable 'pending'; toggling fakes succes |
| 50 | `apps/expert/app/api/deals/[id]/workflow/[step]/route.ts:63` | correctness / concurrenc | Route skips transition validation when the row is missing, fakes success on 0-ro |
| 51 | `apps/expert/app/(main)/work/[id]/workflow-form.tsx:43` | structure/state — partia | Error rollback restores status but not the optimistically-set done_at |
| 52 | `apps/expert/app/(main)/mypage/page.tsx:16` | dead-code | DEAL_STATUS_COLORS carries a 'cancelled' key that the deal_status enum cannot pr |
| 53 | `apps/expert/app/(main)/mypage/page.tsx:261` | correctness — wrong link | '교육·서비스 현황' section's 전체 보기 links to /matching, which is the matching page, not  |
| 54 | `apps/expert/app/(main)/work/[id]/page.tsx:82` | correctness — nondetermi | Dispute target review picked with limit(1) and no ORDER BY — arbitrary when both |
| 55 | `apps/expert/app/api/expert/profile/route.ts:6` | dead-code | GET /api/expert/profile has no callers anywhere in the repo |
| 56 | `apps/expert/app/api/disputes/route.ts:76` | security | Raw DB error message returned to the client |
| 57 | `apps/expert/app/api/disputes/route.ts:30` | correctness | Malformed JSON body throws before validation — 500 instead of 400 |
| 58 | `apps/expert/app/api/disputes/route.ts:11` | structure-state | Route is a near-identical copy of apps/owner/app/api/disputes/route.ts with no p |
| 59 | `apps/expert/components/profile-editor.tsx:118` | honesty | Unset hourly_rate silently becomes 25000 on any profile save |
| 60 | `apps/expert/app/expert-dashboard.tsx:82` | performance | workingCount query waterfalls after the Promise.all instead of joining it |
| 61 | `apps/expert/app/page.tsx:38` | performance | Landing stats and education packages fetched sequentially |
| 62 | `apps/expert/app/page.tsx:25` | honesty / correctness | Expert lookup error is swallowed — a transient DB failure misroutes an existing  |
| 63 | `apps/expert/lib/education/actions.ts:48` | concurrency | createEducationOrder has no idempotency — double submit creates duplicate paid o |
| 64 | `apps/expert/lib/workflow/actions.ts:58` | structure/state | Reverting a step from 'done' leaves stale done_at |
| 65 | `apps/expert/lib/education/actions.ts:61` | honesty | service_order insert failure swallowed without logging |
| 66 | `apps/expert/lib/expert/actions.ts:37` | correctness | NaN bypasses the hourly_rate range validation |
| 67 | `apps/expert/lib/interest/actions.ts:40` | resilience | Non-numeric max_active_interests config silently disables the limit |
| 68 | `apps/expert/lib/message/actions.ts:68` | correctness | sendMatchingInquiry has no content length cap (sendDealMessage caps at 1000) |
| 69 | `apps/expert/lib/interest/actions.ts:9` | structure | getExpertId auth helper duplicated in 5+ action files |
| 70 | `apps/owner/app/(browse)/experts/expert-list.tsx:58` | correctness | Count copy claims total population but the query is capped at limit(50) |
| 71 | `apps/owner/app/(browse)/experts/[id]/page.tsx:29` | security/performance | generateMetadata skips the status check and duplicates the expert fetch |
| 72 | `apps/owner/app/(auth)/callback/route.ts:40` | correctness | user.email! non-null assertion feeds a NOT NULL column — email-less OAuth users  |
| 73 | `apps/owner/app/(browse)/experts/expert-card.tsx:14` | structure/state | GRADE_LABEL map duplicated in two files with no pinning test |
| 74 | `apps/expert/proxy.ts:5` | structure/state | proxy.ts duplicated verbatim across apps with no shared source or pinning test |
| 75 | `apps/owner/app/(browse)/experts/page.tsx:77` | honesty | All Supabase query errors on the experts page are discarded — failures render as |
| 76 | `apps/owner/app/(browse)/services/[slug]/service-detail-view.tsx:38` | correctness / structure- | Two definitions of 'free': detail page uses price===0 while list card uses store |
| 77 | `apps/owner/app/(browse)/services/services-view.tsx:8` | correctness | 'education' category packages unreachable in browse mode though counted on provi |
| 78 | `apps/owner/app/(browse)/services/services-view.tsx:121` | dead-code | expandedProvider === null branch in filter is unreachable |
| 79 | `apps/owner/app/(browse)/services/[slug]/service-detail-view.tsx:13` | structure-state / duplic | Hardcoded production-URL fallback duplicated in two files |
| 80 | `apps/owner/app/(main)/status/[id]/message-thread.tsx:50` | correctness | Error rollback overwrites whatever the user typed while the send was in flight |
| 81 | `apps/owner/app/(main)/status/[id]/payment-button.tsx:50` | resilience | Checkout fetch has no timeout/abort — a hung server leaves the money button disa |
| 82 | `apps/owner/app/(main)/status/page.tsx:52` | dead-code | owner.email selected but never used |
| 83 | `apps/owner/app/(main)/status/[id]/message-thread.tsx:31` | test-gaps | Optimistic send/rollback logic has no test |
| 84 | `apps/owner/app/api/settlements/[id]/refund/route.ts:78` | honesty | Missing payment_key silently skips the Toss cancel yet still marks the settlemen |
| 85 | `apps/owner/app/api/settlements/[id]/refund/route.ts:37` | correctness | Refund amount not validated as an integer number — floats and non-number types p |
| 86 | `apps/owner/app/api/payments/checkout/route.ts:69` | honesty | settlement.payment_key update result silently discarded |
| 87 | `apps/owner/app/api/requests/route.ts:74` | correctness | budget_hope validated to 2^53 but the column is 32-bit int — values over 2,147,4 |
| 88 | `apps/owner/app/api/requests/route.ts:83` | correctness | req_type and scope inserted with no validation or length cap |
| 89 | `apps/owner/app/api/requests/route.ts:89` | security | Raw Postgres error.message returned to the client |
| 90 | `apps/owner/app/api/payments/fail/route.ts:20` | security | Unauthenticated dealId → request_id lookup oracle |
| 91 | `apps/owner/app/api/disputes/route.ts:33` | performance | Dispute reason has no length cap — unbounded text stored per request |
| 92 | `apps/owner/app/api/settlements/[id]/route.ts:22` | security | settlement select('*') returns payment_key (PSP payment credential) to both owne |
| 93 | `apps/owner/lib/deal/actions.ts:83` | honesty | Raw Postgres error message returned to the UI; NaN/float rating slips past the a |
| 94 | `apps/owner/app/api/settlements/[id]/route.ts:29` | honesty | All adminClient query errors swallowed — DB outage is reported as 404/403 |
| 95 | `apps/owner/lib/message/actions.ts:64` | security | Raw DB error message returned to the client |
| 96 | `apps/owner/lib/message/actions.ts:77` | correctness | sendDealInquiry has no content length cap |
| 97 | `apps/owner/lib/request/actions.ts:29` | correctness | budget_hope accepts NaN and negative values without validation |
| 98 | `packages/shared/lib/deal/deal-operations.ts:81` | concurrency & data integ | confirmDealOp compensation write is fire-and-forget; its failure leaves deal='do |
| 99 | `packages/shared/lib/expert-activity.ts:111` | honesty / correctness | batchExpireAndRecalc return value overcounts recalced when expert updates fail |
| 100 | `packages/shared/lib/categories.ts:32` | dead-code | getCategoryBreadcrumb is dead code — exported with zero callers |
| 101 | `packages/shared/lib/expert-scoring.ts:131` | structure/state design | Score/is_newbie computation duplicated between single and batch recalc paths wit |
| 102 | `packages/shared/lib/auth/server-helpers.ts:52` | honesty / resilience | verifyDealOwnership swallows DB errors — outage reads as '거래 정보를 찾을 수 없습니다' |
| 103 | `packages/shared/lib/automation/auto-settlement.ts:38` | resilience of outbound c | No explicit timeout on any outbound Supabase call in the cron path |
| 104 | `packages/shared/lib/cap-pricing.ts:19` | correctness / money | estAmount not rounded to integer won when hourlyRate × estHours is fractional |
| 105 | `packages/shared/lib/matching-algo.ts:116` | correctness | null === null in category fallback grants spurious +5 to unrelated experts |
| 106 | `packages/shared/lib/landing-stats.ts:47` | correctness | "This month" boundary computed in server-local timezone |
| 107 | `packages/shared/lib/provider/auth.ts:15` | honesty | getProviderByAuthUser conflates DB failure with "no provider" |
| 108 | `packages/shared/lib/review-algo.ts:101` | honesty | Comment claims response-time calculation; code only counts message volume |
| 109 | `packages/shared/lib/service-package/queries.ts:65` | honesty | Catalog query failure silently degrades to empty catalog (fakes success) |
| 110 | `packages/shared/lib/service-package/queries.ts:86` | performance | getProvidersByAudience refetches the full package list — no request-scoped dedup |
| 111 | `packages/shared/lib/service-package/queries.ts:56` | resilience | Outbound Supabase calls have no explicit timeout |
| 112 | `packages/ui/components/collapsible-section.tsx:26` | correctness | aria-controls points at a non-existent element while collapsed |
| 113 | `packages/shared/lib/supabase/server.ts:27` | structure/state | Production cookie domain hardcoded as fallback in shared code |
| 114 | `packages/ui/components/page-hero.tsx:36` | correctness | Destructured split drops title text when highlight occurs more than once |
| 115 | `packages/ui/components/workflow-checklist.tsx:15` | structure/state | Workflow step order hardcoded, duplicating the generated enum with no pinning te |
| 116 | `packages/ui/components/money-input.tsx:40` | correctness | Number() round-trip loses precision beyond 15 digits; no length cap on amount in |
| 117 | `packages/ui/components/toast.tsx:48` | correctness | router.replace when stripping toast param drops the URL hash |
| 118 | `packages/ui/components/search-box.tsx:26` | structure/state | Search value seeded once from defaultValue — stale if URL query changes without  |
| 119 | `supabase/migrations/0026_service_order_package_fk.sql:8` | performance | No index on the new `service_order.service_package_id` FK column |
| 120 | `supabase/migrations/0025_service_package.sql:41` | test-gaps | New RLS policies have zero coverage in the RLS test suite |

## 처리 우선순위
1. **0절 — 누락 마이그레이션 커밋** (재현·배포 복구, 최우선. 라이브 Supabase에서 스키마 덤프 → 0019~0026 정리)
2. **관리자 인가 3건**(#1 #2 #5) — PII·정산 노출
3. **동시성 금전경로**(#3 #4 #9 #16 #17) — CAS 가드 + UNIQUE 제약 + 트랜잭션
4. **복원력**(#15 분쟁 fail-open) · **노출**(#12)
5. P2 70건(correctness 16 · honesty 11 · structure 10 · test-gaps 7 · 동시성 12 · perf 5 …)

*방법: 183파일 24그룹 → fable 리더 팬아웃 → 적대적 검증(9 기각) → 커버리지 크리틱(미독 0) → quote-back 212/212. 스키마 갭은 검수자가 git ls-files로 직접 재확인. 진단 전용, 코드 변경 없음. UX 축은 docs/10 별도.*