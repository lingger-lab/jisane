# 코드 축 전수 검수 2026-08-10 — scope=code set=diff(7/16↓) model=fable
> 기준: `~/.claude/rules` (golden-principles·quality-gates·coding-style·quality-model) + `/audit` 스킬
> 범위: docs/07(7/16 감사) 이후 변경분 **183파일 / 20,403라인** — 병렬 대규모 머지(partner→owner/expert 개명·파트너공간) + 결제 작업 전부
> coverage: 183/183 판독(24그룹) · confirmed **P1 22 · P2 70 · P3 120** · refuted 9 · 미독 0
> quote-back: **212/212 OK, 허위 인용 0** (기계 검증)
> 검수자 직접 재확인: 마이그레이션 갭 클러스터 — 아래 0절

## 0. 🔴 최우선 — 저장소 스키마 재현 불가 (검수자 직접 확인)

대규모 머지가 DB 스키마를 **라이브 Supabase에만** 만들고 마이그레이션을 커밋하지 않았다. `git ls-files`로 직접 확인:

- 마이그레이션이 **0018 → 0024로 점프** — `0019`~`0023` 5개가 통째로 없음
- **`owner`(코드 21곳 참조) · `invitation`(8곳) · `provider`(4곳)** 테이블: 커밋된 마이그레이션에 CREATE **전무**
- **RPC `increment_completed_deals`**: 정의 없음(호출 2곳)
- `0024_provider_member`·`0025_service_package`·`0026_...`이 존재하지 않는 `provider` 테이블/컬럼을 FK·인덱스로 참조 → 마이그레이션 체인 자체가 실행 불가

**영향:** `supabase db reset`·신규 환경 배포·스테이징 구성 시 DB가 깨진다. 현재 프로덕션이 도는 건 라이브 DB에 수동 반영돼 있기 때문이며, 저장소만으론 복구 불가. **누락 마이그레이션(0019~0023 + provider/owner/invitation/RPC)을 커밋하는 것이 다른 모든 수정에 선행해야 한다.**

## 1. P1 — 22건 (전건 quote-back OK + 적대적 검증 통과)

클러스터: **스키마 미커밋(0절):** #14 #18 #20 #21 #19 #22 · **동시성 CAS 부재:** #3 #4 #7 #8 #9 #10 #13 #16 #17 · **관리자 인가 fail-open:** #1 #2 #5 · **수수료/익명화 노출:** #12 · **복원력 fail-open:** #15 · **기타:** #6 #11

### P1-1. Dashboard page checks authentication only; admin gate lives solely in the layout
- `apps/admin/app/(protected)/dashboard/page.tsx:36` · security · quote-back OK · ☐ untaken
- 인용: `  if (!user) redirect('/')`
- **문제:** The page verifies only that a user is logged in, then reads the entire business dataset (all owner/expert emails, phone contacts, deals, settlements, ledger) via the service-role adminClient, which bypasses RLS. The ADMIN_EMAILS allowlist check exists only in apps/admin/app/(protected)/layout.tsx. Next.js explicitly documents that layouts are not a reliable auth boundary: layouts and pages render in parallel, and layouts do not re-render on partial/soft navigations, so the page's service-role queries execute for any authenticated non-admin who requests this segment. The mutation actions (autoAssignOverdue etc.) each call verifyAdmin() and are safe, but the read path — which carries PII of every owner and expert — is gated one level too far from the data. The admin check (or verifyAdmin()) should be repeated in the page before the adminClient queries.
- **검증:** page.tsx:36 checks only login; the ADMIN_EMAILS gate exists solely in (protected)/layout.tsx:15-18. proxy.ts (Next 16 middleware) does session refresh only — no admin check. Worse than claimed: Promise.allSettled at page.tsx:39 swallows verifyAdmin()'s throw from autoAssignOverdue/runAutoRelease, so for an authenticated non-admin the page proceeds to execute all service-role PII queries. Next.js documents layouts as not a reliable auth boundary. P1 (defense-in-depth security defect; direct RSC-payload exploitability not proven, so not P0).

### P1-2. ADMIN_EMAILS check fails open: empty-email user becomes admin when env has an empty segment
- `apps/admin/app/docs/statement/[dealId]/page.tsx:34` · security · quote-back OK · ☐ untaken
- 인용: `  const isAdmin = (process.env.ADMIN_EMAILS || '').split(',').map(e => e.trim().toLowerCase()).includes((user.email || '').toLowerCase())`
- **문제:** If ADMIN_EMAILS is unset, empty, or contains an empty segment (e.g. trailing comma 'a@x.com,'), the parsed array contains '' — and a logged-in user whose auth account has no email (Kakao OAuth email consent is optional, so this is attacker-controllable) yields (user.email || '').toLowerCase() === '', so [''].includes('') === true and isAdmin becomes true. isAdmin fully bypasses the owner/expert ownership check on this page, letting such a user read ANY deal's transaction statement (fees, settlement dates, refund amounts, expert name) via adminClient which bypasses RLS. This is a fail-open on an authz boundary; per the repo's own rules the env should be validated/thrown on unset, and the membership check should reject empty emails explicitly. The identical predicate exists in apps/admin/app/(auth)/callback/route.ts, (protected)/layout.tsx, and docs/quote/[dealId]/page.tsx — all share the same fail-open.
- **검증:** Quote verbatim at statement page.tsx:34. Empty ADMIN_EMAILS segment (unset env or trailing comma) yields [''] and an email-less user yields '', so isAdmin=true bypasses the ownership branch (line 38) with adminClient/RLS-bypass reads. Same unguarded copy in docs/quote/[dealId]/page.tsx:35. Correction: (protected)/layout.tsx:11 does NOT fail open (redirects on !user.email first) and callback route only chooses a redirect; also a safe helper verifyAdmin (server-helpers.ts:30) already exists and is unused here. Latent fail-open requiring env misconfig + email-less account -> P1, not P0.

### P1-3. selectCandidate/createMatching: TOCTOU check-then-insert can create duplicate matchings for one request
- `apps/admin/lib/admin/actions.ts:219` · concurrency & data integrity · quote-back OK · ☐ untaken
- 인용: `  if (req.status !== 'open') return { error: '이미 매칭 진행 중인 의뢰입니다.' }`
- **문제:** Both selectCandidate (lines 212-251) and createMatching (lines 352-389) read request.status, check it is 'open', then insert into matching and update request.status — three separate statements, no transaction, and the final request update (lines 245-249, 383-387) has NO .eq('status','open') compare-and-set guard. The matching table has no UNIQUE on request_id (verified: supabase/migrations/0001_init.sql lines 72-80, no later migration adds one), so two concurrent invocations (admin double-click, two admins, or a race with autoAssignOverdue whose overdue snapshot was taken before the manual select) both pass the 'open' check and both insert a matching row → one request proposed to two experts simultaneously. autoAssignOverdue itself (lines 286-334) DOES use pending/open guards, proving the invariant is known — these two manual paths just lack it. Additionally in selectCandidate the two matching_candidate updates (lines 222-232) ignore their errors entirely, and if the matching insert then fails (line 243) the candidates are left flipped to skipped/selected with no matching and the request still 'open' — inconsistent state with no rollback. Fix: partial unique index on matching(request_id) where status not in terminal states, plus .eq('status','open') CAS on the request update, treating 0 rows updated as a lost race.
- **검증:** Verified: selectCandidate (actions.ts:212-251) and createMatching (:352-389) both check req.status!=='open' (:219, :359) then insert matching (:235-241, :372-378) and update request (:245-248, :383-386) with no .eq('status','open') CAS and no transaction. matching has no UNIQUE on request_id (0001_init.sql:72-80 defines only idx_matching_request; the sole unique is deal.matching_id). autoAssignOverdue does use pending/open guards (:290, :334) proving the invariant is known. The race is concrete: dashboard/page.tsx:39 fires autoAssignOverdue() on every dashboard load, racing any manual select. Candidate updates at :222-232 also discard errors as claimed.

### P1-4. releaseSettlement: no compare-and-set on escrow_status and non-atomic multi-step release → double ledger accrual / double counter increment
- `apps/admin/lib/admin/actions.ts:505` · concurrency & data integrity · quote-back OK · ☐ untaken
- 인용: `      escrow_status: 'released',`
- **문제:** releaseSettlement reads escrow_status (line 470-479), then updates settlement to 'released' (lines 502-508) WITHOUT .eq('escrow_status','deposited')/.eq('escrow_status','reviewing') on the update — unlike confirmDepositManual directly above it (line 457) which does add the pending guard, so the omission is an inconsistency, not a pattern. Two concurrent invocations (double-click on the dashboard release button, or dashboard-load runAutoRelease racing a manual release) both pass the read check and both execute the whole tail: guarantee_fund_ledger 'accrue' row inserted TWICE (lines 517-526 — money ledger with no idempotency key), recalcExpertScores run twice (harmless), and increment_completed_deals RPC called twice (lines 540-546) inflating owner.completed_deals. The 5 writes (settlement, deal, ledger, scores, counter) are also not one transaction — a failure mid-way (all update/insert errors on lines 502-526 are ignored, not even destructured) leaves e.g. settlement released but deal not done, silently. Fix: CAS guard + .select() on the settlement update and bail out when 0 rows affected; move the ledger insert/counter behind that gate.
- **검증:** Verified: releaseSettlement updates settlement (:502-508) with only .eq('id') — no escrow_status CAS — while confirmDepositManual directly above adds .eq('escrow_status','pending') (:457). Ledger accrue insert (:517-526) has no idempotency key (guarantee_fund_ledger has no unique, 0001:114-123) and increment_completed_deals RPC (:541-542) runs unguarded; all 5 writes' errors ignored. Worse than filed: the shared batch autoReleaseSettlements (auto-settlement.ts:74-81) also lacks the reviewing CAS, and it runs concurrently from BOTH dashboard load (page.tsx:39 runAutoRelease) and the cron route (api/cron/auto-release/route.ts:23) — double ledger accrual is reachable with zero human involvement.

### P1-5. Newbie review gate and transition validation live ONLY on a dead API route — live path bypasses both
- `apps/expert/app/api/deals/[id]/workflow/[step]/route.ts:86` · security / dead-code / structure-duplication · quote-back OK · ☐ untaken
- 인용: `    if (expertInfo?.is_newbie) {`
- **문제:** Nothing in the repo calls this route: grep for 'api/deals' across all apps finds zero fetch callers; the UI (workflow-form.tsx) calls the server action updateWorkflowStep in apps/expert/lib/workflow/actions.ts instead. That server action has NO pending→in_progress→done transition validation, NO deal.status check, and critically NO is_newbie gate — it fetches deal.status in verifyDealExpertOwnership and never uses it, then updates deal_workflow unconditionally. Consequence today: a newbie expert (is_newbie=true) completes the 'verify' step through the real UI with no admin review, queue_status is never set to 'pending_review', and the '신규 시니어지식인 결과물은 관리자 검수' policy is silently never enforced. Also any caller can invoke the server action directly with arbitrary status (including reverting 'done'→'pending'), since only the client button sequence enforces transitions. This is the duplicated-shape-with-no-pinning-test defect from coding-style rules: two implementations of the same operation that have already diverged, with the safety checks stranded on the unreachable copy. Fix: delete the route and move its transition + newbie-gate logic into updateWorkflowStep (or make the form call the route), plus a test pinning the gate.
- **검증:** Quote present at route.ts:86. Zero callers of the route (grep 'api/deals' hits only docs and the route itself); UI calls updateWorkflowStep (workflow-form.tsx:4,38). apps/expert/lib/workflow/actions.ts:42-74 has no transition validation, no deal.status check (fetched at line 21, never used), no is_newbie gate — updates deal_workflow unconditionally with any caller-supplied StepStatus. No compensating enforcement elsewhere: audit-sampling.ts is random 5% sampling, and the only other is_newbie check is the owner refund route. The newbie review policy and transition rules live only in unreachable code; docs/08 shows the same dead-route pattern was already purged once for approve/confirm.

### P1-6. FIELD_CHIPS vocabulary diverges from register page's category-synced FIELD_GROUPS — stored fields become invisible and unremovable
- `apps/expert/components/profile-editor.tsx:7` · structure-state / correctness · quote-back OK · ☐ untaken
- 인용: `const FIELD_CHIPS = [`
- **문제:** Registration (apps/expert/app/(main)/register/page.tsx FIELD_GROUPS, commented '대분류별 중분류 그룹 (category 테이블과 동기)') offers 28 category-table-synced fields such as '업무자동화', '품질관리', 'R&D 기획', '콘텐츠제작'. This editor renders a different flat 13-chip list that omits most of them and adds '기타', which has no depth-1 category counterpart. Consequences: (1) an expert whose stored field contains e.g. '업무자동화' opens this editor, selectedFields is seeded from profile.field.split(',') (line 44) but no chip renders it — the value is invisible, cannot be deselected, and silently consumes the 5-field cap; (2) selecting '기타' passes client-side but the server's expert_category label match drops it silently, and if it is the only field the old category mappings are never cleared. Same shape in two files, no pinning test, and neither matches the category table. Duplicated CAREER_OPTIONS (line 23 here vs register page line 18) is the same pattern in miniature.
- **검증:** Verified both files: register/page.tsx:7-16 has FIELD_GROUPS with 28 fields and the comment '대분류별 중분류 그룹 (category 테이블과 동기)'; profile-editor.tsx:7-21 has a flat 13-chip list omitting 업무자동화/품질관리/R&D 기획/콘텐츠제작 etc. and adding '기타' (grep confirms '기타' is not in 0011_category.sql). selectedFields seeded from profile.field.split(',') at profile-editor.tsx:43-45 with no chip to render/deselect missing values, and they consume the 5-cap. Server action lib/expert/actions.ts:68-87 confirms the second claim: labels matched against depth-1 categories, and the `if (cats && cats.length > 0)` guard at :76 means an unmatched-only field ('기타') skips both the delete and re-insert, leaving stale mappings. Real correctness defect for stored data.

### P1-7. acceptMatching: status update has no compare-and-set — double-accept race corrupts winner's state
- `apps/expert/lib/matching/actions.ts:78` · concurrency · quote-back OK · ☐ untaken
- 인용: `.update({ status: 'accepted' })`
- **문제:** The action reads matching.status, checks it is 'proposed', then updates to 'accepted' with only .eq('id', matchingId) — no .eq('status', 'proposed') guard. Two concurrent accepts (double-click, two tabs) both pass the check. The loser's deal insert then fails on the UNIQUE(matching_id) constraint (database.types.ts: deal_matching_id_fkey isOneToOne), and its rollback path sets matching.status back to 'proposed' — while the winner's deal, settlement, and workflow rows exist. Result: a live deal whose matching shows 'proposed', re-acceptable UI, and every retry re-corrupts. rejectMatching (line 176) has the same missing guard, so an accept/reject race can mark a dealt matching 'rejected' and flip the request back to 'open' with an active deal. Fix: compare-and-set (.eq('status','proposed')) + verify affected rows, and never blind-reset status in rollback.
- **검증:** Verified: matching/actions.ts:76-79 updates status='accepted' with only .eq('id') after the :50 status check — no .eq('status','proposed') CAS. deal.matching_id is UNIQUE (0001_init.sql:85, database.types.ts:139 isOneToOne:true), so a concurrent double-accept makes the loser fail at deal insert and its rollback (:103) blind-resets matching to 'proposed' while the winner's deal/settlement/workflow rows persist — matching stuck re-acceptable with a live deal, and every retry re-runs the corruption. rejectMatching (:176-179) also lacks the guard; one sub-claim overstated: the request→'open' reset (:183-187) IS guarded by .eq('status','matching'), so 'open with active deal' is unlikely, but 'rejected' matching with a live deal is real. Core defect stands.

### P1-8. acceptInvitation: check-then-update without CAS — losing racer's rollback() wipes the winner's invitation state
- `apps/expert/lib/invitation/actions.ts:122` · concurrency · quote-back OK · ☐ untaken
- 인용: `status: 'accepted',`
- **문제:** Same pattern as acceptMatching: status read ('invited' check at line 68), then update to 'accepted' with no .eq('status','invited') guard. Under a concurrent double-accept, the loser fails at deal insert (UNIQUE deal.invitation_id, isOneToOne) and runs rollback(), which resets the invitation to status 'invited' and nulls est_hours/est_amount/cap_amount (line 113) — destroying the winner's committed state while the winner's deal+settlement+workflow rows remain. The invitation then appears acceptable again but every accept fails at the unique constraint. declineInvitation (line 217) also updates without a status guard. The whole 6-step write is non-transactional with best-effort compensation (acknowledged by comments), but the missing CAS is what turns a benign race into corruption.
- **검증:** Verified: invitation/actions.ts:68 status check, :119-128 update to 'accepted' with no .eq('status','invited') CAS. deal.invitation_id UNIQUE confirmed (database.types.ts:159-160 isOneToOne:true). Loser of a double-accept fails at deal insert (:150) and rollback() (:109-116) resets invitation to 'invited' and nulls est_hours/est_amount/cap_amount — wiping the winner's committed state while the winner's deal remains. declineInvitation (:217-220) also unguarded, so a decline racing an accept overwrites 'accepted' with 'declined' over a live deal. Quote present at :122.

### P1-9. Refund read-modify-write on refunded_amt with no compare-and-set — lost update on money
- `apps/owner/app/api/settlements/[id]/refund/route.ts:68` · concurrency-data-integrity · quote-back OK · ☐ untaken
- 인용: `  const newRefundedAmt = (settlement.refunded_amt || 0) + amount`
- **문제:** The route SELECTs settlement.refunded_amt, computes newRefundedAmt in app code, calls Toss cancel, then UPDATEs refunded_amt unconditionally (.eq('id', settlementId) only, no .eq('refunded_amt', old) CAS and no version column). Two concurrent refund requests with different amounts both read the stale refunded_amt (e.g. 0), both pass the over-refund guard at line 69 against the stale value, both get distinct idempotency keys (refund_{id}_{newAmt}), so BOTH cancels execute at Toss — while the DB ends with last-write-wins refunded_amt reflecting only one of them. The DB then understates the total refunded at Toss, so subsequent refunds can push the true cumulative Toss refund past totalPay. The idempotency-key comment (line 77) covers retry-of-identical-request, not this interleaving. Fix: guard the UPDATE with .eq('refunded_amt', settlement.refunded_amt || 0) and fail/retry on 0 rows, or move the increment+cap into a DB function/constraint.
- **검증:** Verified in refund/route.ts: SELECT at 46-50, guard at 68-69 computed from the read value, UPDATE at 90-98 filtered only by .eq('id', settlementId) — no compare-and-set, no version column. Two concurrent refunds with different amounts read stale refunded_amt, both pass the cap, get distinct idempotency keys (refund_{id}_{newAmt} at line 79), so both cancels execute at Toss while the DB keeps only the last write. DB then understates cumulative Toss refunds, enabling later refunds past totalPay. Admin-only endpoint (x-admin-secret) reduces likelihood but this is a genuine lost-update on money — the exact class quality-gates mandate optimistic locking for. P1 stands.

### P1-10. submitReview: SELECT-then-INSERT duplicate check with no UNIQUE constraint — duplicate reviews under concurrency
- `apps/owner/lib/deal/actions.ts:63` · concurrency-data-integrity · quote-back OK · ☐ untaken
- 인용: `  const { data: existing } = await adminClient`
- **문제:** The duplicate-review guard is a SELECT on review(deal_id, author_type) followed by an INSERT (lines 63-81). There is no UNIQUE constraint backing it: migration 0016_composite_indexes.sql line 10 creates `CREATE INDEX IF NOT EXISTS idx_review_deal_author ON review(deal_id, author_type)` — a plain index, even though its own comment says '중복 리뷰 확인 유니크 체크' (unique check for duplicate reviews). Two concurrent submissions (double-tap of the submit button, retried request) both see `existing` = null and both insert, producing duplicate reviews for one deal. Each duplicate then feeds recalcExpertScores (line 93), inflating the expert's rating-derived score. This is exactly the SELECT-then-INSERT race the DB constraint should close: make idx_review_deal_author UNIQUE and treat the 23505 conflict as '이미 리뷰를 작성하셨습니다.'
- **검증:** Quote matches actions.ts:63. SELECT-then-INSERT guard at lines 63-81 with no backing constraint: 0001_init.sql:126-135 defines review with no UNIQUE on (deal_id, author_type), and 0016_composite_indexes.sql:10 creates a plain CREATE INDEX even though its own comment (line 9) says '중복 리뷰 확인 유니크 체크'. No other migration adds uniqueness. An owner firing concurrent submitReview requests reliably inserts duplicate reviews, each feeding recalcExpertScores (line 93) — deliberately exploitable to skew expert scores, not just an accidental race. Bonus breakage: once duplicates exist, .single() at line 68 errors on multi-row. P1 stands.

### P1-11. Logged-out invite CTA links to /api/auth/login, a route that does not exist — 404 now
- `apps/owner/components/invite-button.tsx:18` · correctness · quote-back OK · ☐ untaken
- 인용: `        href="/api/auth/login"`
- **문제:** The logged-out variant of the expert-detail primary action is `<a href="/api/auth/login">`. A repo-wide grep finds no app/api/auth/** handler in the owner app (or anywhere), no middleware rewrite, and proxy.ts only refreshes the Supabase session. Every logged-out visitor who clicks '로그인 후 초빙하기' lands on a 404 — the invite conversion path is broken in production. The rest of the app initiates login via the signInWithKakao/signInWithGoogle server actions (AppHeader, home CTA forms); this button should use the same mechanism. Already recorded as untaken item P1-9 in docs/10_UX_AUDIT_2026-08-10.md — flagged, not fixed; still live in the code.
- **검증:** Quote matches invite-button.tsx:18. Glob apps/**/api/auth/** finds no route; repo-wide grep for 'api/auth/login' hits only this component and the audit doc; apps/owner/proxy.ts (read in full) only refreshes the Supabase session with no rewrite, and no rewrites config exists in apps/owner. Login elsewhere uses signInWithKakao/Google server actions (layout.tsx, page.tsx). Logged-out '로그인 후 초빙하기' 404s today. Documented as untaken item P1-9 in docs/10_UX_AUDIT_2026-08-10.md:53 — flagged, not fixed. P1 (live break of one conversion path; header login still works).

### P1-12. GET /api/settlements/[id] leaks work_fee/match_fee split and expert_id to the owner, breaking the documented anonymization design
- `apps/owner/app/api/settlements/[id]/route.ts:24` · security · quote-back OK · ☐ untaken
- 인용: `    .select('*, deal:deal!inner(id, request_id, expert_id, work_fee, match_fee, total_pay, status)')`
- **문제:** The endpoint returns the settlement with an embedded deal containing work_fee, match_fee, total_pay and expert_id, and serves that identical payload to BOTH parties (owner path returns the same `{ settlement }` at line 62 as the expert path at line 44). quote-section.tsx documents a product invariant that the owner must only ever see total_pay — exposing the internal fee split '직거래 방지 (direct-deal prevention) 설계를 깨뜨린다' — and the status page carefully passes only total_pay. The same defect was flagged for /api/requests/[id] (UX audit P1-8, untaken); this route repeats it: any authenticated owner can curl their settlement and read exactly what the expert is paid vs the platform's cut, plus the expert_id. Owner-path response should strip work_fee/match_fee/expert_id.
- **검증:** Quote matches route.ts:24. The route returns the identical { settlement } payload (embedding deal.work_fee, match_fee, expert_id) to the expert at line 44 and to the owner at line 62 — no stripping. The invariant is documented at quote-section.tsx:9-14 ('내부 수수료 분해(work_fee·match_fee)…노출되어 익명화(직거래 방지) 설계가 무너진다') and line 67, and the same defect class is recorded as untaken P1-8 in docs/10_UX_AUDIT_2026-08-10.md:49. Caveat weakening exclusivity but not the finding: mypage/page.tsx:249 already shows work_fee to the owner — another instance of the same leak, not a sanctioned design. P1 per the project's own audit grading.

### P1-13. Webhook retry cannot repair a failed deal-status write — idempotency guard short-circuits first
- `apps/owner/lib/payments/confirm-deposit.ts:42` · concurrency-data-integrity · quote-back OK · ☐ untaken
- 인용: `  if (settlement.escrow_status !== 'pending') {`
- **문제:** The documented recovery contract (header comment: DB failure after capture returns ok:false so the caller responds non-2xx and Toss re-sends the webhook) is broken for the second write. Sequence: settlement update (line 58) succeeds → deal update (line 78) fails → ok:false 500 → Toss retries → the retry now sees escrow_status='deposited' and the guard at line 42 returns ok:true alreadyProcessed BEFORE ever reaching the deal update. The deal stays 'quoted' forever: the owner paid, escrow shows deposited, but the deal never enters 'working' and no retry can fix it. The alreadyProcessed branch must still perform the deal.status catch-up (it is already idempotent via the 'quoted' check), or both writes must be one transaction/RPC.
- **검증:** confirm-deposit.ts:42 guard returns ok:true/alreadyProcessed before the deal update at lines 77-91. Webhook route (webhook/route.ts:66-68) returns non-2xx on ok:false to induce Toss retry, but the retry short-circuits at the guard once escrow_status='deposited', so a failed deal-status write is never repaired. No mitigation exists: admin confirmDepositManual (admin/actions.ts:446-448) rejects non-pending settlements and never touches deal.status. Money captured, deal stuck at 'quoted' permanently — the header comment's recovery contract (line 14) is broken for the second write. Requires a rare DB failure between two writes, but the outcome is permanent payment-state inconsistency with no automatic or admin repair path, so P1 stands.

### P1-14. Code targets 'owner'/'invitation' schema that no checked-in migration defines — migrations 0019-0023 missing
- `apps/owner/lib/invitation/actions.ts:71` · honesty · quote-back OK · ☐ untaken
- 인용: `    .from('invitation')`
- **문제:** This action (and message/profile/request/services actions) reads/writes tables 'owner' and 'invitation' and inserts sender_type/author_type 'owner', but the checked-in migrations define only 'client'/'partner' (0001_init.sql line 26: create table client; deal_message CHECK allows 'partner','admin','client' in 0007) and create no 'invitation' table at all. The migration sequence jumps 0018 → 0024, and git history (git log --all on 'supabase/migrations/0019*..0023*') shows the gap files never existed. The live DB was evidently renamed/extended outside version control: a fresh environment built from these migrations cannot run this code, RLS policies still reference the old tables, and whether 'invitation' has any unique constraint is unverifiable from the repo. Migrations are the schema contract (quality-gates §5) and are currently not the source of truth.
- **검증:** Migration directory jumps 0018_seed_matching.sql to 0024_provider_member.sql; git log --all on 0019*-0023* returns empty (files never existed in history, not deleted). No migration creates 'owner' or 'invitation' tables (grep across supabase/migrations: zero matches); 0001_init.sql:26 creates 'client', and 0007_owner_engagement.sql:6 CHECK allows only 'partner','admin','client' for deal_message.sender_type, yet message/actions.ts inserts sender_type:'owner' and invitation/actions.ts:71 queries .from('invitation'). A fresh environment built from checked-in migrations cannot run this code; the schema contract is not in version control. Quote verified at line 71.

### P1-15. Dispute check fails OPEN: query error releases disputed settlements
- `packages/shared/lib/automation/auto-settlement.ts:58` · correctness / resilience / money · quote-back OK · ☐ untaken
- 인용: `  const disputedIds = new Set((disputes || []).map((d: any) => d.target_id))`
- **문제:** The open-dispute query result destructures only { data: disputes } and never checks the error. On any transient DB/network failure, disputes is null, `(disputes || [])` yields an empty Set, every reviewing settlement passes the dispute filter, and step 4 unconditionally releases them — money is released to experts on deals that have an open dispute. The guard whose entire purpose is to block release silently degrades to no-op on error. Fix: check the query error and abort the run (fail closed) when the dispute check cannot be performed.
- **검증:** auto-settlement.ts:51-58 destructures only { data: disputes }; on query error disputes is null, disputedIds becomes an empty Set, and step 4 (line 74-81) releases every reviewing settlement including disputed ones. Fail-open on a money guard is real. Re-graded P0→P1: it requires a transient DB failure at that exact query coinciding with an open dispute on a 3-day-old reviewing settlement — a real defect, not broken/exploitable right now.

### P1-16. Batch release UPDATE has no compare-and-set on escrow_status
- `packages/shared/lib/automation/auto-settlement.ts:81` · concurrency & data integrity · quote-back OK · ☐ untaken
- 인용: `    .in('id', eligibleIds)`
- **문제:** Between the SELECT (escrow_status='reviewing') and the batch UPDATE, a settlement can be refunded (payments/refund flow sets escrow_status='refunded') or otherwise transitioned. The update matches only `.in('id', eligibleIds)` with no `.eq('escrow_status', 'reviewing')`, so it overwrites that concurrent transition back to 'released' (lost update on money state). The same missing predicate means two overlapping cron invocations can both select the same rows and both proceed to steps 5-8 (double guarantee_fund_ledger accrual, double completed_deals increment) — there is no UNIQUE idempotency key on guarantee_fund_ledger(settlement_id, entry_type) to stop the second insert (verified in supabase/migrations/0001_init.sql:115-123).
- **검증:** auto-settlement.ts:81 updates .in('id', eligibleIds) with no .eq('escrow_status','reviewing'). The refund route (apps/owner/app/api/settlements/[id]/refund/route.ts:60) explicitly allows refunding a 'reviewing' settlement and sets 'refunded' at line 93 — so a concurrent refund (Toss cancel already executed) can be overwritten back to 'released'. 0001_init.sql:115-123 confirms guarantee_fund_ledger has no UNIQUE on (settlement_id, entry_type), so overlapping cron runs double-insert accruals. P1 stands.

### P1-17. Multi-step money write is not one transaction; ledger accrual failure swallowed
- `packages/shared/lib/automation/auto-settlement.ts:100` · concurrency & data integrity / honesty · quote-back OK · ☐ untaken
- 인용: `      console.error('[auto-settlement] ledger insert failed:', ledgerError.message)`
- **문제:** Steps 4-8 (release settlements, insert guarantee_fund_ledger accruals, audit sampling, score recalc, completed_deals increments) run as separate statements with no transaction. If the ledger insert fails, settlements are already 'released', the failure is only console.error'd, the function still reports released=N success, and there is no retry or reconciliation — the guarantee fund ledger permanently drifts from released settlements with guarantee_fee > 0. Because release already happened, the next cron run will not re-attempt the accrual (rows are no longer 'reviewing').
- **검증:** auto-settlement.ts:97-102: ledger insert failure is console.error'd and execution continues; line 145-147 still reports released=N success. Since the release at line 74-81 already moved rows out of 'reviewing', the next cron run cannot re-attempt the accrual — permanent ledger drift with no reconciliation path. Multi-step money write is confirmed non-transactional (separate PostgREST calls).

### P1-18. RPC increment_completed_deals (and other automation schema objects) defined nowhere in the repo
- `packages/shared/lib/automation/auto-settlement.ts:139` · correctness / honesty / portability · quote-back OK · ☐ untaken
- 인용: `      .rpc('increment_completed_deals', { p_owner_id: ownerId, p_increment: increment })`
- **문제:** `increment_completed_deals` is called here and in apps/admin/lib/admin/actions.ts:542, but no migration defines it — the only SQL function in supabase/migrations is set_updated_at (0001_init.sql:7). Likewise platform_config (audit-sampling.ts), expert_activity table, and the queue_status/audit_sampled/auto_processed/completed_deals/activity_points/is_newbie columns appear in NO migration; the migrations directory jumps 0018 → 0024 and git history (all branches) never contained 0019-0023. If these objects were created only via dashboard, the repo's schema source of truth cannot rebuild the DB; if they are absent from any environment, every one of these calls fails at runtime and every failure path is console.warn/error + continue, so the crons still report success (silent no-op of audit sampling, activity expiry, completed_deals counting). Cannot verify the live DB from here — either way the missing migrations are a real defect.
- **검증:** Grep of supabase/ finds zero occurrences of increment_completed_deals, platform_config, expert_activity, queue_status, audit_sampled, auto_processed, completed_deals, activity_points, or is_newbie. Migrations jump 0018→0024 (Glob confirmed) and git log --all --diff-filter=A shows 0019-0023 were never committed on any branch. The RPC call exists at auto-settlement.ts:139 and apps/admin/lib/admin/actions.ts:541-542 as claimed; the refund route also selects expert.is_newbie. The repo's migration set cannot rebuild the schema the code depends on, and every failure path is warn/error+continue.

### P1-19. provider_self_update allows updating ALL columns including status — self-approval path
- `supabase/migrations/0024_provider_member.sql:46` · security · quote-back OK · ☐ untaken
- 인용: `CREATE POLICY provider_self_update ON provider`
- **문제:** The UPDATE policy has USING (auth_user_id = auth.uid()) and no explicit WITH CHECK, so the USING expression is reused as WITH CHECK — meaning a provider may update any column of their own row, including `status` (the admin-approval gate: pending → active) and `kind`. If RLS is enforced on the provider table, any pending provider with the anon key can call PostgREST directly (`update provider set status='active' where auth_user_id=uid`) and bypass the admin approval process entirely ("관리자 승인제" per the file's own header). The comment on line 43 labels these policies 명목 (nominal, real code uses adminClient) — but a nominal policy that OPENS a write path is worse than none: RLS-off makes it inert, RLS-on makes it an escalation. Either restrict the policy to non-privileged columns (or drop it and rely on service-role writes only), or add a WITH CHECK that pins status/kind to their old values via a trigger.
- **검증:** 0024_provider_member.sql:46-47: FOR UPDATE USING (auth_user_id = auth.uid()) with no WITH CHECK, and Postgres RLS cannot restrict columns — if RLS is on, a pending provider with an auth account (the migration's whole purpose, line 14) can set status='active' via PostgREST under Supabase's default authenticated grants, bypassing the admin-approval gate stated in the file header. RLS-enable on provider is unverifiable (table created in missing 0019-0023), but every other table in the repo enables RLS (grep: 0001, 0002, 0005, 0006, 0009, 0011, 0012, 0025), and the RLS-off branch is worse (whole table open via anon key). Either branch is a real, reachable security defect. P1 stands.

### P1-20. FK references `provider` table that no committed migration creates — chain unreproducible
- `supabase/migrations/0025_service_package.sql:12` · correctness · quote-back OK · ☐ untaken
- 인용: `provider_id      uuid NOT NULL REFERENCES provider(id) ON DELETE CASCADE,`
- **문제:** No migration in the repo creates a `provider` table (0001_init.sql creates `client` and `partner`; grep across all migrations finds no CREATE TABLE provider and no RENAME). Migrations 0019-0023 are absent and `git log --all --diff-filter=AD` shows they never existed in git history, yet 0024 comments reference '0019의 테이블 루프' and ALTERs `provider`. A fresh `supabase db reset` / CI / staging rebuild / disaster-recovery restore from this repo fails at 0024/0025 with 'relation provider does not exist'. The live DB presumably got these objects out-of-band, meaning the committed migration chain and production schema have silently diverged — exactly the backup/recovery gap quality-gates.md 5 warns about. Root cause is the missing 0019-0023 files; 0025 (and 0026) are the newest migrations extending the broken chain.
- **검증:** Quote matches 0025:12 exactly. No 'CREATE TABLE provider' exists in any committed file (case-insensitive grep: 0 hits; the 'provider' matches in 0001/0013/0015 are the auth_provider enum column). Migration files jump 0018 -> 0024, and git log --all for 0019*-0023* returns nothing — they were never in git history. docs/08_AUDIT_REMEDIATION_2026-07-23.md:9 independently confirms 0019_v2_full_reset.sql/0020_v2_seed.sql existed but were never committed. 0024 ALTERs provider, so a fresh rebuild from the repo fails there; the committed chain and live schema have diverged. P1: real reproducibility/disaster-recovery defect, though production currently works.

### P1-21. Index on `service_order.provider_id` — column never added by any committed migration
- `supabase/migrations/0026_service_order_package_fk.sql:19` · correctness · quote-back OK · ☐ untaken
- 인용: `  ON service_order(provider_id, created_at DESC);`
- **문제:** `service_order` as created in 0005_service_orders.sql has `client_id` and `partner_id` but no `provider_id`, and no committed migration adds one (grep across all migrations). The column must have been added by the missing 0019-0023 migrations, so on a rebuilt-from-repo database this CREATE INDEX fails with 'column provider_id does not exist'. Same root cause as the 0025 finding (missing migrations 0019-0023); also the header comment's snapshot list '스냅샷(package_slug/name/price/is_free)' names an `is_free` column that 0005 does not define either. The repo cannot reproduce the schema this migration assumes.
- **검증:** Quote matches 0026:19. 0005_service_orders.sql creates service_order with only client_id/partner_id (lines 9-10) — no provider_id, and no committed migration adds it (grep for provider_id across migrations hits only 0025/0026). The is_free claim also checks out: is_free appears only in 0025 (service_package) and the 0026 comment, never on service_order. On a repo-rebuilt DB this CREATE INDEX fails. Same root cause as finding 0 (missing 0019-0023) — arguably a duplicate of it, but factually correct as stated. P1.

### P1-22. RLS FOR ALL policy lets a partner self-publish via direct PostgREST, bypassing the admin gate
- `supabase/migrations/0025_service_package.sql:43` · security · quote-back OK · ☐ untaken
- 인용: `CREATE POLICY service_package_owner_all ON service_package`
- **문제:** The file header states the design: partners register drafts and the admin publishes ('파트너가 직접 서비스를 등록(draft)하고 관리자가 publish'). But `service_package_owner_all` is FOR ALL with no column/status restriction, so any authenticated user whose auth.uid matches a provider row can INSERT/UPDATE/DELETE their own rows directly through Supabase's REST API (a browser client with the publishable key exists in packages/shared/lib/supabase/client.ts, and partners get real auth sessions via the partner OAuth callback) — including setting status='published', featured=true, and arbitrary sort_order, and doing so even while provider.status is 'pending'/'rejected'/'suspended' (the policy never checks provider status). The '명목 정책 — 실코드는 adminClient' comment documents that app code bypasses RLS; it does not document accepting that the RLS boundary itself grants self-publish to direct API callers. Since all legitimate writes go through adminClient anyway, the nominal policy should be SELECT-only (deny-by-default for writes) or restricted to status='draft' rows.
- **검증:** Quote matches 0025:43. Policy is FOR ALL with only tenant-ownership USING (no WITH CHECK, so it defaults to USING; no status check on the row or on provider.status). The exploit chain is fully wired: any logged-in user self-creates a pending provider row with auth_user_id = uid (apps/admin/lib/partner/actions.ts:50-61), partners get real Supabase sessions via exchangeCodeForSession (apps/admin/app/partner/callback/route.ts:21), a publishable-key browser client exists (packages/shared/lib/supabase/client.ts), and no migration REVOKEs default PostgREST grants. So a pending/rejected partner can INSERT/UPDATE/DELETE own rows with status='published' and featured=true via direct REST, bypassing the admin-publish gate the file header declares. The '명목 정책' comment covers app code using adminClient, not this direct-API grant. P1 broken access control; not P0 since impact is unauthorized listing publication, not payment/data compromise.

## 2. P2 — 70건

#### P2-1. Candidate fetch has no response-to-request correlation: stale response renders under the wrong request
- `apps/admin/app/(protected)/dashboard/matching-tab.tsx:82` · correctness/race
- handleShowCandidates sets expandedId synchronously, then awaits the fetch and unconditionally writes candidates/hasAiCandidates/autoAssignAt into single shared state slots. There is no check that the response still belongs to the currently expanded request (no `if (expandedId !== requestId) return` and no keying of candidates by request id). Sequence: click 후보 보기 on request A (fetch A in flight) → click 후보 보기 on request B (expandedId=B, fetch B in flight) → A's response resolves last → A's candidates, A's autoAssignAt countdown, and A's hasAiCandidates flag render inside B's expanded panel, and the admin can click 배정 assigning one of A's candidates while looking at B's card (handleAssign does pass req.id, so the wrong-looking list is at minimum severely misleading). One fact (candidates-for-expanded-request) is stored without recording which request it answers for.

#### P2-2. resolveDispute result discarded — failures are silently swallowed and look like success
- `apps/admin/app/(protected)/dashboard/dispute-tab.tsx:58` · honesty
- resolveDispute returns { error?: string } (it can fail with '이의제기를 찾을 수 없습니다', '이미 해결된 이의제기입니다', or a DB error), but the return value is ignored. On failure the button simply stops spinning and the card stays 'open' with zero feedback — the admin cannot distinguish success-pending-refresh from failure. Additionally there is no try/catch: if the server action throws (network drop, verifyAdmin rejection), setResolving(false) is skipped and the button is stuck on '처리 중...' forever. Every other tab in this dashboard (inquiry, partner, matching) at least captures result.error; this one drops it entirely.

#### P2-3. expert_interest fetched as full table (no filter, no limit) just to count per request
- `apps/admin/app/(protected)/dashboard/page.tsx:118` · performance
- The query selects every expert_interest row ever created — including note, created_at, and a joined expert record (id, name, field) that are never used — solely to build interestCounts, a per-request tally. Nothing bounds it: no .in('request_id', openRequestIds), no .limit(). All sibling list queries here cap at 50, but this one grows without bound as the platform accumulates interests, and the joined expert payload multiplies the transfer size. Filter to the open requests' ids (already fetched in the same Promise.all) or use a grouped count; drop the unused columns/join.

#### P2-4. Guarantee fund balance computed by fetching every ledger row and summing in JS
- `apps/admin/app/(protected)/dashboard/page.tsx:56` · performance
- Both accrue and payout queries pull all amount rows from guarantee_fund_ledger with no bound, then reduce in JS (lines 68-69) on every dashboard load — an append-only ledger means this fetch grows monotonically forever, on the admin's hottest page. This is exactly the unbounded-growth shape: works at n=100, pages the request at n=100k. Use a SQL aggregate (RPC with SUM grouped by entry_type, or a maintained balance) instead of shipping the whole ledger to the server component.

#### P2-5. Message thread fetch has no stale-response guard; one shared messages state for all deals
- `apps/admin/app/(protected)/dashboard/progress-tab.tsx:69` · correctness/concurrency
- handleToggleMessages sets expandedMsgId immediately, then awaits getMessagesForDeal and unconditionally writes the shared `messages` state. If the admin opens deal A (slow fetch) then deal B (fast fetch), A's response resolves last and overwrites `messages`, so deal A's thread renders inside deal B's expanded panel, and the shared `msgLoading` flag is cleared by whichever call finishes last. There is no request-id/abort check comparing the response against the currently-expanded deal. The reply input still posts to the correct dealId, so the admin can reply to deal B while reading deal A's messages.

#### P2-6. Props copied into useState — server refresh never reaches the rendered list (two sources of truth)
- `apps/admin/app/(protected)/dashboard/service-tab.tsx:56` · structure/state
- `orders` (server-fetched props) is copied into local `items` state once; useState ignores the initializer on subsequent renders. Every other admin action in this dashboard calls revalidatePath('/dashboard') (verified in lib/admin/actions.ts), which re-renders the server component with fresh orders — but ServiceTab keeps rendering the stale `items` snapshot from first mount. Conversely, a status change made here updates `items` only, so props and state permanently diverge until a hard reload. One fact (order list/status) now has two owners. Either key the component by data, sync via useEffect/derived state, or drop local state and use router.refresh after PATCH.

#### P2-7. Service-role data reads gated only by login check; admin allowlist lives in layout only
- `apps/admin/app/(protected)/review-input/[id]/page.tsx:21` · security
- The page checks only `if (!user)` and then reads deal financials, reviews, and AI suggestions via `adminClient` (service role, bypasses RLS). The ADMIN_EMAILS allowlist check exists only in (protected)/layout.tsx. Next.js layouts do not re-run on soft navigation within the segment and official guidance is to keep authz next to the data access — every server action in this repo calls verifyAdmin() per-invocation, making this page the sole read path relying on the layout as its security boundary. A revoked admin with a live session, or any future refactor of the layout, silently exposes deal financial data to any logged-in user. Add a verifyAdmin() (or equivalent email check) in the page itself.

#### P2-8. Matching creation is non-atomic and the second write's failure is silently swallowed
- `apps/admin/app/api/admin/matching/route.ts:48` · concurrency-data-integrity / honesty
- The flow is: insert into matching, then update request.status to 'matching', then return {success:true}. The two writes are not one transaction, and the result of the request UPDATE is never checked — if it fails (RLS, network, constraint), the endpoint still returns success while the request stays 'open' with a live 'proposed' matching row attached. A subsequent POST for the same request then passes the status==='open' guard and creates a second matching. Either wrap both writes in an RPC/transaction or at minimum check the update error and report failure.

#### P2-9. SELECT-then-INSERT race: status guard has no DB-level backing (no UNIQUE, no compare-and-set)
- `apps/admin/app/api/admin/matching/route.ts:28` · concurrency-data-integrity
- Two concurrent POSTs for the same request_id both read status='open', both pass the guard, and both insert a matching — migrations define no UNIQUE on matching(request_id) (0001_init.sql only constrains deal.matching_id), and the request update is a plain SET, not a compare-and-set on status='open'. Result: two 'proposed' matchings for one request, each of which can later become its own deal. A partial unique index on matching(request_id) WHERE status IN ('proposed','accepted'), or `.update(...).eq('status','open')` with a row-count check, would close it. Admin-only endpoint, so the window is narrow — quality debt rather than exploit.

#### P2-10. Duplicate-review guard is SELECT-then-INSERT with no UNIQUE constraint backing it
- `apps/admin/app/api/reviews/route.ts:22` · concurrency & data integrity
- The duplicate check reads review(deal_id, author_type='admin') and then inserts. Migration 0016 creates only a plain INDEX on review(deal_id, author_type) — its own comment says '중복 리뷰 확인 유니크 체크' but the index is NOT unique — so two concurrent POSTs both see no existing row and both insert (the exact SELECT-then-INSERT race). Worse, the guard self-destructs once duplicates exist: .single() errors when >1 row matches, the error is discarded, `existing` is null, and the guard passes — every subsequent call inserts yet another admin review. Duplicate admin reviews feed review aggregation/expert scoring. Fix: UNIQUE constraint on (deal_id, author_type) and treat the insert's unique-violation as the 409.

#### P2-11. Activity insert + points recalc is two steps, not atomic; failure leaves stored derived value stale and retry duplicates the activity
- `apps/admin/app/api/expert-activity/route.ts:51` · concurrency & data integrity / structure-state
- expert.activity_points is a stored derivation of expert_activity rows. This route inserts the activity, then recalculates in a second call. If recalcActivityPoints fails/throws after a successful insert, the client gets a 500 while the activity row persists — activity_points is now stale, and the natural admin reaction (retry) inserts a second activity row because there is no idempotency key (no UNIQUE on e.g. (expert_id, type, day)). Additionally recalcActivityPoints (packages/shared/lib/expert-activity.ts:32) discards the select error — on a transient DB read failure `activities` is null, the sum computes to 0, and it overwrites the expert's real activity_points with 0. Either wrap insert+recalc in one RPC/transaction or make recalc fail loudly instead of writing a value derived from a failed read.

#### P2-12. Admin-email predicate duplicated inline in 5 files with no shared helper or pinning test
- `apps/admin/app/page.tsx:13` · structure/state
- The exact shape `(process.env.ADMIN_EMAILS || '').split(',').map(e => e.trim().toLowerCase()).includes(...)` is copy-pasted in apps/admin/app/page.tsx:13, docs/statement/[dealId]/page.tsx:34, docs/quote/[dealId]/page.tsx:35, (auth)/callback/route.ts:26, and (protected)/layout.tsx:15. Five repetitions of a security predicate with no `isAdminEmail(user)` helper means the P1 empty-segment fix (and any future change, e.g. moving to a DB role) must land in five places — one will be missed. This is exactly the 'same shape duplicated with no test pinning them together' defect class; abstraction threshold (3rd repetition) is long past. On this page the impact is only revealing the 관리자 nav link, but the drift risk applies to the real gates.

#### P2-13. Transaction-statement dates rendered in server timezone, not KST
- `apps/admin/app/docs/statement/[dealId]/page.tsx:96` · correctness
- issueDate (line 96), dealDate (line 97), 입금일 (line 152), and 정산일 (line 158) all use toLocaleDateString('ko-KR') with no timeZone option, so they format in the server's timezone. On a UTC-hosted deployment (Vercel/Cloud Run default), any event occurring between 00:00 and 09:00 KST prints as the previous day, and the 발행일 itself is wrong for the first 9 hours of every KST day. This is a printable 거래명세서 (transaction statement) where the deposit/settlement date has financial meaning — dates must be converted at the display boundary with an explicit { timeZone: 'Asia/Seoul' }. The same pattern likely exists in the sibling quote document page.

#### P2-14. ORDER_STATUS_LABELS duplicated verbatim from shared labels with no pinning test
- `apps/admin/app/partner/dashboard/order-labels.ts:1` · structure/state
- ORDER_STATUS_LABELS_FALLBACK is a key-for-key, value-for-value copy of ORDER_STATUS_LABELS in packages/shared/lib/labels.ts:23 (pending/paid/processing/completed/cancelled with identical Korean labels). The shared map is already imported by client components in other apps (apps/expert/app/expert-dashboard.tsx, apps/owner status/mypage pages), so there is no bundling reason for a local copy, no comment documents an intentional tradeoff, and despite the name it is not a fallback — it is the primary label source for both partner dashboard views. Two files now answer 'what is the label for status X' and nothing pins them together; when a status is added or a label reworded in labels.ts, the partner dashboard silently drifts (shows the raw enum via the `|| o.status` branch or an outdated label).

#### P2-15. Post-publication edits bypass the admin review gate
- `apps/admin/app/partner/dashboard/services/[id]/page.tsx:37` · correctness
- The new-service page promises '등록하면 비공개(검수 대기) 상태로 저장되고, 관리자 검수 후 게시판에 공개됩니다', and this edit page states only category/target are immutable. But the edit page loads packages of ANY status (including 'published'), and updateServicePackage (apps/admin/lib/partner/actions.ts:217-229) updates name/description/price/deliverables/value_desc while leaving status untouched — migration 0025 has no trigger resetting status either (verified). A partner can get an innocuous draft approved, then rewrite the name, description, and price of the now-public listing with zero re-review; the publicly-visible RLS policy (0025 line 42: USING (status = 'published')) keeps serving the edited content. If skipping re-review on edits is a product decision, it is undocumented at either code site.

#### P2-16. generateAiCandidates: SELECT-then-INSERT existence check with no UNIQUE constraint backing it
- `apps/admin/lib/admin/actions.ts:151` · concurrency & data integrity
- The duplicate guard is a SELECT (lines 145-149) followed by an INSERT (lines 195-197). matching_candidate has no UNIQUE(request_id, expert_id) or similar (supabase/migrations/0012_matching_ai.sql defines only plain indexes), so two concurrent calls both see zero rows and both insert 3 candidate rows → 6 candidates with duplicate ranks 1-3 for one request, and autoAssignOverdue later matches on '.eq(rank,1)' finding two rank-1 rows for the same request, inserting two matchings. Admin-only surface so contention is low, but the guard is exactly the SELECT-then-INSERT anti-pattern; a UNIQUE(request_id, rank) (candidates are regenerated never, per the '생성 후 불변' design) would make the second insert fail with 23505 which the code could map to the same Korean error.

#### P2-17. submitReview: duplicate-review guard is check-then-insert; review table has no UNIQUE(deal_id, author_type)
- `apps/admin/lib/admin/actions.ts:582` · concurrency & data integrity
- Lines 575-582 SELECT for an existing admin review then INSERT (lines 596-598). Verified in supabase/migrations/0001_init.sql lines 126-135: review has no unique constraint on (deal_id, author_type). A double-submit races past the check and inserts two admin reviews for one deal, which then skews recalcExpertScores (line 612) and any average-rating aggregation permanently — the duplicate is silent and never cleaned up. Same fix pattern: UNIQUE(deal_id, author_type) and treat 23505 as the '이미 리뷰가 작성되었습니다' case. generateAiReview (lines 623-630) has the identical check-then-insert shape against review_ai_suggestion (also no unique on deal_id), though its blast radius is smaller since getAiSuggestion picks latest-by-created_at.

#### P2-18. Supabase SSR cookie pattern deviation: refreshed tokens written only to the response, not to request.cookies
- `apps/admin/proxy.ts:16` · correctness
- The canonical @supabase/ssr middleware pattern writes refreshed session cookies to BOTH request.cookies (so server components rendered later in the same request see the new token) and a response recreated via NextResponse.next({ request }) AFTER the mutation. Here `response` is created once up front (line 6) and setAll only writes to it (lines 14-22), never to request.cookies. Consequence: on the request where the access token is refreshed by `supabase.auth.getUser()` (line 27), every downstream server component / route handler in that same request still reads the OLD (expired) token from the request cookies — createClient(cookieStore) in server actions gets an expired JWT and getUser() can intermittently return null, producing once-per-token-lifetime spurious 'login required' failures that vanish on reload (the browser now has the new cookie). Same shape as the documented Supabase pitfall; the expert/owner proxies (other groups) share this file's pattern and should get the same fix.

#### P2-19. Depth-2 or unknown category param silently applies NO filter, showing all requests
- `apps/expert/app/(browse)/requests/page.tsx:55` · correctness
- The filter only handles selectedCat.depth === 1 and depth === 0. If the category param is a depth-2 (detail) category id — which is a real path, because the request detail page's back link is built as `/requests?category=${request.category_id}` and request.category_id can be a depth-2 category — neither branch matches and no .in() filter is applied at all. The URL claims a category filter, but the list silently shows every open request, and RequestList's selectedMajorIdx also resolves to -1 so no chip appears active. Same silent fall-through for a stale/garbage category id. Either map depth-2 ids up to their parent or filter .eq('category_id', category) for depth 2.

#### P2-20. declineInvitation error result ignored — UI stuck on '거절 중...' forever with no message
- `apps/expert/app/(main)/invitations/[id]/invitation-actions.tsx:22` · honesty / swallowed error
- declineInvitation (apps/expert/lib/invitation/actions.ts) returns { error } on normal, reachable paths: '접근 권한이 없습니다.' and '이미 처리된 초빙입니다.' (e.g. invitation already accepted/declined in another tab). handleDecline awaits it, discards the return value, and never resets `declining`, so on any returned error the button stays disabled showing '거절 중...' indefinitely and the user gets zero feedback. The success path only works because the action redirects. Handle the returned error (show it, reset declining) — contrast with the accept path, which correctly surfaces acceptState.error.

#### P2-21. Search sanitizer strips only % and , — other PostgREST .or() metacharacters can break the filter, and the query error is silently swallowed as '0 results'
- `apps/expert/app/(browse)/requests/page.tsx:75` · correctness / honesty
- `query.replace(/[%,]/g, '')` neutralizes wildcard and condition injection, but the raw value is still interpolated unquoted into the PostgREST .or() logic-tree string (`title.ilike.%...%`). Characters PostgREST parses structurally in that grammar — parentheses, and `.` sequences — can make the filter string unparseable, producing a query error. Line 79 then destructures only `data` (`const { data: requests } = await requestsQuery`), discarding `error`, so a failed query renders the honest-looking empty state "현재 열린 의뢰가 없습니다" instead of an error — a silent fake-success. Use ilike() builder args (which escape values) instead of hand-built .or() strings, and check/surface the query error.

#### P2-22. Breadcrumb reimplemented with an uncached full category-table fetch, duplicating shared getCachedCategories + getCategoryBreadcrumb
- `apps/expert/app/(browse)/requests/[id]/page.tsx:66` · structure / performance
- packages/shared/lib/categories.ts already provides getCachedCategories (5-min TTL) and getCategoryBreadcrumb — used by the sibling list page (requests/page.tsx line 39). This detail page instead fetches the entire category table raw on every request view and hand-rolls the parent-chain walk. That is (a) a per-view full-table query on a hot public page where a cache exists, and (b) the same breadcrumb logic duplicated in two places with no test pinning them together — they will drift. The hand-rolled walk at line 78 also uses `catMap.get(current.parent_id)! : undefined!` non-null-assertion tricks and, unlike the shared depth-bounded version, would infinite-loop the request on a cyclic parent chain in category data. Replace lines 64-83 with the shared helpers.

#### P2-23. Fee hidden when budget_hope is null/0, yet accepting still creates a deal with a 100,000원 default work_fee
- `apps/expert/app/(main)/matching/[id]/page.tsx:71` · correctness/honesty
- The 작업비 block is gated on truthy `req.budget_hope`, so when budget_hope is null (or 0) the expert sees no fee at all — but the accept path (`acceptMatching` in apps/expert/lib/matching/actions.ts line 66: `const workFee = request.budget_hope || 100000`) silently substitutes a 100,000원 work_fee and creates a deal. The expert can accept a matching whose price they were never shown, and the shown-vs-charged logic diverges across two files with no test pinning them together. Either the page should render the same fallback fee the accept path will use, or accept should refuse when budget_hope is missing.

#### P2-24. sendDealMessage awaited without try/catch — network failure leaves optimistic message shown as sent
- `apps/expert/app/(main)/work/[id]/message-thread.tsx:40` · honesty / correctness
- handleSubmit optimistically appends the temp message, then awaits the server action with no try/catch. If the action invocation rejects (network drop, server error before the action returns its {error} shape), the rejection is unhandled: the rollback branch (result.error) never runs, the temp message stays rendered as if delivered, and the typed content is lost from the input. The sibling dispute-button.tsx wraps its request in try/catch and surfaces '네트워크 오류' — this component omits the same protection on the same page.

#### P2-25. Thread state seeded from props and never re-synced; the action's revalidatePath refresh is a no-op for this component
- `apps/expert/app/(main)/work/[id]/message-thread.tsx:21` · structure/state — stale prop seed, two sources of truth
- messages is initialized once from initialMessages via useState. sendDealMessage calls revalidatePath(`/work/${dealId}`), which re-renders the server page with fresh deal_message rows — but the remounted-only useState ignores the new prop, so (a) admin/manager replies never appear until a full remount/navigation, and (b) the sent message keeps its temp-${Date.now()} id and client timestamp forever instead of the persisted row. Two answers to 'what are the messages' (server rows vs local array) that diverge immediately after first send. Sync the seed (e.g. key the component on server data, useEffect re-seed, or render server messages + only pending optimistic ones).

#### P2-26. Duplicate-dispute check is SELECT-then-INSERT with no UNIQUE idempotency key
- `apps/expert/app/api/disputes/route.ts:54` · concurrency-data-integrity
- The 'already open dispute' guard reads first (lines 54-60) and inserts afterwards (line 66). Two concurrent POSTs for the same review_id both see zero existing open disputes and both insert, producing duplicate open disputes despite the 409 guard. No migration in the repo defines the dispute table at all (grep across supabase/migrations finds no CREATE TABLE for dispute), so there is no evidence of a partial UNIQUE index (target_type, target_id) WHERE status='open' backing this check — this is exactly the SELECT-then-INSERT race the pre-check cannot close. Fix: enforce with a partial unique index and translate the unique-violation error to 409.

#### P2-27. Edge runtime export is deprecated in this Next 16 version
- `apps/expert/app/opengraph-image.tsx:3` · correctness / deprecation
- The vendored docs for this exact Next build state: 'The Edge Runtime is deprecated. Remove the `runtime` export from your route files.' (node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/02-route-segment-config/runtime.md line 23). AGENTS.md explicitly says to heed deprecation notices in these docs. ImageResponse works on the Node runtime; drop the export before a future minor removes it. The owner and admin apps' opengraph-image files likely share the pattern (out of this group's scope).

#### P2-28. Ownership/authz and duplicate-guard logic has zero tests (apps/expert has no test files at all)
- `apps/expert/app/api/disputes/route.ts:49` · test-gaps
- Glob for *.test.ts/*.spec.ts under apps/expert returns nothing. The review-ownership check (review -> deal -> expert_id), the 409 duplicate guard, and the 400/401/403/404 branches of this security-relevant endpoint are all unexercised; a regression in the `deal?.expert_id !== expert.id` comparison (e.g. after a select-shape change makes `deal` null-typed differently) would ship silently.

#### P2-29. expert_category sync: unchecked delete-then-insert — a failed insert silently strips the expert of all categories
- `apps/expert/lib/expert/actions.ts:77` · correctness
- The delete of existing expert_category rows and the re-insert are two separate statements with NO error checks on either (results of both awaits are discarded) and no transaction. If the insert fails after the delete succeeds (constraint, transient DB error), the expert ends with zero category mappings — invisible to them and to matching (matching-algo joins on expert_category), with no log and the action still redirecting to '?success='. This is silent data loss on the matching-critical table plus a swallowed error.

#### P2-30. expert.field and expert_category are two sources of truth that drift when no labels match
- `apps/expert/lib/expert/actions.ts:76` · structure/state
- field (comma-joined label string) and expert_category rows both answer 'what categories does this expert have'. The sync runs only inside if (cats && cats.length > 0) — if the submitted field labels match no depth-1 category rows (label drift, renamed category) or the category lookup errors, the delete+insert is skipped entirely and the OLD expert_category mappings persist while expert.field now says something else. The expert keeps being matched under categories they removed. The sync should run whenever field changed (delete always, insert what matched), or field should be derived from expert_category rather than stored separately.

#### P2-31. acceptMatching never checks request.status — a request already 'dealt' can receive a second deal via another matching
- `apps/expert/lib/matching/actions.ts:56` · correctness
- The request is fetched only for budget_hope/scope; its status is neither selected nor checked. deal uniqueness is per matching_id, not per request_id, so if two proposed matchings exist for one request (nothing in this file prevents it), expert B can accept after expert A — sequentially, no race needed — creating a second deal+settlement for the same request and re-stamping it 'dealt'. Guard: select status and refuse unless the request is still in a matchable state, or move the guard into the DB (partial unique index on deal(request_id) for active statuses).

#### P2-32. expressInterest limit: count-then-insert race, and the count includes interests on already-matched requests
- `apps/expert/lib/interest/actions.ts:42` · concurrency
- Two defects in one check. (a) Check-then-act: two concurrent expressInterest calls both read count=4 (<5) and both insert — the max_active_interests cap is not enforceable this way; only a DB-side guard (trigger/constraint) makes it real. (b) The comment says '동시 활성 관심표현 제한' but expert_interest has no status column and rows are only deleted by explicit withdrawInterest — interests on requests that have since gone 'matching'/'dealt' still count. An expert whose interests all got resolved without withdrawal is permanently blocked at the cap until they manually withdraw stale rows. The count should join request.status = 'open' (or interests should be cleaned when a request leaves 'open'). The duplicate-insert path itself is fine (23505 handled by a real UNIQUE constraint).

#### P2-33. updateWorkflowStep ignores deal.status — expert can rewrite workflow steps after inspection/settlement
- `apps/expert/lib/workflow/actions.ts:42` · security/correctness
- verifyDealExpertOwnership selects deal.status (line 21) but no caller ever uses it — a dead read that marks the missing check. An expert can call updateWorkflowStep on a deal in any status (submitted, under owner inspection, disputed, settled) and flip 'deliver' back to 'pending' or forward to 'done', mutating the record the owner/admin relies on for inspection and auto-release. Server actions are public endpoints, so the UI hiding the buttons is not a guard. Gate the update on deal.status being an in-progress status.

#### P2-34. Zero tests over the money-path deal creation and its manual rollback logic
- `apps/expert/lib/matching/actions.ts:32` · test-gaps
- apps/expert contains no test files at all (glob for *.test/*.spec and __tests__ both empty). acceptMatching and acceptInvitation each perform 4-6 dependent writes computing work_fee/match_fee/total_pay with hand-rolled compensation logic — exactly the code whose failure modes (partial rollback, wrong fee, race) do not surface in manual happy-path use. At minimum the rollback branches and calcCapPricing/calcMatchFee wiring deserve unit/integration coverage.

#### P2-35. '__none__' sentinel against a uuid column relies on a swallowed 400 error
- `apps/owner/app/(browse)/experts/page.tsx:65` · correctness/honesty
- expert.id is uuid (0001_init.sql: partner.id uuid primary key, renamed to expert). PostgREST cannot cast '__none__' to uuid, so the empty-category branch produces a 22P02/400 error, not an empty result set. Because the response is destructured as `const { data: experts }` with the error field ignored (line 77), `experts` is null and the page happens to render the empty state — the feature 'works' only via a silently swallowed database error. Any monitoring on DB errors will see noise, and a future refactor that checks the error will break the empty-category path. Use a real empty short-circuit (skip the query and render []) instead of a sentinel value.

#### P2-36. Search string interpolated into PostgREST .or() — parens break the query, commas silently mutate it
- `apps/owner/app/(browse)/experts/page.tsx:74` · correctness/security
- Sanitization strips only % and , before interpolating into the .or() filter string. A query containing ')' (e.g. "AI(인공지능)") prematurely closes the or-group, PostgREST returns 400, the error is discarded at line 77, and the user silently sees '"..."에 맞는 시니어지식인이 없습니다' — a wrong answer with no error surfaced. Stripping ',' also silently changes the search term ("삼성,LG" searches for "삼성LG"), and stripping '%' makes literal-percent searches impossible. Comma-stripping does prevent injecting additional filter conditions, so this is a correctness/DoS-of-the-feature issue rather than exploitable data exposure, but the trust-boundary handling (blocklist regex instead of proper escaping or a textSearch/ilike builder per column) is fragile.

#### P2-37. SELECT-then-INSERT for owner record instead of upsert on the UNIQUE key
- `apps/owner/app/(auth)/callback/route.ts:37` · concurrency & data integrity
- Owner creation is check-then-insert: select by auth_user_id, insert if absent. Two concurrent callback hits for the same new user (double-click, duplicate OAuth redirect, browser prefetch) both see no row and both insert. The UNIQUE constraint on auth_user_id (0001_init.sql line 28, carried through the client→owner rename) prevents duplicate rows, but the loser's insert errors and the user is bounced to /?error=profile_create even though their profile now exists — a spurious login failure exactly on first sign-in. The task-flagged fix applies: use .upsert({...}, { onConflict: 'auth_user_id', ignoreDuplicates: true }) so the race is absorbed by the constraint instead of surfacing as an error.

#### P2-38. Full-table fetch of expert_category on every request plus O(n·m) find loop
- `apps/owner/app/(browse)/experts/page.tsx:24` · performance
- Every page load pulls the entire expert_category table with no filter and no limit (`.select('expert_id, category_id')`), then filters it in JS — unbounded growth: cost scales with total experts × categories platform-wide, not with the 50 rendered rows. The mapping loop at line 84 then does `allCategories.find(...)` per expert_category row, making it O(rows × categories) quadratic. Filter server-side (`.in('category_id', filterCatIds)` for the filter path; `.in('expert_id', renderedIds)` for the display map) and build a Map for category lookup — the page already builds `catMap` this way in experts/[id]/page.tsx.

#### P2-39. Same DB query executed twice per page load; providers derivable from packages already held
- `apps/owner/app/(browse)/services/page.tsx:5` · performance / structure-state
- getProvidersByAudience('owner') internally calls getPackagesByAudience('owner') again (packages/shared/lib/service-package/queries.ts:86), so Promise.all fires two identical service_package SELECTs on every page view. Worse structurally: ProviderInfo (id, name, packageCount, freeCount) is 100% derivable from the packages array that is already serialized to the same client component — two copies of one fact cross the wire, and if the two fetches ever race against a concurrent publish they can disagree (provider card counts vs. visible packages). Derive providers from packages (server- or client-side) and drop the second query.

#### P2-40. Category tree hardcoded in form; DB category table is the source, matched by exact label with silent null on drift
- `apps/owner/app/(main)/request/request-form.tsx:9` · structure-state / duplicated shape
- CATEGORY_TREE duplicates the DB `category` table (seeded in 0011_category.sql/0013_seed_data.sql; also re-hardcoded in apps/expert/app/(main)/register/page.tsx, apps/expert/components/profile-editor.tsx, apps/admin/app/(public)/service/page.tsx) with no pinning test. The server action resolves the chip by exact Korean label: adminClient.from('category').select('id').eq('label', reqType).eq('depth', 1).single() (apps/owner/lib/request/actions.ts:42), and on .single() miss falls to `categoryId = catResult.data?.id || null` — so any rename/reseed of a DB category silently stores the request with category_id null, degrading matching with no error anywhere. Either drive the chips from getCachedCategories or add a test pinning CATEGORY_TREE labels to the seed.

#### P2-41. All six dashboard queries swallow errors — DB failure renders as fake empty state
- `apps/owner/app/(main)/mypage/page.tsx:98` · honesty / swallowed errors
- None of the six Promise.all results has its .error read; every consumer does `(res.data || [])`. If any query fails (RLS/permission change, network, bad join), the page silently renders '등록된 의뢰가 없습니다' / '작성한 리뷰가 없습니다' etc. — indistinguishable from a genuinely empty account, with no log and no user-facing signal. An owner with active deals seeing an empty 매칭 현황 after a schema change is exactly the silent-degradation class the queries in queries.ts at least console.error about; here not even that. Check res.error and render an error state (or throw to the error boundary).

#### P2-42. Message list state captured once from props — revalidated server data never reaches the rendered thread
- `apps/owner/app/(main)/status/[id]/message-thread.tsx:27` · structure/state
- `sendOwnerMessage` calls `revalidatePath('/status/{requestId}')` (apps/owner/lib/message/actions.ts:66), so after every send the RSC page re-renders and passes a fresh `messages` prop that includes the counterpart's (expert/admin) new messages. But the component copies the prop into state exactly once via `useState(initialMessages)` and never reconciles afterward, so the already-mounted thread keeps showing the stale snapshot: expert/admin replies never appear until a hard page reload, and the owner's own sent message survives only as the optimistic `temp-` row rather than the server row. Call the re-apply twice mentally: new props in, rendered surface unchanged — not idempotent. Fix by syncing state to the prop (e.g. key the component on data version, or merge server messages into state) or dropping local state and letting the server list + a pending-only overlay be the single source of truth.

#### P2-43. DB query error rendered as a fake empty state ('아직 등록한 의뢰가 없습니다')
- `apps/owner/app/(main)/status/page.tsx:74` · honesty
- Both list queries ignore `error` on the Supabase response and coalesce `data` to `[]`. A transient DB/network failure is then indistinguishable from a genuinely empty account: the page shows '아직 등록한 의뢰가 없습니다' / '신청한 전문서비스가 없습니다' and summary counts of 0 — silently faking success to an owner who may have active paid deals. Same pattern for `serviceOrdersRes` on line 75. Check `requestsRes.error` and fail loudly (error state or throw to the error boundary) instead of degrading to an empty list.

#### P2-44. All query errors on the detail page swallowed — infra failure masquerades as not-found/no-data
- `apps/owner/app/(main)/status/[id]/page.tsx:51` · honesty
- Every `adminClient` call on this page destructures only `data` and never inspects `error`. If the `request` query fails for an infrastructure reason (not a missing row), `request` is null and the user is silently redirected to /status as if the request didn't exist or wasn't theirs. Likewise a failed `deal` query (line 60-65) leaves `deal` null and renders the dead-end fallback card ('진행 상황을 확인 중입니다') for a deal that may actually be in the paid/working state, and failed workflow/message/review queries render as empty sections. Nothing is logged, so the failure is invisible to both the user and the operator. Distinguish error from no-rows (Supabase returns both fields) and surface/log the error path.

#### P2-45. Guarantee-fund ledger cap applied per refund call, not cumulatively — over-records fund payout on partial refunds
- `apps/owner/app/api/settlements/[id]/refund/route.ts:121` · correctness
- Each refund inserts a guarantee_fund_ledger entry of Math.min(guarantee_fee, amount). With guarantee_fee=5,000 and two partial refunds of 3,000 each, the ledger records 3,000 + 3,000 = 6,000 payout against a settlement that only accrued 5,000 — the per-call min never accounts for what earlier refunds of the same settlement already booked. The single-full-refund path (amount > guarantee_fee → capped at guarantee_fee) shows the intended invariant is 'total payout ≤ guarantee_fee per settlement', which partial refunds break deterministically. Ledger totals for the fund drift upward with every multi-step refund.

#### P2-46. Duplicate-dispute guard is SELECT-then-INSERT with no UNIQUE constraint backing it
- `apps/owner/app/api/disputes/route.ts:62` · concurrency-data-integrity
- The 'already open dispute' check (SELECT ... eq target_type/target_id/status='open' limit 1) followed by a separate INSERT is exactly the race the check exists to prevent: two concurrent POSTs both see zero rows and both insert, yielding two open disputes for the same settlement — the 409 path never fires. No migration in the repo defines a partial unique index on dispute(target_type, target_id) WHERE status='open' (the dispute table's DDL is not in supabase/migrations at all — files jump 0018 → 0024), so nothing at the DB level enforces the invariant. Fix: partial unique index + treat unique-violation as the 409.

#### P2-47. Zero tests on any money path — refund cap, confirm idempotency, webhook signature, orderId round-trip
- `apps/owner/app/api/settlements/[id]/refund/route.ts:13` · test-gaps
- A glob for *.test.ts/*.spec.ts across the repo matches only node_modules. The refund over-refund cap, the confirm-deposit idempotent race (webhook vs success redirect), the webhook HMAC verification, calcPayableAmount consistency between checkout and confirm, and the buildOrderId/parseOrderId pair (whose own comment warns that a one-character drift 'silently rejects every callback') all have no pinning test. These are exactly the pure-logic, high-consequence functions where unit tests are cheapest; payment.ts's parse/build pair is a duplicated-contract-across-files shape that per project rules requires a pinning test.

#### P2-48. requestRevision: deal_message insert result ignored — notification can silently fail after the revision succeeded
- `apps/owner/lib/deal/actions.ts:28` · honesty
- After requestRevisionOp flips the workflow step back to in_progress, the owner-side notification insert into deal_message (lines 28-36) discards the returned error entirely — no destructuring, no check. If the insert fails (constraint, transient DB error), the function still returns `{}` success: the workflow is reset but the expert never receives the '[수정 요청] …' message explaining why, and the owner believes the expert was notified. Also a two-step write with no transaction: workflow update in requestRevisionOp and the message insert here can diverge. At minimum check the insert error and surface/log it.

#### P2-49. Zero tests over owner deal actions — money-adjacent authz/state flows (approve, confirm, review) untested
- `apps/owner/lib/deal/actions.ts:41` · test-gaps
- Glob for apps/owner/**/*.{test,spec}.* returns no files. approveDeal (quote → working), confirmDeal (working → done + escrow reviewing), requestRevision, and submitReview are the owner-side state machine around escrowed money and expert scoring, all reachable as server actions; none has a unit or integration test (status-guard rejection paths, ownership rejection, duplicate-review path). The duplicate-review race (finding above) is exactly the class a concurrency test on this module would have caught.

#### P2-50. Deal status write is read-then-write with no compare-and-set across a 15s external call
- `apps/owner/lib/payments/confirm-deposit.ts:81` · concurrency-data-integrity
- deal.status is read at line 21-25, then the Toss confirm HTTP call (up to TOSS_TIMEOUT_MS=15s) runs, then the update writes status='working' filtered only by .eq('id', dealId) — no .eq('status','quoted') CAS. Any status transition made during that window (e.g. admin cancels/disputes the deal) is silently overwritten back to 'working' (lost update). The settlement update directly above (line 66) does it correctly with .eq('escrow_status','pending'); this write should carry the same guard.

#### P2-51. Invitation cap and duplicate guard are SELECT-count-then-INSERT races
- `apps/owner/lib/invitation/actions.ts:88` · concurrency-data-integrity
- Both the max-5 active-invitations check (lines 70-78) and the duplicate-invitation check (lines 81-90) count first and insert later with no DB constraint backing them. Two concurrent submissions (double-click, two tabs) both observe count below the cap and dupCount=0, then both insert — producing duplicate invitations to the same expert and/or exceeding the configured cap. Per quality-gates §2 this needs a partial UNIQUE index (owner_id, expert_id) WHERE status='invited' for the dup guard; the SELECT-then-INSERT is itself the race. No such constraint exists in any checked-in migration.

#### P2-52. Refreshed auth cookies not synced back to the request — stale token reaches server components
- `apps/owner/proxy.ts:16` · correctness
- setAll writes refreshed session cookies only to the outgoing response. The official @supabase/ssr middleware pattern also does request.cookies.set(...) and then recreates NextResponse.next({ request }) so that server components rendered in the SAME request see the refreshed token. As written, on the request where the access token expires and gets refreshed, downstream RSCs still read the old cookie → supabase.auth.getUser() fails → the user is rendered logged-out/redirected for that one request (recovers on the next). Intermittent, order-dependent, invisible to tests — exactly the documented Supabase SSR pitfall.

#### P2-53. Owner auto-create block duplicated verbatim in 3 files, with insert error swallowed
- `apps/owner/lib/invitation/actions.ts:38` · structure-state
- The 'select owner by auth_user_id, insert if missing' block appears identically in invitation/actions.ts (lines 29-48), request/actions.ts (lines 39-59), and services/actions.ts (lines 37-56) — third repetition with no shared helper and no pinning test, so the copies can drift independently. Additionally each copy ignores the insert error entirely (only data is destructured): under the auth_user_id UNIQUE constraint, two concurrent first-time actions race, the loser's insert fails, newOwner is null, and the user is told '계정 생성에 실패했습니다' even though the account row now exists. A single getOrCreateOwner helper doing upsert-on-conflict (or re-select on unique violation) fixes both.

#### P2-54. Escrow money path has zero automated tests despite a documented idempotency contract
- `apps/owner/lib/payments/confirm-deposit.ts:16` · test-gaps
- confirmAndRecordDeposit is the single point where real money capture is recorded, is called from two racing endpoints (webhook + success redirect), and its header comment specifies a three-clause idempotency contract — yet no *.test.* / *.spec.* file exists anywhere under apps/owner and nothing in the repo references confirmAndRecordDeposit from a test. The already-processed short-circuit, the ALREADY_PROCESSED_PAYMENT continuation, and the capture-then-DB-failure retry path (which finding #1 shows is broken) are all unpinned; a regression here silently loses or double-counts payments.

#### P2-55. 3-day release timer derived from mutable updated_at, not a stored reviewing-transition fact
- `packages/shared/lib/automation/auto-settlement.ts:45` · structure/state design / correctness
- The comment contract is 'reviewing 상태 전환 후 3일 경과', but the implementation uses settlement.updated_at, which the set_updated_at trigger bumps on ANY update to the row. Any unrelated touch of a reviewing settlement resets the 3-day clock indefinitely; conversely nothing records when the reviewing transition actually happened. The fact 'when did this enter reviewing' has no owner — it is inferred from a general-purpose column that other writers also move. Store a reviewing_at timestamp at the transition (deal-operations confirmDealOp) and filter on that.

#### P2-56. Non-numeric audit_sample_rate silently disables all auditing for 5 minutes at a time
- `packages/shared/lib/automation/audit-sampling.ts:23` · resilience / honesty
- parseFloat of a malformed platform_config value yields NaN; `Math.random() < NaN` is always false, so 100% of records are marked auto_passed and 0% audited — with no log line distinguishing this from a legitimately low rate — and the NaN is cached for the 5-minute TTL and re-cached on every refresh while the bad value persists. Also the config .single() error is swallowed (a DB error is indistinguishable from 'row missing' and silently falls back to 0.05). Validate with Number.isFinite and clamp to [0,1], logging loudly on fallback.

#### P2-57. Returns sampledIds as 'audited' even when the marking UPDATE failed
- `packages/shared/lib/automation/audit-sampling.ts:71` · honesty
- If the queue_status='audited' update errors, the failure is console.error'd and the function still returns the full sampledIds list; the caller (auto-settlement) then reports result.audited = sampledIds.length for rows that were never actually marked. The return value fakes success — an operator reading 'audited=5' believes 5 settlements entered the audit queue when zero did. Return only ids whose update succeeded (or propagate the error).

#### P2-58. Deal status transitions use SELECT-then-UPDATE with no compare-and-set
- `packages/shared/lib/deal/deal-operations.ts:24` · concurrency & data integrity
- approveDealOp checks deal.status !== 'quoted' from a prior SELECT (inside verifyDealOwnership), then updates by id only — the UPDATE lacks `.eq('status', 'quoted')`. confirmDealOp (line 66) has the same shape for working→done. A concurrent transition between check and write (e.g., admin cancels the deal, or a double-submitted approve racing a cancel) is overwritten — classic lost update on a state machine. Add the expected-status predicate to each UPDATE and treat 0 affected rows as the '승인할 수 없는 상태' error.

#### P2-59. requestRevisionOp ignores its UPDATE result entirely and returns success
- `packages/shared/lib/deal/deal-operations.ts:112` · honesty / correctness
- The deal_workflow update's return value is not destructured — neither error nor affected rows is checked — and the function unconditionally returns { ownerId }, which callers treat as success (the owner app then inserts a deal_message announcing the revision request). If the update fails, or if no deal_workflow row matches (deal_id, step='deliver') so 0 rows are touched, the owner is told the revision was requested while the expert's checklist never changes. Also note the '수정 요청: ...' note overwrites any previous note on re-request (single note column, last write wins).

#### P2-60. Zero tests anywhere in the repo for the money-math and scoring pure functions
- `packages/shared/lib/cap-pricing.ts:18` · test-gaps
- The repo contains no *.test.* files at all (verified via find, excluding node_modules) and no test scripts. calcCapPricing/calcMatchFee band boundaries (30000 throw, 100000/300000/500000/800000/3000000 breakpoints), computeCareerScore/computeReviewScore/computeCompletionScore, getCategoryBreadcrumb depth handling, and the auto-settlement eligibility filter are all pure, trivially testable logic that decides money amounts and expert ranking — with no regression net. A one-band off-by-one in calcMatchFee changes real invoices silently.

#### P2-61. Query errors swallowed as zeros, then cached for 10 minutes
- `packages/shared/lib/landing-stats.ts:75` · honesty
- None of the seven parallel Supabase queries checks `.error` — supabase-js never throws, so a failed query yields `count: null` / `data: null`, which `?? 0` / `?? []` silently converts to zero stats (0 experts, 0 deals, null satisfaction). The wrong result is then written into the module TTL cache (line 127 `ownerStatsCache = { data: result, ts: Date.now() }`), so one transient DB error poisons the landing page with fake zeros for a full 10 minutes with no log or signal. Violates the no-silent-fallback rule: either surface the error or at minimum skip caching a result built from failed queries. Same pattern in fetchExpertLandingStats (lines 157-160, 213).

#### P2-62. Full-table row fetches for averages/counts, silently capped at PostgREST's 1000-row limit
- `packages/shared/lib/landing-stats.ts:69` · performance
- avgSatisfaction fetches every `review.rating` row, avgBudget fetches every `request.budget_hope` row (line 151), and categoryCounts fetches every `expert_category` row (line 72) / every open `request.category_id` row (line 154) to aggregate in JS. Supabase/PostgREST applies a default max-rows cap (1000), so once any of these tables passes 1000 rows the average/count is computed over a silent truncation — the number keeps rendering but is wrong, with no error. It is also unbounded payload growth on a landing-page path. Use SQL aggregation (avg(), count() group by) or an RPC instead of shipping whole tables to the app.

#### P2-63. cancelPayment throws on timeout/network failure, breaking its own {success:false} contract
- `packages/shared/lib/payment.ts:173` · resilience
- confirmPayment explicitly documents and implements a never-throw contract (fetch wrapped in try/catch, lines 116-135) because callers check only `.success`. cancelPayment has the identical result shape and the identical caller pattern — apps/owner/app/api/settlements/[id]/refund/route.ts:80-81 does `const cancelResult = await cancelPayment(...); if (!cancelResult.success)` with no try/catch — but its fetch is NOT wrapped, so an AbortSignal timeout or network failure escapes as an unhandled exception out of the refund route. Worse, in that window the cancel may have succeeded at Toss while local settlement state was never updated (money moved, DB says otherwise); recovery relies entirely on the admin retrying with the same idempotency key. Wrap the fetch like confirmPayment does and return {success:false, error}.

#### P2-64. Hand-maintained mirror of DB query shapes with no pinning test — drift already happened once
- `packages/shared/lib/query-types.ts:8` · structure/state
- This file restates Supabase relational-select shapes (owner/expert/deal/settlement columns) as manual interfaces used across three apps to cast query results. Nothing pins these to database.types.ts, so a column rename or type change compiles cleanly while every consumer's cast silently lies. The file's own `v2` header line is evidence the drift class is real (client→owner, partner→expert, career_yrs→career_years all had to be chased by hand). Per coding-style.md, a duplicated shape needs a pinning test (e.g. `satisfies`-based assignability checks against Database['public']['Tables'][...]['Row'] picks) that goes red when the generated types diverge.

#### P2-65. Zero tests on money-critical pure functions (fee bands, VAT, orderId round-trip)
- `packages/shared/lib/pricing.ts:6` · test-gaps
- The repo contains no project-owned test files at all (glob over **/*.test.ts hits only node_modules). calcMatchFee implements a 7-band fee schedule where every band boundary (30000/100000/300000/500000/800000/3000000) and the throw path decide real billing; calcVat/calcPayableAmount are documented as the single source for what the customer is charged; and payment.ts's buildOrderId/parseOrderId pair carries a comment explicitly warning that a one-character drift 'silently rejects every callback' — exactly the case a round-trip pinning test exists for. All are pure, trivially testable, and untested; findCandidates and calculateAiRating scoring rules likewise. This is the highest-value/lowest-cost test gap in the shared package.

#### P2-66. getPackageBySlug swallows DB errors entirely — outage rendered as 404
- `packages/shared/lib/service-package/queries.ts:78` · honesty
- `if (error || !data) return null` conflates 'row not found' with any query/connection failure, and unlike getPackagesByAudience it does not even console.error. During a transient Supabase outage or a schema/select mismatch, every package detail page silently 404s (callers treat null as not-found) with zero log trail. Honesty rule: errors must fail loudly or be a documented, logged degradation — this is neither. At minimum log the error; better, distinguish PGRST116 (no rows) from real errors and rethrow the latter.

#### P2-67. Hand-written DB row mirror + double cast defeats generated types — drift has no pinning test
- `packages/shared/lib/service-package/queries.ts:67` · structure/state
- PackageRowWithProvider (lines 12–28) restates the service_package schema by hand, and the `as unknown as PackageRowWithProvider[]` double cast throws away whatever typing supabase-js infers from the select string. The generated Database types (used everywhere else via types.ts) are bypassed, so if migration 0025+ ever renames/retypes a column (or the `provider:provider!inner(name)` embed shape changes between object and array), tsc stays green and the drift is discovered at runtime as undefined fields. No test pins the interface to the generated Row type. Derive the shape from Database['public']['Tables']['service_package']['Row'] or add a type-level pinning assertion.

#### P2-68. client_send_message RLS policy does not bind sender_id — attribution spoofing
- `supabase/migrations/0007_owner_engagement.sql:18` · security
- The INSERT policy checks sender_type = 'client' and deal ownership, but never constrains the sender_id column. Compare the sibling partner policy in 0006_partner_engagement.sql line 49, which explicitly requires `sender_id in (select id from partner where auth_user_id = auth.uid())`. deal_message has RLS enabled (0006 line 38) and sender_id is a bare `uuid not null` with no FK, so any authenticated client holding the Supabase anon key can call PostgREST directly and insert a message into their own deal with sender_id set to ANY uuid — an admin's id, the partner's id, or another client's id. If any UI resolves the author from sender_id, the message is displayed as written by someone else; at minimum the audit trail of who said what in a dispute-bearing deal thread is forgeable. Fix: add `sender_id in (select c.id from client c where c.auth_user_id = auth.uid())` to the WITH CHECK, mirroring the partner policy.

#### P2-69. Migration alters table `provider` that no migration in the repo creates; RLS enable state unverifiable
- `supabase/migrations/0024_provider_member.sql:12` · correctness
- The migrations folder jumps from 0018 to 0024 — migrations 0019–0023 (which per this file's own comments contain the provider table creation, the partner→expert rename, and the 0019 updated_at trigger loop it guards against) are absent from supabase/migrations/. Consequences: (1) the migration chain cannot be replayed on a fresh database — 0024 fails at ALTER TABLE provider because the table does not exist; (2) it is impossible to verify from the repo whether RLS is even enabled on provider — no `alter table provider enable row level security` exists anywhere in the folder, so the two policies created here (lines 44–47) are either inert (RLS off → with Supabase's default grants the whole provider table is readable/writable via the anon key) or live (RLS on → see the self-update finding). Restore or squash-commit the missing migrations, and add an explicit `ALTER TABLE provider ENABLE ROW LEVEL SECURITY` (idempotent) next to the policies, as 0025 does for service_package (0025 line 38).

#### P2-70. Slug immutability is comment-only policy — nothing in the DB enforces it
- `supabase/migrations/0025_service_package.sql:13` · data-integrity
- The comment declares slug immutable after creation ('생성 후 불변 정책') because `service_order.package_slug` snapshot lookups (and the 0026 backfill) join on it. But there is no trigger or constraint preventing UPDATE of slug, and the `service_package_owner_all` FOR ALL policy explicitly permits a partner to change it via direct API. If a slug changes, legacy orders whose `service_package_id` is NULL (unmatched backfill or ON DELETE SET NULL) can never be re-linked, and any remaining slug-based lookup silently misses. A BEFORE UPDATE trigger raising on `NEW.slug IS DISTINCT FROM OLD.slug` would make the stated policy real.

## 3. P3 — 120건

#### P3-1. All deal_message rows fetched to derive per-deal counts
- `apps/admin/app/(protected)/dashboard/page.tsx:242` · performance
- For every working deal, every message row is transferred just to increment messageCounts[deal_id] (lines 247-249). Message threads grow without bound per deal; a grouped count (or head:true count per deal via RPC) would return one row per deal instead of one row per message.

#### P3-2. State-mutating automation runs as a side effect of a GET page render
- `apps/admin/app/(protected)/dashboard/page.tsx:39` · concurrency/design
- autoAssignOverdue() and runAutoRelease() mutate matchings/settlements on every dashboard render — a GET that can also be triggered by router prefetch and by multiple concurrent admin tabs. The double-assign race is mitigated server-side (the action uses a .eq('status','pending') compare-and-set, per its own comment), and dedicated cron routes exist for the same jobs, so this is duplicated triggering rather than corruption. Two residual issues: failures surface only as console.error, invisible to the admin looking at the very dashboard that claims current state; and render-time mutation means the summary counts computed a few lines later can already reflect (or miss) the mutation depending on timing. The intent is documented in the comment, so flagged at P3 rather than P2.

#### P3-3. '즉시 처리' banner counts disputes + overdue settlements but navigates only to the dispute tab
- `apps/admin/app/(protected)/dashboard/dashboard-tabs.tsx:72` · correctness
- urgentCount (page.tsx:84) is disputeOpen + settlements stuck in reviewing >7 days, yet the banner's click handler always opens the dispute tab. When the urgency is entirely overdue settlements (disputes = 0), the admin lands on an empty dispute tab with 'N건' unaccounted for — the number and the destination answer different questions.

#### P3-4. est_hours rendered under a cap_amount-only null guard — can print 'nullh'
- `apps/admin/app/(protected)/dashboard/invitation-tab.tsx:95` · correctness
- The guard on line 93 checks only inv.cap_amount != null, but the span also renders inv.est_hours, which is independently nullable per the interface (est_hours: number | null). A row with cap_amount set and est_hours null renders 'nullh · 1,000,000원'.

#### P3-5. closeInquiry failure is silently dropped — no user-visible error
- `apps/admin/app/(protected)/dashboard/inquiry-tab.tsx:39` · honesty
- The success path is handled (optimistic closedIds add), but when result.error is set nothing happens: the spinner clears and the row stays open with no explanation. The admin's only signal that the close failed is that nothing changed. A one-line error state (as partner-tab does) closes the gap.

#### P3-6. Assign button has no in-flight disable — double-click fires the action twice
- `apps/admin/app/(protected)/dashboard/matching-tab.tsx:266` · correctness
- handleAssign has no busy state and the 배정/이 후보로 매칭 button never gets disabled, unlike every other mutating button in this dashboard (resolving, closingId, busy, generating). A double-click fires createMatching/selectCandidate twice; the server's status guard rejects the second call, but its error ('이미 매칭된 의뢰' style) is then rendered into actionError, telling the admin the assignment failed right after it succeeded. Also, neither handleShowCandidates nor handleAssign has a try/catch, so a thrown server action leaves loading stuck on '후보 검색 중...' permanently.

#### P3-7. autoAssignAt read via type-assertion escape — action return type doesn't declare the field
- `apps/admin/app/(protected)/dashboard/matching-tab.tsx:85` · structure
- The cast (result as { autoAssignAt?: string }).autoAssignAt (repeated at line 100) means getCandidatesForRequest's declared return type is missing a field the UI depends on. The compiler can no longer catch the action renaming or dropping autoAssignAt — the countdown feature would silently disappear. Fix the action's return type and delete both casts.

#### P3-8. Query→component shape bridged with 'as unknown as' double cast, no pinning
- `apps/admin/app/(protected)/dashboard/page.tsx:329` · structure
- draftPackages flows into DraftPackageItem[] (declared independently in partner-tab.tsx) through an as unknown as cast, and allProviders through a bare as ProviderItem[] on line 328 — the select string in this file and the interface in partner-tab.tsx are the same shape maintained in two places with the type system explicitly disabled between them. Adding/removing a column in the select will not fail compilation; it will render undefined. Same pattern in inquiry-tab.tsx:6, which declares its own local InquiryItem while this page constructs the InquiryItem imported from @jisane/shared/query-types — two definitions of one shape with no test or type relation pinning them together.

#### P3-9. AI suggestion auto-seeded into form state, making the explicit 'AI 제안 적용' button redundant and defeating confirmed/modified tracking
- `apps/admin/app/(protected)/review-input/[id]/review-form.tsx:61` · structure/state
- The star-rating state is initialized from aiSuggestion (`aiSuggestion?.overall_rating || 0` etc.), yet the UI also offers an explicit 'AI 제안 적용' button (handleApplyAi) that sets the exact same values — on first render the button is a no-op, and the design clearly intended application to be an explicit act. Because the axis ratings are therefore always populated whenever a suggestion exists, submitReview's `isModified = processRating !== undefined` marks the suggestion 'modified' even when the admin submits the AI values untouched, so the pending→confirmed path is effectively unreachable and any confirmed-vs-modified analytics are skewed. Drop the auto-seed (start at 0) and let the button be the single application mechanism.

#### P3-10. Message-count badge is a server snapshot that goes stale after the optimistic reply append
- `apps/admin/app/(protected)/dashboard/progress-tab.tsx:110` · structure/state
- msgCount comes from the server-provided messageCounts prop while the thread contents live in client `messages` state. After handleSendReply optimistically appends the admin's reply (and after any messages loaded that arrived post-render), the badge still shows the old count — the same question ('how many messages does this deal have') is answered by two owners that are never reconciled. Minor since it self-corrects on next dashboard render, but derive the count from the loaded thread when it is open.

#### P3-11. Reviewable-state invariant (all 5 workflow steps done) enforced only by the dashboard link, not by page or submitReview
- `apps/admin/app/(protected)/review-input/[id]/page.tsx:35` · test-gaps/correctness
- The dashboard shows the '리뷰 입력' link only when all 5 workflow steps are done, but this page renders the form for any existing deal id regardless of status, and submitReview (verified in lib/admin/actions.ts) checks only deal existence and duplicate review — not deal status. An admin navigating directly can file a completion review for an in-progress deal, which then feeds recalcExpertScores. Low impact (admin-only actor) but the invariant is client-link-only; enforce deal.status in the action.

#### P3-12. API route duplicates the server-action matching flow but skips matching_candidate reconciliation
- `apps/admin/app/api/admin/matching/route.ts:33` · structure-state / correctness
- apps/admin/lib/admin/actions.ts (createMatching, ~line 222-241) performs the same request-status check + matching insert + request update, but ALSO marks matching_candidate rows 'selected'/'skipped' before inserting. This route creates the matching without touching matching_candidate, so candidates for the request are left 'pending' — two code paths answer 'create a matching' with divergent resulting state, with no test pinning them together. A matching created through this ops endpoint leaves stale pending candidates that the dashboard and the auto-assign batch still see as live.

#### P3-13. Unguarded request.json() and no schema validation of body
- `apps/admin/app/api/admin/matching/route.ts:10` · correctness / boundary-validation
- Malformed/non-JSON body makes `await request.json()` throw, producing an unhandled 500 instead of a 400. request_id/expert_id are only truthiness-checked — no type/UUID validation (global rule: validate at boundaries with a schema). A non-UUID value surfaces as a raw Postgres error via the 500 path.

#### P3-14. Raw Supabase error.message returned to the client
- `apps/admin/app/api/admin/deals/route.ts:24` · security / honesty
- On query failure the untransformed database error message (table/column names, constraint details) is sent in the 500 response. Caller is admin-secret authenticated, so exposure is limited, but it leaks schema internals and is inconsistent with the Korean user-facing error copy used elsewhere. Same pattern at matching/route.ts:42. Already noted in docs/10_UX_AUDIT_2026-08-10.md item 19 and still unfixed.

#### P3-15. Timing-unsafe admin secret comparison (===/!== instead of constant-time compare)
- `apps/admin/app/api/admin/deals/route.ts:6` · security
- The x-admin-secret header is compared with `!==`, a non-constant-time string comparison. Same pattern in matching/route.ts:6 and the other x-admin-secret routes. docs/07_CODEBASE_AUDIT_2026-07-16.md flagged this (L-4) recommending crypto.timingSafeEqual and it remains unfixed — a known-but-open item, not a documented-intentional tradeoff. Practical exploitability over network jitter is low, hence P3.

#### P3-16. Unbounded queries — no limit/pagination on deals or workflows
- `apps/admin/app/api/admin/deals/route.ts:21` · performance
- Both the deal query and the follow-up deal_workflow `.in(dealIds)` query fetch every matching row with no `.limit()` or range. Fine at current scale for an ops endpoint, but response size and the in-memory grouping grow linearly and unboundedly with deal history. The status filter defaulting to 'working' mitigates but does not bound it (e.g. status=done grows forever).

#### P3-17. Silent fallback to hardcoded production URLs when NEXT_PUBLIC_* env is unset
- `apps/admin/app/(public)/ax-process/page.tsx:81` · honesty / structure-state
- `process.env.NEXT_PUBLIC_OWNER_URL || 'https://owner.jisane.cloud'` (and NEXT_PUBLIC_EXPERT_URL in service/page.tsx:18-19) silently points CTAs at production if the env var is missing — a preview/staging build with a misconfigured env sends users to prod with no signal. The literal fallback URL is also duplicated across at least two files with nothing pinning them together. Global rules prefer throwing on unset config or a documented deliberate fallback; there is no comment marking this as deliberate.

#### P3-18. Owner ownership check skipped entirely when deal.request_id is null
- `apps/admin/app/docs/quote/[dealId]/page.tsx:40` · security (row authz)
- For non-admin users the only row-scope check is inside `if (deal.request_id)`. deal.request_id is nullable in the schema (0001_init: `request_id uuid references request(id)` with no NOT NULL), and this branch explicitly anticipates null — but when it is null, any authenticated user who has an owner profile can view any such deal's quote (work_fee, match_fee, total_pay, cap pricing, request detail). Latent today because both current deal-creation paths (matching, invitation) populate request_id, but the schema permits the state and seed/manual/admin data can produce it. For a request_id-null deal the check should fall back to invitation.owner_id (the deal.invitation_id is already fetched later) or deny.

#### P3-19. rating accepts non-integers and numeric strings; DB error leaks via error.message
- `apps/admin/app/api/reviews/route.ts:17` · correctness (boundary validation)
- `!rating || rating < 1 || rating > 5` passes rating=4.5 and rating="3" (string comparison coerces). The review.rating column is `int check (rating between 1 and 5)`; a non-integer or non-coercible value fails at the DB and the raw Postgres error.message is returned to the caller (line 44). Validate with Number.isInteger and a schema per the boundary-validation rule; deal_id is likewise unvalidated (non-UUID → DB error 500).

#### P3-20. req.json() outside the try block; malformed body crashes to an unhandled 500
- `apps/admin/app/api/expert-activity/route.ts:18` · correctness (boundary validation)
- The try/catch covers only verifyAdmin(). A request with an invalid or empty JSON body makes `await req.json()` throw, producing an unhandled exception instead of a 400. Also `approved_by` is stored verbatim from the request body with no validation — an arbitrary string becomes the recorded approver identity (admin-gated, so integrity-minor).

#### P3-21. No length cap on content/category; unguarded JSON parse; sequential profile lookups
- `apps/admin/app/api/inquiry/route.ts:22` · correctness (boundary validation) / performance
- POST validates only that content is a string — any authenticated user can store megabyte-scale content and arbitrary category strings (unbounded growth; the admin GET later ships these rows). `await request.json()` is unguarded (malformed body → unhandled 500 instead of 400). The expert and owner lookups (lines 27-37) are sequential awaits with no dependency — trivially parallelizable. Cap content length, validate category against the known set, wrap the parse.

#### P3-22. Unbounded query — no .limit() on the requests list
- `apps/admin/app/api/admin/requests/route.ts:13` · performance
- The sibling admin GET (/api/inquiry) caps at .limit(50), but this endpoint returns every request row matching the status filter, growing without bound as the table grows. The default status 'open' narrows it today; a status like 'done'/'closed' accumulates forever. Add a limit/pagination.

#### P3-23. Two parallel admin-auth mechanisms across sibling admin endpoints; non-constant-time secret compare
- `apps/admin/app/api/admin/requests/route.ts:6` · structure/state (two sources of truth) / security
- Admin identity is answered by two different owners: x-admin-secret header compared with `!==` (this file, settlements, inquiry GET, reviews) versus session-based verifyAdmin()/ADMIN_EMAILS (expert-activity, quote page's isAdmin). Same question, two answer paths — they can drift (rotating ADMIN_SECRET does not touch ADMIN_EMAILS holders and vice versa). The `!==` string compare is also not constant-time (timing side channel; low practical risk over network jitter). Consolidate on one server-side admin check helper.

#### P3-24. request row fetched twice and six sequential round-trips for one page
- `apps/admin/app/docs/quote/[dealId]/page.tsx:54` · performance
- The request table is queried once for owner_id (line 41, authz) and again for title/detail (line 54) — one select of `owner_id, title, detail` serves both. Overall the page performs up to six sequential Supabase round-trips (deal, owner, request x2, expert, invitation) that a single joined select (deal with request/expert/invitation embeds) would replace. Server-rendered doc page, so latency is user-visible.

#### P3-25. Third-party chatbot script loaded on every admin route, including the protected dashboard
- `apps/admin/app/layout.tsx:79` · security
- The root layout injects embed.js from an external Cloud Run host (ragbot-web-n6qj3b5f3q-du.a.run.app) with lazyOnload strategy on every page of the admin app — including /dashboard, /docs/quote, /docs/statement and the partner dashboard. A remote script has full DOM/session access; if that host (apparently a separate first-party bot service, but still a distinct trust domain with its own deploy pipeline) is compromised or the *.run.app URL is ever recycled, arbitrary JS runs inside the admin console. Consider scoping the widget to public marketing routes only (a nested layout or pathname guard) and excluding the (protected) and partner/dashboard trees.

#### P3-26. Three independent lookups (settlement, request title, expert) serialized instead of parallel
- `apps/admin/app/docs/statement/[dealId]/page.tsx:50` · performance
- After the authz check, the settlement query (line 50), request-title query (line 59), and expert query (line 71) are awaited sequentially even though they are mutually independent — three DB round trips in series where one Promise.all would do (the pattern is already used correctly at line 29 for the owner/expert lookups). Minor latency on a print page, but it triples the sequential query depth for no reason.

#### P3-27. Dashboard pages check provider existence only; active-status gate lives solely in the layout
- `apps/admin/app/partner/dashboard/orders/page.tsx:16` · security
- The layout uses requireActiveProvider (status must be 'active') and its comment declares it the authz point, but all three pages under it (orders/page.tsx:16, page.tsx:17, profile/page.tsx:17) re-guard with the weaker getProviderByAuthUser, which passes for pending/rejected/suspended providers too. Next.js layouts do not re-execute on soft navigation between sibling pages under the same layout segment, so a provider suspended mid-session can keep soft-navigating between 개요/신청 확인/전문가회원 정보 and reading order/owner data until a hard reload finally re-runs the layout redirect. Mutations are safe (completeOrder/updateProviderProfile in lib/partner/actions.ts re-run requireActiveProvider server-side) and the exposure is scoped to the provider's own rows, so this is defense-in-depth erosion rather than cross-tenant leakage — but the pages already pay for a provider fetch and could call requireActiveProvider at the same cost.

#### P3-28. Save-success signal (?success=saved) is emitted but never consumed — silent success
- `apps/admin/app/partner/dashboard/profile/page.tsx:11` · honesty
- updateProviderProfile (apps/admin/lib/partner/actions.ts:98) ends with redirect('/partner/dashboard/profile?success=saved'), but this page takes no searchParams prop and nothing in apps/admin reads a success param (grep: no useSearchParams and no searchParams-success reader anywhere in the app; the services pages have the same dropped params success=created/saved). After saving, the user sees the form re-render with no confirmation at all — the success path is indistinguishable from having done nothing, while the error path does render state.error in profile-form.tsx:54. Either render a success notice from the param or drop the dead query string.

#### P3-29. Provider row fetched twice per request (layout + page) with no request-level dedup
- `apps/admin/app/partner/dashboard/page.tsx:16` · performance
- Every dashboard page view runs requireActiveProvider in layout.tsx:23 and then getProviderByAuthUser again in the page (page.tsx:16, orders/page.tsx:15, profile/page.tsx:16) — two identical `provider` SELECTs against Supabase per request. getProviderByAuthUser in packages/shared/lib/provider/auth.ts is not wrapped in React.cache(), so the duplicate roundtrip is not deduplicated. Wrapping it in React.cache() (or having pages reuse requireActiveProvider, which also fixes the status-gate finding) removes the extra hot-path query.

#### P3-30. Search only covers the 100 most recent orders; truncation is invisible
- `apps/admin/app/partner/dashboard/orders/page.tsx:23` · correctness
- The page fetches at most 100 orders (limit(100), newest first) and OrdersList filters client-side over that slice. Once a provider passes 100 orders, older orders become unreachable: they are neither listed nor findable via the search box, and no UI element indicates the list is truncated or offers pagination. A search miss renders '검색 결과가 없습니다' even when a matching older order exists in the DB.

#### P3-31. ?success= redirect param has no consumer — success feedback silently dropped
- `apps/admin/app/partner/dashboard/services/page.tsx:12` · dead-code
- createServicePackage and updateServicePackage redirect to '/partner/dashboard/services?success=created' / '?success=saved' (apps/admin/lib/partner/actions.ts:174, 234), but this page takes no searchParams and a grep of the entire apps/admin/app/partner tree finds no reader (no toast, no banner, no useSearchParams). The success half of the action contract is dead: users get error feedback inline but no success confirmation, and the URL carries a meaningless param. Either render the confirmation (a toast component exists in packages/ui) or drop the param from the redirects.

#### P3-32. Page guard checks provider existence, not active status — leans on layout guard that does not re-run on soft navigation
- `apps/admin/app/partner/dashboard/services/page.tsx:18` · security
- This page (and [id]/page.tsx:21 identically) only checks that a provider row exists; the status check ('active' vs pending/rejected/suspended) lives solely in the dashboard layout (requireActiveProvider in apps/admin/app/partner/dashboard/layout.tsx:23). In the App Router the layout segment is cached on client-side navigation, so a provider suspended mid-session can keep browsing dashboard pages whose own guard passes. Exposure is limited — all queries are scoped to the provider's own rows and every mutation re-checks requireActiveProvider server-side — so this is a defense-in-depth gap, not a cross-tenant hole. Using requireActiveProvider in the pages too would close it.

#### P3-33. OAuth callback: check-then-insert of expert row treats a unique-violation race as fatal, bouncing a valid user to an error page
- `apps/expert/app/(auth)/callback/route.ts:37` · correctness
- Lines 30-34 SELECT the expert row, and if absent, insert (lines 37-41). Two concurrent callback hits (double-click on the OAuth consent redirect, browser prefetch, or retried navigation) both see no row; assuming expert.auth_user_id kept the UNIQUE it inherits from the original partner schema (0001_init.sql line 44), the second insert fails 23505 and the code redirects that fully-authenticated user to `/?error=profile_create` — a fake failure: the profile exists, the session is valid. The insert error handling should distinguish 23505 (row now exists → proceed to '/' or '/register') from real failures, or use upsert on auth_user_id. Also note `.single()` on line 34 errors (not just returns null) when the constraint is somehow absent and duplicates accumulated, which would loop into inserting more duplicates.

#### P3-34. user.email! non-null assertion: OAuth account without email makes signup permanently fail
- `apps/expert/app/(auth)/callback/route.ts:40` · correctness
- The provider is read as kakao-or-google (line 27), and Kakao accounts can lack an email (email consent optional unless the Kakao app enforces it). For such a user, `user.email!` passes undefined into a NOT NULL email column, the insert fails every time, and the user is redirected to `/?error=profile_create` on every login attempt — a hard signup dead-end with no recovery path. Either enforce email consent at the provider config and document that here, or make the column/payload nullable like apps/admin/lib/partner/actions.ts does (`email: user.email ?? null`, line 56 of that file).

#### P3-35. Unbounded full-table fetch of expert_category (and all active experts with select *) on every candidate lookup
- `apps/admin/lib/admin/actions.ts:73` · performance
- getCandidatesForRequest (line 73) and generateAiCandidates (line 165) each pull the ENTIRE expert_category table plus `expert.select('*')` for all active experts, on every admin matching-panel open with no pagination or column trimming. Fine at today's scale, but this is the hot path of the matching dashboard and grows linearly with expert count times categories; Supabase also silently caps responses at 1000 rows by default, so past that the matching algorithm would silently score against a truncated category map — a quiet correctness cliff, not just latency. Worth filtering expert_category by the active-expert id set (or a join) before that threshold is reached.

#### P3-36. generateMetadata fetches the title WITHOUT the status='open' filter the page body enforces — non-public request titles leak via the 404 page's <title>
- `apps/expert/app/(browse)/requests/[id]/page.tsx:17` · security / information disclosure
- The page body query (line 54-59) requires .eq('status', 'open') and notFound()s otherwise, but generateMetadata selects the title by id alone via adminClient (RLS bypassed). In Next.js, generateMetadata still resolves when the page throws notFound, so the browser-tab title of the resulting 404 page is `<real title> - 지사네` for closed/dealt requests. Notably, invitation-accepted deals auto-create requests titled '[초빙] {expertName} 시니어지식인 직접 의뢰' with status 'dealt' — never public — so a leaked title includes an expert's name for a private direct deal. Mitigated by unguessable UUIDs, but the two queries answer 'is this request visible?' differently; add the same status filter to generateMetadata.

#### P3-37. Server-action awaits have no try/finally — a rejected action leaves the button permanently disabled on '처리 중...'
- `apps/expert/app/(browse)/requests/[id]/interest-button.tsx:21` · resilience
- handleClick awaits withdrawInterest/expressInterest with no try/catch. If the action rejects (network failure, server exception before returning the {error} envelope), setIsLoading(false) at line 36 never runs, so the button is stuck disabled showing '처리 중...' until a full page reload. Wrap the awaits in try { … } finally { setIsLoading(false) } and surface a generic failure message in catch.

#### P3-38. Same missing try/finally as interest-button — rejected action leaves that row's button stuck on '...'
- `apps/expert/app/(browse)/requests/request-list.tsx:73` · resilience
- handleInterest is a near-verbatim copy of interest-button.tsx's toggle: on a rejected server-action promise, setLoading(null) at line 91 is skipped and the affected row's button stays disabled at '...' until reload. Fix with try/finally in both places (second repetition — per the 3-repetition rule no shared abstraction is required yet, but the same fix must land in both copies).

#### P3-39. est_hours submits via hidden input, so the visible number input's min/max HTML validation never applies
- `apps/expert/app/(main)/invitations/[id]/invitation-actions.tsx:64` · correctness / test-gap
- The visible <input type="number" min="1" max="200"> lives outside the accept <form>; the form submits a hidden mirror of the same state, so browser validation is bypassed. The client-side disabled check only enforces >= 1 (line 67), not the 200 max, so a user entering 999 gets an enabled button and only learns the limit from the server round-trip error ('예상 시간을 1~200 범위로 입력해주세요.' — server validation confirmed in lib/invitation/actions.ts lines 48-51, so no security hole). Also, decimals/exponent forms ('1.5', '1e3') pass the raw string to the server where parseInt truncates them — displayed cap and stored cap stay consistent only because both sides happen to use parseInt. Mirror max=200 in the disabled check or move the real input inside the form.

#### P3-40. interested Set seeded from props into useState and never re-synced — two sources of truth for interest state
- `apps/expert/app/(browse)/requests/request-list.tsx:46` · structure / state
- interestedIds arrives fresh from the server on every RSC render (including after the actions' revalidatePath('/requests')), but the client Set is initialized once and ignores all subsequent prop values while the component stays mounted (category/search navigation via router.push does not remount it). Today the drift is benign because the only in-mount mutations are the user's own toggles, but any server-side change during the mount (withdrawal in another tab, admin removal) renders a stale '관심 표현 완료'. If the server prop is meant to be authoritative, reconcile on prop change; otherwise document that client state intentionally wins within a mount.

#### P3-41. Silent fallback to hardcoded production URL when NEXT_PUBLIC_ADMIN_URL is unset
- `apps/expert/app/(browse)/education/education-view.tsx:134` · honesty / configuration
- If NEXT_PUBLIC_ADMIN_URL is missing at build time, the '자세히' link silently points to production https://jisane.cloud — in a preview/staging build this sends users cross-environment with no log or comment documenting the fallback (honest-implementation: deliberate fallbacks must be requested/visible/documented). Prefer failing the build (throw if unset) or documenting the fallback at the site.

#### P3-42. Hard limit(50) with no pagination or truncation indicator
- `apps/expert/app/(browse)/requests/page.tsx:51` · correctness / UX
- The list caps at 50 open requests; the 51st-oldest and beyond are invisible with no 'more' affordance, and the result summary ('N건의 열린 의뢰') presents the truncated count as the total. Fine at current scale, but the truncation is silent — add pagination or at least an indicator when the cap is hit.

#### P3-43. Interest state copied from prop and never re-synced; server revalidation targets the wrong path
- `apps/expert/app/(main)/matching/opportunity-section.tsx:22` · structure/state
- `interested` is a one-time useState copy of the server prop `interestedIds` — a second source of truth that never re-applies when a fresh prop arrives. Worse, the actions it calls (apps/expert/lib/interest/actions.ts) do `revalidatePath('/matching')` and `revalidatePath('/requests')`, but this component is actually mounted from `apps/expert/app/expert-dashboard.tsx` on the home route '/' (the /matching list page now just redirects to '/', per its own comment). So the route that renders `interestedIds` is never revalidated: after toggling interest, navigating away and back to '/' serves the stale cached RSC payload, and the button state reverts to the pre-toggle value while the DB says otherwise. Only visible to a human driving the app in that order — exactly the silent state-shape class.

#### P3-44. generateMetadata resolves owner company/CEO name via adminClient with no auth or ownership check
- `apps/expert/app/(main)/invitations/[id]/page.tsx:17` · security
- generateMetadata queries the invitation's owner (company, ceo_name) with the RLS-bypassing adminClient before any authentication/ownership check runs (those live only in the page body, lines 33-61). The owner's company name lands in the page <title> for any request bearing a valid invitation UUID; whether it leaks to a non-owner depends on Next's metadata handling around the page's notFound()/redirect. UUIDs make guessing impractical, so low risk, but the authorization boundary should sit in front of the adminClient read, not only in the page body.

#### P3-45. Ownership verified by a redundant second query after the full row was already fetched
- `apps/expert/app/(main)/invitations/[id]/page.tsx:55` · performance/security
- The invitation row (including owner PII: email, company) is fetched unscoped at lines 45-50, then a separate count query (lines 55-61) checks whether it belongs to the current expert. This is an extra DB round-trip and a fetch-before-authorize pattern; adding `.eq('expert_id', expert.id)` to the first query (as the matching detail page correctly does at matching/[id]/page.tsx:36) would be one query, and no other expert's row would ever be loaded. Correct today because notFound() fires before render, but fragile under future edits.

#### P3-46. Single shared `loading` id: a second concurrent click clears the first's completion, re-enabling an in-flight button
- `apps/expert/app/(main)/matching/opportunity-section.tsx:59` · concurrency
- `loading` holds only one request id. Click request A's button, then request B's while A is pending: `setLoading(requestId)` overwrites A→B, and when A's await resolves first, `setLoading(null)` clears B's loading state while B's server call is still in flight — B's button re-enables and can be clicked again mid-flight. Duplicate expressInterest is caught server-side by the 23505 unique violation and withdraw is idempotent, so impact is a confusing error message rather than data corruption.

#### P3-47. Hard limit(20) with no pagination or truncation indicator
- `apps/expert/app/(main)/invitations/page.tsx:41` · correctness/test-gap
- The invitations list caps at 20 rows with no 'more' affordance; an expert with more than 20 invitations silently cannot see the older ones, and nothing in the UI signals truncation.

#### P3-48. Stale loading skeleton: mirrors the removed /matching list page, but the segment now only serves the detail route
- `apps/expert/app/(main)/matching/loading.tsx:1` · dead-code/structure
- matching/page.tsx now immediately redirects to '/' ('대시보드가 홈(/)으로 승격됨'), so the only real page under this segment is /matching/[id]. This loading.tsx still draws the old list-page skeleton (header + 3 summary cards + matching list), which is what flashes while the detail page loads — a layout that matches nothing the user will see. Effectively dead layout left behind by the dashboard promotion.

#### P3-49. Missing deal_workflow row renders as toggleable 'pending'; toggling fakes success (0-row update)
- `apps/expert/app/(main)/work/[id]/workflow-form.tsx:55` · honesty / fake success
- The form iterates ORDERED_STEPS and treats an absent deal_workflow row as status 'pending' with an active '시작' button. If workflow rows were never seeded for a deal (steps prop empty — the parent page tolerates this: allStepsDone checks steps.length === 5), clicking runs the optimistic update and calls updateWorkflowStep, whose UPDATE ... eq(deal_id).eq(step) matches 0 rows, returns no error, and the action returns {} (success). The UI shows the step as started/completed, nothing is persisted, and a reload silently reverts everything. The dead API route has the same shape (see its line 63 guard). Either seed rows transactionally at deal creation and hard-fail here when a row is missing, or upsert.

#### P3-50. Route skips transition validation when the row is missing, fakes success on 0-row update, and validates check-then-act
- `apps/expert/app/api/deals/[id]/workflow/[step]/route.ts:63` · correctness / concurrency (in dead code)
- Three defects in the (currently unreachable) route: (1) if the deal_workflow row does not exist, the `if (workflow)` guard skips transition validation and the final UPDATE matches 0 rows yet returns { success: true }; (2) the transition check is SELECT-then-UPDATE without a compare-and-set (.eq('status', currentStatus) on the UPDATE), so two concurrent PATCHes can both pass the guard (TOCTOU); (3) `await request.json()` is unguarded (malformed body → unhandled 500) and `note` is written to the DB with no type/length validation. P3 only because the route is dead; these become P1/P2 if it is resurrected as the live path per the P1 finding.

#### P3-51. Error rollback restores status but not the optimistically-set done_at
- `apps/expert/app/(main)/work/[id]/workflow-form.tsx:43` · structure/state — partial rollback
- On failure the rollback maps status back to currentStatus but leaves done_at at the optimistic new Date().toISOString() written during the attempt. The form itself never renders done_at, so it is invisible today, but the local row now disagrees with the server and any future consumer of localSteps.done_at (e.g. reusing WorkflowChecklist here, which does show timestamps) inherits the stale value. Roll back the whole optimistic patch, not one field.

#### P3-52. DEAL_STATUS_COLORS carries a 'cancelled' key that the deal_status enum cannot produce
- `apps/expert/app/(main)/mypage/page.tsx:16` · dead-code
- deal_status is "quoted" | "working" | "done" (database.types.ts line 1196); the 'cancelled' entry can never match, and work/page.tsx's Record<DealStatus,...> maps confirm the closed set. Dead map entry that implies a status the system does not have — delete it or add the enum value if cancellation is planned.

#### P3-53. '교육·서비스 현황' section's 전체 보기 links to /matching, which is the matching page, not an orders view
- `apps/expert/app/(main)/mypage/page.tsx:261` · correctness — wrong link target
- Section B lists service_order rows, but its '전체 보기' link navigates to /matching (the matching-proposal page, whose own section C uses the identical link at line 300). The expert app has no orders list page in the route tree, so either the link is a copy-paste of section C's or the destination page is expected to show orders — either way the user asking for 'all my orders' lands on matching proposals. The empty-state link in the same section correctly goes to /education.

#### P3-54. Dispute target review picked with limit(1) and no ORDER BY — arbitrary when both owner and admin reviews exist
- `apps/expert/app/(main)/work/[id]/page.tsx:82` · correctness — nondeterministic row pick
- The query filters author_type in ('owner','admin') with .limit(1).single() and no ordering. If a deal has both an owner review and an admin review, which one becomes disputable is decided by the database's arbitrary row order, and the expert can never dispute the other. Order deterministically (e.g. owner first) or surface both.

#### P3-55. GET /api/expert/profile has no callers anywhere in the repo
- `apps/expert/app/api/expert/profile/route.ts:6` · dead-code
- A repo-wide grep for 'api/expert/profile' returns zero matches outside the route file itself. The mypage page fetches the same expert columns directly server-side (apps/expert/app/(main)/mypage/page.tsx line 42), so this route is an orphaned second answer to the same question (a third copy of the profile select shape, alongside the ExpertProfile interface in profile-editor.tsx). Delete it, or wire it to a real consumer.

#### P3-56. Raw DB error message returned to the client
- `apps/expert/app/api/disputes/route.ts:76` · security
- On insert failure the Postgres/Supabase error string (which can reveal table names, column names, or constraint names) is sent verbatim in the 500 response body. Log it server-side and return a generic Korean message like the other branches do.

#### P3-57. Malformed JSON body throws before validation — 500 instead of 400
- `apps/expert/app/api/disputes/route.ts:30` · correctness
- `req.json()` rejects on a non-JSON or empty body and nothing catches it, so a client sending a bad payload gets an unhandled 500 rather than the intended 400 validation response. Wrap in try/catch (or `.catch(() => null)`) and return 400.

#### P3-58. Route is a near-identical copy of apps/owner/app/api/disputes/route.ts with no pinning test
- `apps/expert/app/api/disputes/route.ts:11` · structure-state
- The owner app has the same dispute-creation route with the same duplicate-check at the same lines (55/67), differing only in the ownership chain and raised_by value. The shared skeleton (validation, dup-check, insert, error contract) is duplicated across apps with nothing pinning them together — a fix to the race or the error-leak in one app will silently miss the other. Extract the common flow into packages/shared or add a parity test.

#### P3-59. Unset hourly_rate silently becomes 25000 on any profile save
- `apps/expert/components/profile-editor.tsx:118` · honesty
- The field is labeled '(선택)' but when profile.hourly_rate is null the input pre-fills 25000, so saving the form for any other reason (e.g. adding a field chip) writes a rate the user never chose. Use placeholder/empty default and let the server keep null when blank.

#### P3-60. workingCount query waterfalls after the Promise.all instead of joining it
- `apps/expert/app/expert-dashboard.tsx:82` · performance
- The four dashboard queries run in parallel (line 44), but the deal count is awaited sequentially afterwards even though it depends on nothing from them — an avoidable extra round-trip on the logged-in home page. Additionally, the matching and service_order queries have no .limit() (openRequests is capped at 20), so a long-lived expert account renders an ever-growing full history on the dashboard.

#### P3-61. Landing stats and education packages fetched sequentially
- `apps/expert/app/page.tsx:38` · performance
- `fetchExpertLandingStats()` and `getPackagesByAudience('expert')` are independent but awaited one after the other on the logged-out landing path (which is dynamic due to cookies()), doubling the data-fetch latency. Wrap in Promise.all.

#### P3-62. Expert lookup error is swallowed — a transient DB failure misroutes an existing expert to onboarding
- `apps/expert/app/page.tsx:25` · honesty / correctness
- The `error` from `.single()` is discarded; any transient failure (network, RLS/config issue) makes `expert` null and the code falls through to redirect('/register') as if the logged-in user were a brand-new expert, instead of failing loudly. Distinguish 'no row' (PGRST116) from a real error before treating the user as unregistered.

#### P3-63. createEducationOrder has no idempotency — double submit creates duplicate paid orders
- `apps/expert/lib/education/actions.ts:48` · concurrency
- The service_order insert has no idempotency key, no UNIQUE constraint dedupe (unlike expert_interest's 23505 handling), and no existing-order check. A double-click / double-submit of the enrollment form (or a retry after a slow response) inserts two orders for the same expert+package, each carrying price — duplicate rows an admin can double-invoice. Add a UNIQUE-backed idempotency key (e.g. unique(expert_id, package_slug) among non-cancelled orders) or a client token.

#### P3-64. Reverting a step from 'done' leaves stale done_at
- `apps/expert/lib/workflow/actions.ts:58` · structure/state
- done_at is set when newStatus === 'done' but never cleared when a done step is moved back to 'pending'/'in_progress'. The row then reports status 'in_progress' with a completion timestamp — two answers to 'is this step done', and anything keying off done_at (audit, auto-release timing) reads the stale value. Set done_at: null on non-done statuses.

#### P3-65. service_order insert failure swallowed without logging
- `apps/expert/lib/education/actions.ts:61` · honesty
- The Supabase error is replaced by a generic Korean message and never logged (sibling actions log via console.error before returning). A recurring insert failure (missing column, FK violation after a schema change) would be operationally invisible. Also detail has no length cap before insert.

#### P3-66. NaN bypasses the hourly_rate range validation
- `apps/expert/lib/expert/actions.ts:37` · correctness
- parseInt of a non-numeric hourly_rate yields NaN; NaN < 10000 and NaN > 100000 are both false, so the range check passes, and JSON serialization sends null to the DB — the user's garbage input silently becomes 'no rate' (which later defaults to 25,000 KRW in acceptInvitation) instead of a validation error. Same for career_years (NaN → career_score 1.0, stored career_years null). Guard with Number.isNaN or a zod schema at the boundary.

#### P3-67. Non-numeric max_active_interests config silently disables the limit
- `apps/expert/lib/interest/actions.ts:40` · resilience
- If platform_config.value is a non-numeric string, Number() yields NaN and (activeCount ?? 0) >= NaN is always false — the cap silently fails open with no log. A misconfigured value should either fall back to the default loudly or be validated (Number.isFinite).

#### P3-68. sendMatchingInquiry has no content length cap (sendDealMessage caps at 1000)
- `apps/expert/lib/message/actions.ts:68` · correctness
- The sibling action in the same file enforces content.length > 1000, but the inquiry path validates only non-empty — an unbounded text insert into inquiry.content via the same trust boundary. Inconsistent boundary validation for the same kind of user input.

#### P3-69. getExpertId auth helper duplicated in 5+ action files
- `apps/expert/lib/interest/actions.ts:9` · structure
- Near-identical resolve-expert-from-auth helpers exist in interest/actions.ts, message/actions.ts (identical), matching/actions.ts (getExpertIdFromAuth), invitation/actions.ts (getExpertFromAuth), plus inline copies in education and expert actions — well past the 3rd repetition. A future change to the auth-to-expert resolution (e.g. suspended-expert check) must be applied in 6 places; one will be missed. Extract to a shared lib helper with a column-select parameter.

#### P3-70. Count copy claims total population but the query is capped at limit(50)
- `apps/owner/app/(browse)/experts/expert-list.tsx:58` · correctness
- The server query in experts/page.tsx has `.limit(50)` with no pagination, but the client renders `${experts.length}명의 활동 시니어지식인` / search-result counts as if it were the total. Once the platform has >50 active experts, the page permanently shows '50명' and silently hides the rest — no 'more' affordance, no pagination. Either paginate, fetch a real count ({ count: 'exact' }), or word the copy as a truncated list.

#### P3-71. generateMetadata skips the status check and duplicates the expert fetch
- `apps/owner/app/(browse)/experts/[id]/page.tsx:29` · security/performance
- generateMetadata fetches name/field with no `status !== 'active'` guard, while the page body 404s inactive experts. When notFound() is thrown by the page, the metadata already resolved from generateMetadata can still populate the document title — leaking the name/field of a suspended or deactivated expert on the 404 response. It is also a second, un-deduplicated adminClient query for the same row the page fetches (supabase-js calls are not deduped by Next the way fetch() is); wrapping the row fetch in React cache() and calling notFound() from generateMetadata on inactive would fix both.

#### P3-72. user.email! non-null assertion feeds a NOT NULL column — email-less OAuth users cannot sign up
- `apps/owner/app/(auth)/callback/route.ts:40` · correctness
- The owner.email column is `text not null` (0001_init.sql client.email, carried through rename). Some providers (notably Kakao without email-consent scope) return users with no email; `user.email!` only silences TypeScript, so the insert sends null, violates NOT NULL, and the user is dead-ended at /?error=profile_create with no way to proceed. Either guard explicitly with a clear error path or make the column/flow tolerate a missing email.

#### P3-73. GRADE_LABEL map duplicated in two files with no pinning test
- `apps/owner/app/(browse)/experts/expert-card.tsx:14` · structure/state
- The identical grade→label map exists in expert-card.tsx:14 and experts/[id]/page.tsx:15 (grep confirms exactly these two copies). packages/shared/lib/labels.ts already exists as the natural home. Two copies of the same domain shape with no test pinning them together will drift silently when a grade is added or a label reworded (already flagged in docs/10_UX_AUDIT_2026-08-10.md #33 but still unfixed in code).

#### P3-74. proxy.ts duplicated verbatim across apps with no shared source or pinning test
- `apps/expert/proxy.ts:5` · structure/state
- apps/expert/proxy.ts and apps/owner/proxy.ts are line-for-line identical (verified), including the COOKIE_DOMAIN fallback logic to '.jisane.cloud'. Session-refresh middleware is exactly the kind of file where a fix (cookie options, matcher, auth call) lands in one app and silently misses the others. Extract to packages/shared (a `createProxy()` factory) or add a parity test that fails when the copies diverge.

#### P3-75. All Supabase query errors on the experts page are discarded — failures render as empty states
- `apps/owner/app/(browse)/experts/page.tsx:77` · honesty
- Every query on this page destructures only { data } and ignores { error } (expert list line 77, expert_category line 22-25). Any DB failure (network, RLS, the 400s from the two findings above) renders the '현재 활동 시니어지식인이 없습니다' empty state — indistinguishable from a genuinely empty platform, invisible in logs. At minimum log the error server-side; ideally render a distinct error state per the no-fail-open rule.

#### P3-76. Two definitions of 'free': detail page uses price===0 while list card uses stored isFree flag
- `apps/owner/app/(browse)/services/[slug]/service-detail-view.tsx:38` · correctness / structure-state
- ServicePackage carries both `price` and a separately stored DB column `is_free` (service-catalog.ts:30, mapped from row.is_free in queries.ts:46 — not derived from price). The list card (services-view.tsx:41-48) shows the 무료 badge and '₩0' from pkg.isFree, while this detail page and its submit button (line 96) decide from pkg.price === 0. A row with is_free=true and price>0 (or the inverse) renders a card advertising ₩0 whose detail page charges `N원 신청하기` — a user-visible money contradiction with no constraint or test pinning the two columns together. Pick one source (derive isFree = price === 0, or make the UI read one field everywhere).

#### P3-77. 'education' category packages unreachable in browse mode though counted on provider cards
- `apps/owner/app/(browse)/services/services-view.tsx:8` · correctness
- ServicePackage['category'] is 'ax_consulting' | 'biz_consulting' | 'education' and CATEGORY_LABELS (line 13) maps all three, but CATEGORY_TABS offers only the first two. Browse mode filters `p.category === activeCategory`, so an owner-audience education package can never be displayed in the accordion — yet it is included in the provider card's `서비스 N개` count (ProviderInfo.packageCount counts all packages) and is findable via text search. Result: provider says N services, expanding it can show fewer with no tab that reveals the rest. Add an education tab or exclude the category from counts; there is no comment marking the omission intentional, and no test covers it.

#### P3-78. expandedProvider === null branch in filter is unreachable
- `apps/owner/app/(browse)/services/services-view.tsx:121` · dead-code
- `filtered` is only rendered inside `{expandedProvider && (...)}` (line 224), so when expandedProvider is null the list never mounts and the `expandedProvider === null ||` disjunct can never influence output. Either render an all-providers list when collapsed (making the branch live) or drop the condition to `p.providerId === expandedProvider`.

#### P3-79. Hardcoded production-URL fallback duplicated in two files
- `apps/owner/app/(browse)/services/[slug]/service-detail-view.tsx:13` · structure-state / duplication
- `process.env.NEXT_PUBLIC_ADMIN_URL || 'https://jisane.cloud'` appears here and again as ADMIN_URL in services-view.tsx:25 — the same fact defined twice with no shared constant, and a build missing the env var silently points staging/preview builds at the production admin site (preview↔prod divergence nobody sees). Hoist to one shared constant and consider failing loudly when the env var is unset in non-prod builds.

#### P3-80. Error rollback overwrites whatever the user typed while the send was in flight
- `apps/owner/app/(main)/status/[id]/message-thread.tsx:50` · correctness
- On send failure the handler restores the failed draft with `setContent(savedContent)`. If the user had already started typing a second message while the first was awaiting (`content` was cleared at line 44, so the input is editable), their in-progress draft is silently replaced by the failed one. Restore only when the input is still empty, or surface the failed text next to the error instead of overwriting the field.

#### P3-81. Checkout fetch has no timeout/abort — a hung server leaves the money button disabled forever
- `apps/owner/app/(main)/status/[id]/payment-button.tsx:50` · resilience
- The checkout POST has no `AbortSignal.timeout(...)`; browser fetch has no default timeout. If /api/payments/checkout hangs (upstream Toss call stalls server-side), `pending` stays true indefinitely, the button shows '결제창 여는 중...' with no way to retry short of a page reload, and no error is ever surfaced. On the payment-critical path, add an abort timeout that resets `pending` and reports via `onError`.

#### P3-82. owner.email selected but never used
- `apps/owner/app/(main)/status/page.tsx:52` · dead-code
- The owner lookup selects `id, email` but only `id` is read anywhere in the page; the detail page (status/[id]/page.tsx line 35) selects only `id`. Drop `email` from the select — it is an unused PII fetch.

#### P3-83. Optimistic send/rollback logic has no test
- `apps/owner/app/(main)/status/[id]/message-thread.tsx:31` · test-gaps
- The optimistic-update path (append temp row, clear input, on error remove temp row + restore draft + show error) is exactly the kind of branchy client state logic that regresses silently; none of the status/[id] client components have unit tests. At minimum, test handleSubmit's success and failure branches (temp row removed on error, error message set, content restored).

#### P3-84. Missing payment_key silently skips the Toss cancel yet still marks the settlement refunded
- `apps/owner/app/api/settlements/[id]/refund/route.ts:78` · honesty
- If settlement.payment_key is null (deposited via a legacy path, manual DB state, or the swallowed checkout update combined with any gap in confirm-deposit), the route skips the actual Toss cancel entirely but proceeds to set refunded_amt/escrow_status='refunded' and returns {success:true} — the DB claims money moved that never did, with no log line and no warning field in the response (unlike the ledger-failure branch at line 130 which is honest about partial completion). The comment '(payment_key가 있는 경우)' documents the branch but not the consequence. At minimum this branch should be loud: log + warning in the response, or refuse to mark refunded without a real cancel.

#### P3-85. Refund amount not validated as an integer number — floats and non-number types pass the guard
- `apps/owner/app/api/settlements/[id]/refund/route.ts:37` · correctness
- `!amount || amount <= 0` accepts amount=100.5 (Toss KRW cancels require integers → downstream 500 from Toss) and accepts any truthy non-numeric string (e.g. "abc": "abc" <= 0 is false), which then string-concatenates at line 68 (0 + "abc" → "0abc") and only fails at the DB/Toss layer with an opaque error. Money should be validated as a positive safe integer at the boundary (Number.isSafeInteger(amount) && amount > 0), matching what requests/route.ts already does for budget_hope.

#### P3-86. settlement.payment_key update result silently discarded
- `apps/owner/app/api/payments/checkout/route.ts:69` · honesty
- The update storing the pre-checkout paymentKey never checks its error or row count — a failure (or a missing settlement row, which matches 0 rows without error) is invisible. Impact is softened because confirmAndRecordDeposit re-writes payment_key at capture time, but a swallowed write on the payment path violates the no-swallowed-errors rule and would mask a missing-settlement precondition until the user has already authorized payment at Toss (confirm then 404s with the authorization dangling).

#### P3-87. budget_hope validated to 2^53 but the column is 32-bit int — values over 2,147,483,647 become a raw 500
- `apps/owner/app/api/requests/route.ts:74` · correctness
- Number.isSafeInteger allows up to 9,007,199,254,740,991, but 0001_init.sql defines budget_hope as `int`; any value above 2^31-1 fails at insert and the raw Postgres error message is returned to the client (line 89 `error.message`), also leaking DB internals. Cap the accepted range at the column's range (or a sane business max) at the boundary.

#### P3-88. req_type and scope inserted with no validation or length cap
- `apps/owner/app/api/requests/route.ts:83` · correctness
- title and detail get trim + length caps, but req_type and scope (free `text` columns) pass through verbatim with no whitelist, no trim, and no length bound — a client can store arbitrarily large strings (unbounded growth) or values no UI recognizes. Validate against the known set or at least cap length like the sibling fields.

#### P3-89. Raw Postgres error.message returned to the client
- `apps/owner/app/api/requests/route.ts:89` · security
- Both GET (line 32) and POST (line 89) return the Supabase/Postgres error message verbatim in the 500 body, disclosing schema/constraint internals (table, column, constraint names) to any authenticated owner. disputes/route.ts line 76 has the same pattern. Return a generic message and log the detail server-side.

#### P3-90. Unauthenticated dealId → request_id lookup oracle
- `apps/owner/app/api/payments/fail/route.ts:20` · security
- The fail redirect endpoint takes any dealId with no auth and no ownership check, resolves it via adminClient, and reflects the associated request_id in the redirect URL — anyone holding a deal UUID (e.g. leaked from a log or URL) can map it to its request UUID and confirm the deal's existence. Low impact since UUIDs are unguessable and only an ID is revealed, but the redirect target could instead be resolved after an ownership check on the /status page itself (which already authorizes).

#### P3-91. Dispute reason has no length cap — unbounded text stored per request
- `apps/owner/app/api/disputes/route.ts:33` · performance
- Unlike requests/route.ts (detail capped at 5,000 chars), the dispute reason is only checked for non-emptiness; a client can POST megabytes of text that is stored verbatim in dispute.reason and later rendered in the admin dashboard. Add a length cap consistent with the sibling boundary validation.

#### P3-92. settlement select('*') returns payment_key (PSP payment credential) to both owner and expert
- `apps/owner/app/api/settlements/[id]/route.ts:22` · security
- `select('*')` on settlement includes payment_key (defined in 0001_init.sql line 103), the Toss payment key used server-side for confirm/cancel API calls against the PSP. It is returned verbatim in the JSON response to whichever party passes the ownership check — including the expert, who is not the payer and has no use for it. A leaked payment_key plus the secret-key-holding server surface widens the blast radius of any later SSRF/endpoint bug (e.g. a refund endpoint keyed by payment_key). Select only the columns the UI needs instead of *.

#### P3-93. Raw Postgres error message returned to the UI; NaN/float rating slips past the app-level range check
- `apps/owner/lib/deal/actions.ts:83` · honesty
- `return { error: error.message }` surfaces the raw PostgREST/Postgres error string (English internals like 'invalid input syntax for type integer' or check-constraint names) directly into the Korean-language form UI. Related: the guard `rating < 1 || rating > 5` (line 59) does not reject NaN (both comparisons false for NaN) or non-integers, so a malformed rating reaches the DB and comes back as exactly such a raw error. Validate Number.isInteger(rating) && 1<=rating<=5 and map DB errors to a user-facing message.

#### P3-94. All adminClient query errors swallowed — DB outage is reported as 404/403
- `apps/owner/app/api/settlements/[id]/route.ts:29` · honesty
- Every query in the handler destructures only `data` and drops `error` (settlement fetch line 22, expert line 37, request line 48, owner line 55). A transient DB failure on the settlement fetch is indistinguishable from a genuinely missing row (404 'Settlement not found'), and a failure on the ownership lookups turns a legitimately-owned settlement into a 403. Fail-closed is the right direction for authz, but a 5xx with a logged error on unexpected DB errors would stop misreporting infrastructure failures as client errors.

#### P3-95. Raw DB error message returned to the client
- `apps/owner/lib/message/actions.ts:64` · security
- sendOwnerMessage (line 64) and sendDealInquiry (line 92) return error.message from Supabase verbatim to the browser, leaking internal schema/constraint details (table names, constraint names). Every other action in this group deliberately returns a generic Korean message instead ('저장에 실패했습니다...' etc.) — this file is the inconsistent one.

#### P3-96. sendDealInquiry has no content length cap
- `apps/owner/lib/message/actions.ts:77` · correctness
- sendOwnerMessage enforces content.length > 1000 rejection (line 50), but sendDealInquiry directly below validates only non-empty and inserts content of unbounded size into inquiry.content (text column, no DB limit in the checked-in schema) — inconsistent boundary validation on the same user-input surface.

#### P3-97. budget_hope accepts NaN and negative values without validation
- `apps/owner/lib/request/actions.ts:29` · correctness
- parseInt on arbitrary form input yields NaN for non-numeric strings; supabase-js JSON-serializes NaN to null, so garbage input is silently stored as 'no budget' rather than rejected, and negative numbers (e.g. '-500') pass straight through to the insert. A Number.isFinite + >= 0 check (or zod at the boundary per golden principle #6) is missing.

#### P3-98. confirmDealOp compensation write is fire-and-forget; its failure leaves deal='done' with settlement='deposited' and no log
- `packages/shared/lib/deal/deal-operations.ts:81` · concurrency & data integrity / honesty
- When the settlement→reviewing update fails, the code compensates by reverting deal to 'working' — but the compensation's own result is never destructured or checked. If the revert also fails (same outage that failed the settlement update is the likely cause), the system lands in deal='done' + settlement='deposited': the owner sees a completed deal whose settlement never enters reviewing and thus never auto-releases, and nothing is logged about the failed revert, so nobody knows the compensation didn't take. Check the revert's error and log it loudly; the documented two-step-with-compensation tradeoff (comment at line 80) only holds if the compensation is verified.

#### P3-99. batchExpireAndRecalc return value overcounts recalced when expert updates fail
- `packages/shared/lib/expert-activity.ts:111` · honesty / correctness
- The console.info on line 110 correctly reports recalced as affectedExpertIds.length - recalcFailed, but the return statement reports recalced: affectedExpertIds.length — the two disagree whenever recalcFailed > 0, and the caller receives a count that includes failed updates. Return the subtracted value.

#### P3-100. getCategoryBreadcrumb is dead code — exported with zero callers
- `packages/shared/lib/categories.ts:32` · dead-code
- Repo-wide grep finds no call site for getCategoryBreadcrumb (only its definition); matching-algo.ts imports only the CategoryRow type. Per the delete-dead-code rule, remove it (version control keeps it) — or if it is about to be used, note that it rebuilds a Map from all rows on every call, which would be O(n·m) if invoked per list row.

#### P3-101. Score/is_newbie computation duplicated between single and batch recalc paths with no pinning test
- `packages/shared/lib/expert-scoring.ts:131` · structure/state design
- recalcExpertScores (lines 65-73) and batchRecalcExpertScores (lines 127-131) each independently restate the same derivation: reviewScore, doneCount/totalCount filters, completionScore, and the isNewbie '< 3 reviews' threshold. With zero tests in the repo, nothing pins the two paths together — a threshold change in one silently diverges the other (auto-settlement prefers the batch path but falls back to the single path, so both run in production). Extract one computeScores(reviews, deals) used by both.

#### P3-102. verifyDealOwnership swallows DB errors — outage reads as '거래 정보를 찾을 수 없습니다'
- `packages/shared/lib/auth/server-helpers.ts:52` · honesty / resilience
- Only { data: deal } is destructured from the .single() call; the error is discarded. A transient DB failure is indistinguishable from a genuinely missing deal, so during an outage every owner action (approve, confirm, revision) returns 'deal not found' with nothing logged server-side. Distinguish PGRST116 (no rows) from other errors and log/surface the latter.

#### P3-103. No explicit timeout on any outbound Supabase call in the cron path
- `packages/shared/lib/automation/auto-settlement.ts:38` · resilience of outbound calls
- autoReleaseSettlements issues ~7+ sequential PostgREST calls (plus N RPCs and N expert updates) with no timeout; supabase-js's default fetch has no timeout, so a hung connection stalls the entire cron invocation instead of failing fast. Also the initial settlements query swallows its error the same way as the dispute query (null data → silent empty run reported as success). The proper fix is a fetch timeout at supabase client construction (packages/shared/lib/supabase/*, outside this group) — flagged here because the cron is the longest sequential chain depending on it.

#### P3-104. estAmount not rounded to integer won when hourlyRate × estHours is fractional
- `packages/shared/lib/cap-pricing.ts:19` · correctness / money
- calcMatchFee rounds its own result, but estAmount/capAmount/workFee/totalPay pass through unrounded. If estHours is fractional (e.g., 1.5) and hourlyRate is not a clean multiple, workFee and totalPay become non-integer won flowing into int DB columns (guarantee_fund_ledger.amount is int) — Postgres will reject or the value silently violates the money-as-integer rule. The current caller (apps/expert/lib/invitation/actions.ts) defaults rate to 25000, which masks it today. Apply Math.round at estAmount.

#### P3-105. null === null in category fallback grants spurious +5 to unrelated experts
- `packages/shared/lib/matching-algo.ts:116` · correctness
- In the fallback branch (reached when reqMidId is set but neither mid nor major matched), the check `ec?.parent_id === reqMajorId` is meant to award +5 when an expert's mid-category belongs to the request's major. If the request's category is depth-1 with a null parent_id, reqMajorId is null (line 68), and any expert registered with a top-level category (depth 0, parent_id null) then satisfies null === null and gets +5 despite zero relatedness. Guard with `if (reqMajorId && ec?.parent_id === reqMajorId)`. Only manifests if depth-1 rows can have null parents, hence P3.

#### P3-106. "This month" boundary computed in server-local timezone
- `packages/shared/lib/landing-stats.ts:47` · correctness
- firstDayOfMonth() builds midnight of the 1st in the server's local timezone and converts to ISO for a `created_at >= ...` filter. On a UTC server, the KST month boundary is off by 9 hours (requests created 00:00-09:00 KST on the 1st count into the previous month), and the value silently changes if the deploy region/TZ changes. deterministic-work.md: be explicit about the zone (compute the KST month start explicitly). Low stakes since it only feeds a landing-page counter.

#### P3-107. getProviderByAuthUser conflates DB failure with "no provider"
- `packages/shared/lib/provider/auth.ts:15` · honesty
- `.single()` returns {data: null, error} both when no row exists and when the query genuinely fails (outage, RLS/permission error, or — if auth_user_id is not unique — multiple rows). The error is discarded, so requireActiveProvider maps every failure mode to {ok:false, reason:'no_provider'}: an active partner hit by a transient DB error is silently shown the "you are not a provider" onboarding screen instead of an error. Fails closed (safe direction for authz), but it is a silent degradation with no log; use .maybeSingle() for the not-found case and surface/log real errors distinctly.

#### P3-108. Comment claims response-time calculation; code only counts message volume
- `packages/shared/lib/review-algo.ts:101` · honesty
- The inline comment says '평균 응답 시간 계산' (average response-time calculation) but the block computes nothing time-based — it thresholds total message count (>=10 → 5, >=5 → 4). The file header likewise promises '메시지 응답 속도' as an axis. The '(간이 추정)' marker discloses approximation, but the comment still names a computation that does not exist, and per honest-implementation.md a comment claiming behavior the code lacks is a defect. Reword the comment to '소통 빈도 기반 간이 추정' or implement the time delta.

#### P3-109. Catalog query failure silently degrades to empty catalog (fakes success)
- `packages/shared/lib/service-package/queries.ts:65` · honesty
- On any DB error getPackagesByAudience returns [] — the landing/services pages then render an empty catalog that is indistinguishable from 'no packages published'. It is logged (console.error) but the fallback is not documented as a deliberate graceful degradation at the site, and downstream (e.g. getProvidersByAudience) also silently shows nothing. A vendor/dependency failure degrading to a worse code path without surfacing violates the no-silent-fallback rule; consider throwing (page-level error boundary) or documenting the degradation intent in a comment at the site.

#### P3-110. getProvidersByAudience refetches the full package list — no request-scoped dedup
- `packages/shared/lib/service-package/queries.ts:86` · performance
- getProvidersByAudience calls getPackagesByAudience internally; a page that renders both the package grid and the provider summary issues two identical DB round-trips per request. Wrapping getPackagesByAudience in React cache() (or 'use cache') would dedupe within a render at no cost.

#### P3-111. Outbound Supabase calls have no explicit timeout
- `packages/shared/lib/service-package/queries.ts:56` · resilience
- All catalog queries go through adminClient with the supabase-js default fetch (no AbortSignal/timeout). If Supabase hangs rather than errors, the server render hangs with it instead of failing fast. Endemic pattern, but this shared query module is the right place to set a bounded fetch (custom fetch with AbortSignal.timeout) once.

#### P3-112. aria-controls points at a non-existent element while collapsed
- `packages/ui/components/collapsible-section.tsx:26` · correctness
- The content div (id={contentId}) is only mounted when open (line 54 `{open && ...}`), so while collapsed the button's aria-controls references an ID that is not in the DOM — technically invalid ARIA that some AT/validators flag. Either always render the container (hidden when closed) or set aria-controls conditionally.

#### P3-113. Production cookie domain hardcoded as fallback in shared code
- `packages/shared/lib/supabase/server.ts:27` · structure/state
- When COOKIE_DOMAIN is unset in production the code silently falls back to the literal '.jisane.cloud'. This is config baked into code (portability debt): a new environment/domain that forgets the env var gets auth cookies scoped to the wrong apex and a confusing login failure. Prefer requiring COOKIE_DOMAIN in production (throw) or documenting the fallback at the site. Not a secret, so P3.

#### P3-114. Destructured split drops title text when highlight occurs more than once
- `packages/ui/components/page-hero.tsx:36` · correctness
- `title.split(highlight)` returns N+1 parts for N occurrences, but only the first two are destructured — every part after the second is silently discarded from the rendered <h1>. Failure scenario: title="AI 도입, AI 파트너와 함께" with highlight="AI" splits into ['', ' 도입, ', ' 파트너와 함께'] and renders as "AI 도입, " — the trailing " 파트너와 함께" vanishes from the page's main heading with no error. Guard exists only for zero occurrences (the includes() check on line 35), not multiple. Fix: split on the first occurrence only, e.g. `const idx = title.indexOf(highlight)` then slice before/after.

#### P3-115. Workflow step order hardcoded, duplicating the generated enum with no pinning test
- `packages/ui/components/workflow-checklist.tsx:15` · structure/state
- The ordered step list restates the workflow_step enum whose canonical order already exists in packages/shared/lib/database.types.ts line 1390 (`workflow_step: ["intake", "structure", "generate", "verify", "deliver"]`, exposed via the generated Constants). Because the array is typed WorkflowStep[], REMOVING an enum value breaks compile, but ADDING one (e.g. a new 'review' step in a future migration) compiles cleanly and the checklist silently omits the new step — the deal appears complete while a step is untracked. Same class of drift for WORKFLOW_STEP_LABELS, which is typed `Record<string, string>` (labels.ts line 31) so a missing label also passes the compiler and renders undefined. Derive the list from the generated Constants (or add a pinning test asserting the two arrays are equal).

#### P3-116. Number() round-trip loses precision beyond 15 digits; no length cap on amount input
- `packages/ui/components/money-input.tsx:40` · correctness
- Formatting goes through `Number(digits)`, which is a float64 — pasting or typing 16+ digits (e.g. "99999999999999999") silently reformats to a DIFFERENT number (100,000,000,000,000,000) and that altered value is what the hidden input submits (raw is derived from display, so at least display and submitted value stay consistent). Unrealistic for legitimate KRW amounts but reachable by paste, and there is no maxLength/max guard, so garbage-in becomes a plausible-looking wrong amount rather than a validation error. Add a digit-length cap (KRW amounts fit comfortably in 12 digits) or format via string chunking instead of Number().

#### P3-117. router.replace when stripping toast param drops the URL hash
- `packages/ui/components/toast.tsx:48` · correctness
- Both toasts rebuild the URL as `url.pathname + url.search`, omitting `url.hash`. A redirect like `/status/123?success=payment#messages` loses the #messages fragment (and any scroll anchor) when the toast strips the query param. Same pattern in ErrorToast at line 124. Append `url.hash` to the replace target.

#### P3-118. Search value seeded once from defaultValue — stale if URL query changes without remount
- `packages/ui/components/search-box.tsx:26` · structure/state
- `useState(defaultValue)` captures the initial value only. If the surrounding page changes the q param through some other control (category link, clear-filters button) while SearchBox stays mounted, the box keeps showing the old query while the results reflect the new one — two answers to "what is the current search". The defaultValue naming makes the uncontrolled contract explicit, so this is on call sites: any page rendering SearchBox alongside other q-mutating controls must key it (e.g. `key={searchParams.get('q')}`) to force remount. Worth a code comment on the prop stating that requirement.

#### P3-119. No index on the new `service_order.service_package_id` FK column
- `supabase/migrations/0026_service_order_package_fk.sql:8` · performance
- Postgres does not auto-index FK referencing columns. Every DELETE on `service_package` (including cascades from a provider delete) must scan `service_order` to apply ON DELETE SET NULL, and any join/filter by package id does the same. Harmless at current row counts but a known FK-without-index trap; a simple `CREATE INDEX ON service_order(service_package_id)` closes it.

#### P3-120. New RLS policies have zero coverage in the RLS test suite
- `supabase/migrations/0025_service_package.sql:41` · test-gaps
- supabase/tests/0001_rls_test.sql contains no reference to `service_package` or `provider` (grep: no matches), so neither the public-select gate (only 'published' rows visible to anon) nor the owner policy's tenant scoping is pinned by a test. Given the policies are described as nominal, a test is the only thing that would catch the over-grant flagged in the P1 security finding or a future accidental widening.

## 처리 우선순위
1. **0절 — 누락 마이그레이션 커밋** (재현·배포 복구, 최우선)
2. **관리자 인가 3건**(#1 #2 #5) 3. **동시성 금전경로**(#3 #4 #9 #16 #17) 4. **복원력**(#15)·**노출**(#12) 5. P2 70건

*방법: 183파일 24그룹 → fable 리더 → 적대적 검증(9 기각) → 커버리지 크리틱(미독 0) → quote-back 212/212. 스키마 갭은 검수자 직접 재확인. 진단 전용, 코드 변경 없음. UX 축은 docs/10.*