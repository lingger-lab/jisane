# UX 전수 검수 2026-08-10 — scope=ux set=full model=fable
> 기준: `ui-ux-design` 8렌즈(states·hierarchy·feedback·consistency·a11y·copy·craft·effort) + `/audit` 스킬
> coverage: **160/160 파일**(16,445라인) 판독 · confirmed **P1 10 · P2 51 · P3 85** · refuted 6 · 미독 0
> quote-back: **146/146 OK, 허위 인용 0** (기계 검증)
> 시각 스윕: 3앱 공개 화면 10종 데스크톱+모바일(375px), 35샷 캡처·주요 21샷 판독 + 전 화면 기계검사(a11y트리·콘솔·룰체크)
> axes NOT run: 로그인 필요 화면 시각 검증(자격증명 없음 — 결제 버튼·대시보드·작업 화면은 코드 판독만)

## 시각 스윕 결과 (실제 렌더 확인)
- **V1 (P2, states/honesty):** DB 장애가 정상 화면으로 위장 — 3앱 랜딩 통계 "0 전문 분야·0건 열린 의뢰·0+ 서비스", 브라우즈는 "시니어지식인이 없습니다" 정상 빈 상태. 장애와 진짜 빈 상태 구분 불가.
- **V2 (P2, states):** expert `/education` 빈 상태 부재 — 히어로 아래 완전 공백. 형제 `/requests`엔 빈 상태 메시지 있음.
- **V3 (P2, effort):** admin 스플래시 = 매 방문 클릭 게이트(자동 소멸 없음, 주석에 "반복 노출·세션 제한 없음" 명시=의도). 매 방문 1클릭 강제 + LCP/SEO 저해 — 재검토 권고.
- **V4 (P3, a11y):** 터치 타겟 <24px 화면당 1~8개(WCAG 2.5.8) — 주로 푸터·헤더 보조 링크.
- **V5 (P3, copy):** admin `/service` 시니어지식인회원 카드 CTA "기업 매칭 신청하기" 의미 모호.
- **✅ 양호:** 모바일 375px 파손 0 · 전 화면 콘솔 에러 0(Next 16.3 하이드레이션 정상) · 빈 상태/검색/필터 렌더 양호.

## 검수 과정 절차 결함 (정직 기록)
- 포트 3000에 타 프로젝트(R&D Cost Planer)가 돌아 admin 1차 스윕이 남의 앱 캡처 → 폐기 후 3010 재스윕.
- 스윕 도구 뷰포트 유지 특성으로 admin 2차가 모바일 폭 캡처 → 뷰포트 명시 후 3차 재캡처.

## 1. P1 — 10건 (전건 quote-back OK + 적대적 검증 통과)

### P1-1. Assign button has no in-flight state — double-click can create duplicate matching
- `apps/admin/app/(protected)/dashboard/matching-tab.tsx:266` · feedback · quote-back OK · ☐ untaken
- 인용: `onClick={() => handleAssign(req.id, c.expert_id)}`
- **문제:** handleAssign awaits createMatching/selectCandidate but the button is never disabled and shows no busy label during the request. Two rapid clicks both pass the server's status==='open' check (both read before either writes), so a duplicate matching can be created; meanwhile the admin gets zero feedback that anything is happening. Every other mutating button in this dashboard (dispute resolve, inquiry close, partner approve) disables itself while busy — this one, the highest-stakes action in the tab, does not.
- **검증:** Quote present at matching-tab.tsx:266. handleAssign (105-115) sets no in-flight state; the assign button (264-270) has no disabled attr, unlike generating/resolving/closingId/busy patterns used by every other mutating button in the tab family. Server race is real: createMatching (actions.ts:352-380) and selectCandidate (212-243) both read request.status then insert; grep of all supabase/migrations shows no UNIQUE on matching(request_id) — only idx_matching_request (0001_init.sql:165) — so two rapid clicks can insert duplicate matching rows.

### P1-2. Admin service-orders API accepts ANY logged-in user as admin
- `apps/admin/app/api/admin/service-orders/route.ts:10` · consistency · quote-back OK · ☐ untaken
- 인용: `  return !!user`
- **문제:** isAdmin() only checks that a Supabase session exists (return !!user) — it never checks an admin role, allowlisted email, or the x-admin-secret used by every sibling admin route (deals/matching/requests/settlements/summary use ADMIN_SECRET; expert-activity uses verifyAdmin()). Any user who can complete the OAuth login on this app can list every customer's service orders (GET returns select('*')) and flip order statuses via PATCH (e.g. cancelled→paid — PATCH also has no transition validation). Three different auth patterns across one admin API surface is exactly the consistency gap that let the weakest one ship.
- **검증:** isAdmin() at service-orders/route.ts:10 is literally 'return !!user' — session existence only. No compensating control: apps/admin/proxy.ts only refreshes the Supabase session (no authz, matcher covers all routes), and every sibling admin route uses x-admin-secret (deals/matching/requests/settlements/summary/guarantee-fund) or verifyAdmin() with an ADMIN_EMAILS allowlist (server-helpers.ts:34-37). proxy.ts sets cookie domain '.jisane.cloud' in production, so a session obtained on the owner/expert apps is valid here. Any registered user can GET select('*') on service_order and PATCH any status. P1 (P0 would require no account at all; an account is a low bar, so this is borderline-P0).

### P1-3. Quote ownership check skipped for deals without request_id
- `apps/admin/app/docs/quote/[dealId]/page.tsx:40` · security · quote-back OK · ☐ untaken
- 인용: `    if (deal.request_id) {`
- **문제:** For a non-admin user who has any owner record, the request-ownership verification only runs when deal.request_id is set. Invitation-based deals (the capInfo path proves they exist) can have a null request_id, so ANY logged-in owner who obtains such a dealId sees another party's full quote: request title/detail, expert name, work_fee, match_fee, cap pricing. The sibling statement page (line 40) correctly redirects when request_id is null; the quote page does the opposite. Not P0 only because dealIds are UUIDs and not enumerable.
- **검증:** quote/[dealId]/page.tsx:40 — for a non-admin owner the request-ownership check runs only when deal.request_id is set; null falls through fail-open and renders the full quote. Sibling statement page line 40 fails closed for the identical case, proving intent. deal.request_id is nullable (0001_init.sql:86, database.types.ts:92) with no DB constraint. Mitigating: both current insert paths always set request_id (invitation/actions.ts:140, matching/actions.ts:90) and the invitation flow's own comment admits null-request_id 'ghost deals' were a real historical state — so legacy rows may exist and nothing in the DB prevents new ones. Fail-open access control over another party's pricing/fee data; P1 stands (not P0: UUIDs non-enumerable, vulnerable row state not produced by current writers).

### P1-4. Decline error swallowed — button stuck on '거절 중...' forever with no message
- `apps/expert/app/(main)/invitations/[id]/invitation-actions.tsx:22` · feedback · quote-back OK · ☐ untaken
- 인용: `await declineInvitation(invitationId)`
- **문제:** handleDecline sets declining=true then awaits declineInvitation but ignores its return value. The action returns { error } on real paths ('접근 권한이 없습니다', '이미 처리된 초빙입니다' — e.g. the invitation was withdrawn/processed concurrently in another tab or by the owner). On that path nothing redirects, the error is never shown, declining is never reset, and the user is left with a permanently disabled '거절 중...' button and no explanation. Contrast: the accept path correctly surfaces acceptState.error via useActionState. (Related: on success the action redirects to /invitations?success=invitation_declined, but invitations/page.tsx never reads searchParams, so the success signal is silently dropped too.)
- **검증:** invitation-actions.tsx:20-23 — handleDecline sets declining=true then `await declineInvitation(invitationId)` with the return value ignored. lib/invitation/actions.ts:209-215 shows declineInvitation really returns { error: '접근 권한이 없습니다' } and { error: '이미 처리된 초빙입니다' } on non-redirect paths; the component has no error state for decline, never resets declining, so the button stays disabled at '거절 중...' (line 96) forever with no message. Also verified: success redirect targets /invitations?success=invitation_declined (actions.ts:222) but invitations/page.tsx:21 takes no props and never reads searchParams. Contrast with accept path which surfaces acceptState.error (lines 72-74). Real swallowed-error defect, though only reachable on concurrent/stale-state races — P1 stands.

### P1-5. Newbie review gate and status-transition rules enforced only in an unreferenced API route — live UI path bypasses both
- `apps/expert/app/api/deals/[id]/workflow/[step]/route.ts:79` · states/feedback (enforcement lives only in dead code) · quote-back OK · ☐ untaken
- 인용: `if (step === 'verify' && newStatus === 'done') {`
- **문제:** The workflow UI (workflow-form.tsx:38) calls the server action updateWorkflowStep in apps/expert/lib/workflow/actions.ts, which performs NO transition validation and NO newbie gate — it writes any status directly. This PATCH route contains the pending->in_progress->done transition guard (line 65) and the '신규자 보증 게이트' (line 79, sets queue_status pending_review and blocks verify completion for is_newbie experts), but a repo-wide grep shows nothing calls /api/deals/:id/workflow/:step (only docs/04_TRD.md references it). Consequence: a newbie expert marks 'verify' done with zero admin review — the documented quality gate for new experts simply never fires — and since server actions are network-callable endpoints, a client can also replay updateWorkflowStep with arbitrary statuses (e.g. done->pending) that the route would reject. Duplicated logic has already drifted (coding-style.md: mirror without pinning test).
- **검증:** Quote matches route.ts:79. workflow-form.tsx:4/38 calls updateWorkflowStep (apps/expert/lib/workflow/actions.ts:42-74), which validates only auth+ownership and writes any status/step directly — no transition table, no newbie gate. Repo grep for the API path finds zero callers ('workflow/' matches only the actions import), so the pending→in_progress→done guard (route.ts:65-70) and the is_newbie pending_review gate (route.ts:79-98) are dead code. A newbie expert completing 'verify' via the live UI never gets queue_status='pending_review', and the server action is a network-callable endpoint accepting arbitrary statuses (e.g. done→pending). Real enforcement bypass, duplicated-and-drifted logic.

### P1-6. Viewport blocks pinch-zoom on a seniors-targeted app (WCAG 1.4.4)
- `apps/expert/app/layout.tsx:35` · a11y · quote-back OK · ☐ untaken
- 인용: `maximumScale: 1,`
- **문제:** maximumScale: 1 prevents users from zooming the page on browsers that honor it. This product's primary audience is explicitly senior experts, for whom text zoom is a core need; capping scale at 1 is a WCAG 2.1 AA 1.4.4 (Resize Text) failure on the exact user group most likely to rely on it. Remove maximumScale (initialScale alone is enough to prevent the iOS input-zoom jump if inputs are >=16px).
- **검증:** apps/expert/app/layout.tsx:35 contains exactly `maximumScale: 1,` with no comment justifying it. Android Chrome honors maximum-scale=1 and blocks pinch-zoom (iOS Safari ignores it since iOS 10, so impact is Android-only, but that is still a live WCAG 2.1 AA 1.4.4 failure). Product copy throughout targets senior experts (시니어지식인), the group most dependent on zoom. axe-core grades meta-viewport maximum-scale<2 as critical. P1 stands.

### P1-7. Expert search silently returns 0 results for terms containing parentheses (e.g. "(주)")
- `apps/owner/app/(browse)/experts/page.tsx:73` · states/effort · quote-back OK · ☐ untaken
- 인용: `const safe = query.replace(/[%,]/g, '')`
- **문제:** The search term is interpolated into a PostgREST .or() filter string with only % and , stripped. Parentheses are NOT stripped or quoted, and they are structural in PostgREST's or= logic-tree syntax. A user searching "(주)지사네" — the standard Korean corporate prefix — produces `name.ilike.%(주)지사네%` whose `)` terminates the or-group early, the query 400s, the error is discarded (line 77 destructures only data), and the page renders as if zero experts matched. A very plausible input class silently fails with a fake empty state and no error feedback.
- **검증:** Quote exact at page.tsx:73; only % and , stripped before interpolation into .or() at line 74. PostgREST logic-tree syntax requires double-quoting values containing parentheses, so '(주)' input yields an unbalanced ')' that 400s the query; line 77 destructures only data, so the error is discarded and line 115 renders (experts ?? []) — a silent fake empty state. No sanitization or error handling elsewhere. Kept P1: functional break with fully silent failure on a plausible Korean input class.

### P1-8. API leaks work_fee/match_fee breakdown the UI's anonymization design deliberately hides
- `apps/owner/app/api/requests/[id]/route.ts:45` · consistency · quote-back OK · ☐ untaken
- 인용: `    .select('id, status, work_fee, match_fee, total_pay, scope, due_date, created_at')`
- **문제:** quote-section.tsx documents a product invariant: exposing the internal fee split (work_fee/match_fee) to the owner 'breaks the anonymization (direct-deal prevention) design', and the status page carefully passes only total_pay to the client. But GET /api/requests/[id] — reachable by the same authenticated owner — returns every deal's work_fee and match_fee in plain JSON. Any owner can open devtools or curl the endpoint and see exactly what the expert is paid vs. the platform's cut, defeating the documented 직거래 방지 design. The select list should drop work_fee/match_fee (mirror the page's field whitelist).
- **검증:** route.ts:45 select includes work_fee/match_fee and line 48 returns them raw. quote-section.tsx:10-13 and page.tsx:182-184 document the anonymization/direct-deal-prevention invariant and whitelist only total_pay. No in-repo caller of the endpoint exists (grep: zero hits outside docs), but it is deployed and reachable by any authenticated owner for their own requests — the motivated owner probing the API is exactly the adversary the documented design guards against, and the data is served today. P1 stands as a live info-disclosure defeating a documented product invariant.

### P1-9. Logged-out invite CTA links to a route that does not exist — 404
- `apps/owner/components/invite-button.tsx:18` · effort/states · quote-back OK · ☐ untaken
- 인용: `        href="/api/auth/login"`
- **문제:** The logged-out variant of the expert-detail primary action links to /api/auth/login, but the owner app has no such route: no app/api/auth/** handler exists (Glob returned nothing), there is no middleware.ts, and next.config.ts has no rewrites. Every logged-out visitor who clicks '로그인 후 초빙하기' lands on a 404 instead of a login flow — the invite conversion path is broken right now. The rest of the app initiates login via the signInWithKakao/signInWithGoogle server actions (AppHeader LoginDropdown, home CTA forms); this button should do the same or open the login dropdown.
- **검증:** invite-button.tsx:18 links to /api/auth/login; no app/api/auth route exists anywhere in the monorepo (glob empty), no middleware.ts in apps/owner, and next.config.ts has no rewrites — the only repo-wide occurrence of 'auth/login' is this link, so it 404s. Downgraded from P0 to P1: the same page's AppHeader LoginDropdown (layout.tsx:62-70) provides a working login path, so the flow is degraded (dead-end CTA), not fully blocked.

### P1-10. maximumScale: 1 blocks pinch-zoom (WCAG 1.4.4 failure)
- `apps/owner/app/layout.tsx:35` · a11y/WCAG AA · quote-back OK · ☐ untaken
- 인용: `  maximumScale: 1,`
- **문제:** The viewport export sets maximumScale: 1. iOS Safari ignores it, but Chrome on Android honors it and disables pinch-zoom entirely, failing WCAG 2.1 AA 1.4.4 (Resize Text, requires up to 200% zoom). For a platform whose brand is senior expertise, low-vision users being unable to zoom is a real exclusion. Remove maximumScale (initialScale: 1 alone is sufficient to prevent the input-focus auto-zoom this is usually added for, given inputs use >=16px text).
- **검증:** layout.tsx:35 has 'maximumScale: 1' verbatim in the viewport export, with no justifying comment. Chrome on Android honors it and disables pinch-zoom, a WCAG 2.1 AA 1.4.4 failure — material for a senior-targeted platform. P1 stands (P2 defensible).

## 2. P2 — 51건

#### P2-1. Dispute resolve swallows the server action's error — silent failure
- `apps/admin/app/(protected)/dashboard/dispute-tab.tsx:58` · feedback
- resolveDispute returns Promise<{error?: string}> (verified in lib/admin/actions.ts:706 — e.g. '이미 해결된 이의제기입니다.', or the raw DB error), but handleResolve ignores the return value entirely. On failure the button flips from '처리 중...' back to '해결' with no message, no state change — the admin cannot tell whether the dispute was resolved. Disputes are the 'urgent' banner category, so a silently failed resolve leaves a live escalation looking handled.

#### P2-2. Inquiry close failure produces no visible error
- `apps/admin/app/(protected)/dashboard/inquiry-tab.tsx:39` · feedback
- handleClose checks result.error but the failure branch does nothing: no error message state exists in this component, so on a failed closeInquiry (action returns {error: string}, verified at lib/admin/actions.ts:752) the '종료' button just reverts from '...' with no explanation. The admin's only clue is that the status badge did not change — indistinguishable from a slow revalidation.

#### P2-3. One-click irreversible dispute resolution — no confirm, no resolution note
- `apps/admin/app/(protected)/dashboard/dispute-tab.tsx:87` · feedback
- '해결' immediately marks the dispute resolved with no confirmation dialog and no way to record how it was resolved. This is inconsistent with partner-tab.tsx, where every status change (approve/reject/suspend, even the reversible ones) goes through confirm(). A dispute — the one entity the UI itself flags as '즉시 처리' urgent — is the action most deserving of a destructive-confirm guard, and there is no un-resolve path in the actions file.

#### P2-4. Tab bar has no tablist semantics — active tab invisible to assistive tech
- `apps/admin/app/(protected)/dashboard/dashboard-tabs.tsx:102` · a11y
- The 9 tabs are plain buttons with no role="tablist"/"tab", no aria-selected, and no aria-controls; the active tab is conveyed only by background color. Screen-reader users cannot tell which tab is selected or that these buttons switch panels, and there is no arrow-key navigation. The same unlabeled-toggle pattern repeats in the filter pills of dispute-tab.tsx:27 and invitation-tab.tsx:52 (no aria-pressed).

#### P2-5. Enter key bypasses in-flight guard, allowing duplicate admin message sends
- `apps/admin/app/(protected)/dashboard/progress-tab.tsx:223` · feedback
- The send button is disabled while sending (disabled={sending || !replyText.trim()}), but the input's onKeyDown path calls handleSendReply directly, and handleSendReply only guards empty text (line 75: if (!replyText.trim()) return) — not the sending flag. replyText is only cleared after the server round-trip succeeds, so pressing Enter twice while a send is in flight fires sendAdminMessage twice and posts the same admin message to the deal thread twice.

#### P2-6. Message-load failure is rendered as the empty state, contradicting the unread badge
- `apps/admin/app/(protected)/dashboard/progress-tab.tsx:70` · states
- getMessagesForDeal (apps/admin/lib/admin/actions.ts:740) returns { messages: data || [] } — a DB error yields an empty array with no error field, and the tab has no error branch for the thread load: a failed fetch renders '아직 메시지가 없습니다.' while the toggle button beside it still shows msgCount > 0. The admin sees a visible contradiction (badge says 3, thread says none) and may reply blind to messages that actually exist. Loading/empty/data are handled; the error state is not.

#### P2-7. AI generation triggers full page reload, silently discarding typed review text
- `apps/admin/app/(protected)/review-input/[id]/review-form.tsx:95` · feedback
- The 공개 의견 and 내부 비고 textareas are uncontrolled form fields. An admin who has typed comments and then clicks 'AI 평가 생성' gets window.location.reload() on success, which wipes both textareas and all selected star ratings with no dirty-guard or warning. The action already calls revalidatePath('/review-input/[dealId]') (actions.ts:686), so a router refresh would surface the suggestion without destroying form state; the hard reload turns a helper button into a data-loss trap.

#### P2-8. Matching creation is non-atomic; second write's failure is silently swallowed
- `apps/admin/app/api/admin/matching/route.ts:48` · feedback
- After inserting the matching row, the request.status→'matching' update is awaited without checking its error. If it fails, the API still returns { success: true }: the admin sees success feedback, the request stays 'open', and it can be matched again (the status==='open' precheck is also racy — two concurrent proposals both pass it, and there is no unique constraint fallback). Product effect: duplicate proposals to experts and an admin dashboard that contradicts the DB, with no visible error anywhere.

#### P2-9. Query failure renders guarantee-fund balance as ₩0 instead of an error state
- `apps/admin/app/api/admin/guarantee-fund/route.ts:16` · states
- Neither accrueRes.error, payoutRes.error, nor the entries query error is checked; on any DB failure the route returns { balance: 0, entries: [] } with HTTP 200. The admin dashboard cannot distinguish 'fund is empty' from 'query failed' — the error state is presented as data, for the number this business publicly promises to disclose quarterly. Fail loud (500) instead of degrading to a fake zero.

#### P2-10. Dashboard summary shows all-zeros on DB failure, indistinguishable from a quiet day
- `apps/admin/app/api/admin/summary/route.ts:22` · states
- None of the five Promise.all results are error-checked; count||0 and amount||0 convert every failure into 0. An admin opening the dashboard during an outage sees '매칭 대기 0 · 진행 0 · 정산 대기 0' — a plausible, reassuring lie. The empty state and the error state are the same pixels; the API must return an error so the UI can render the error card (dashboard has error.tsx) instead of fake-empty data.

#### P2-11. Fee transparency promise ('7구간 단가 사전 공개') has no published fee table anywhere
- `apps/admin/app/(public)/standard/page.tsx:16` · effort
- Principle 1 claims the 매칭비 7-tier price list is disclosed up front ('사전 공개'), but its href is '' — of the five principles it is the only money-related one with no supporting artifact, and no public page in the app publishes the tiers (scope page likewise says '7단계 등급표 기준 · 사전 공개' with nothing to click). The core promise of the brand ('감추지 않는 것이 표준') is asserted but not verifiable by the user: recognition is impossible, and the claim reads as marketing rather than the structural proof the surrounding pages sell. Publish the tier table and link it here and from the scope form.

#### P2-12. Privacy policy names no actual privacy officer — the '담당자' field is a self-referential placeholder
- `apps/admin/app/(public)/privacy/page.tsx:167` · copy
- Section 9 lists '담당자: 개인정보 보호책임자' — the officer's name is literally the job title. PIPA requires designating an identifiable 개인정보 보호책임자 (name or at least department + contact); a placeholder that shipped undermines the policy's validity and the trust positioning of the whole site. Fill in the real name/department.

#### P2-13. Order status PATCH allows any→any transition (cancelled→paid, completed→pending)
- `apps/admin/app/api/admin/service-orders/route.ts:51` · feedback
- The PATCH validates the status is in the enum but not that the transition is legal from the current status — an operator mis-click (or, combined with the weak isAdmin, any logged-in user) can move a cancelled order back to paid or a completed order to pending with no confirm step and no audit trail. Destructive/irreversible state changes need a transition table server-side, and the UI needs a destructive-confirm for cancellation.

#### P2-14. Viewport maximumScale:1 blocks pinch-zoom (WCAG 1.4.4)
- `apps/admin/app/layout.tsx:34` · a11y
- maximumScale: 1 disables pinch-to-zoom on Android Chrome (iOS Safari ignores it). This app explicitly targets senior users (시니어지식인) — the audience most dependent on zoom. WCAG 2.1 AA 1.4.4 requires text scalable to 200%. Remove maximumScale or set it to 5.

#### P2-15. Partner dashboard tabs have no current-tab indication
- `apps/admin/app/partner/dashboard/layout.tsx:47` · effort
- All four nav tabs (개요/전문가회원 정보/서비스 관리/신청 확인) render with the identical muted style — no active background, no color change, no aria-current='page'. The user cannot tell which section they are on (recognition over recall fails), and screen-reader users get no location cue. The server layout has no pathname; either read the pathname in a small client component or use per-segment styling so the active tab is visually and semantically distinct.

#### P2-16. Server-side error wipes every field of the partner apply form
- `apps/admin/app/partner/apply/apply-form.tsx:14` · feedback
- React 19 auto-resets uncontrolled form fields whenever a function passed to <form action> completes — including when applyAsPartner returns { error }. The form only renders state.error and never restores typed values (no defaultValue fed back from the action state), so a user who filled name, 유형, 분야, 연락처, website, and the 소개 textarea loses everything on any server rejection and must retype it all. Echo submitted values back in the action state and set them as defaultValue, or control the fields.

#### P2-17. Splash overlay does not isolate background content from keyboard/screen-reader users
- `apps/admin/components/splash-overlay.tsx:20` · a11y
- The full-screen overlay is a fixed div painted over the page with no focus management: focus is not moved to the '시작하기' button on mount, there is no focus trap, and the content underneath is not marked inert/aria-hidden. A keyboard user's first Tab lands on invisible nav links behind the opaque overlay, and screen-reader users hear the whole page while sighted users see only the splash. The comment documents the repeat-display policy as intentional, but not this interaction gap.

#### P2-18. Education list has no empty state for zero providers or a filter with no matches
- `apps/expert/app/(browse)/education/education-view.tsx:92` · states
- If getProvidersByAudience returns an empty array the page renders the hero and then nothing (no '아직 등록된 교육이 없습니다' message), and when a provider is expanded but the 무료/유료 filter matches zero packages the list area is silently blank — filtered.map over an empty array renders nothing and no '결과 없음' fallback exists. Both blank screens read as broken rather than empty.

#### P2-19. Login CTA on request detail sends the user to home with no way back to the request
- `apps/expert/app/(browse)/requests/[id]/page.tsx:157` · effort
- An anonymous visitor reading a request must click '로그인 후 관심 표현' which links to '/', log in there, and then manually re-find the request — no returnTo/next param is carried, and the OAuth callback always redirects to '/' or '/register'. For the core conversion path (see request → express interest) this drops the user's context at the exact moment of intent.

#### P2-20. generateMetadata fetches owner company for ANY invitation id — no auth or ownership check
- `apps/expert/app/(main)/invitations/[id]/page.tsx:17` · a11y/security
- The page body enforces login + ownership (redirect at line 35, ownership count check at lines 55-61 → notFound), but generateMetadata queries the invitation's owner company/ceo_name with adminClient for any supplied UUID before any of those checks run, and puts it in the document title. The ownership gate should be applied (or the metadata derived from the already-authorized fetch) so a non-owner request can never resolve another company's identity into the title.

#### P2-21. Silent default hourly rate (25,000원) used to compute a financial commitment
- `apps/expert/app/(main)/invitations/[id]/page.tsx:63` · feedback
- If the expert never set hourly_rate, the accept UI silently computes and displays the cap amount from a 25,000원 default with no disclosure that this is a placeholder rate ('기본 단가가 적용됩니다' or a prompt to set the rate first). The expert commits to a contract cap derived from a number they never chose; there is no code comment marking this as an intentional product default either. The server action recomputes from getExpertFromAuth's hourlyRate, so any drift between the two defaults would make the displayed cap differ from the stored one.

#### P2-22. Native confirm() for irreversible reject — inconsistent with the app's inline confirm pattern
- `apps/expert/app/(main)/matching/[id]/matching-actions.tsx:26` · consistency
- The same product surface uses a styled inline confirmation for declining an invitation (invitation-actions.tsx lines 87-106, error-colored panel with 거절 확인/취소), but rejecting a matching uses the browser-native confirm() dialog — unstyled, English buttons on most locales, and not themeable. After confirming, rejectMatching redirects to /matching which itself redirects to / (matching/page.tsx), so the user lands on the home dashboard with no success feedback for an action the dialog itself calls irreversible.

#### P2-23. Login/register CTA drops the user on the home page with no return path
- `apps/expert/app/(browse)/requests/request-list.tsx:243` · effort
- For logged-out or non-expert visitors, the per-request CTA '로그인 후 관심 표현' / '시니어지식인 등록 후 관심 표현' is a plain Link to '/'. The user must find the login entry on the home page, authenticate, then manually navigate back and re-find the specific request they wanted (recall, not recognition). No returnTo/next param preserves the request they were acting on, and the register variant should link to /register, not home.

#### P2-24. Category filter tabs expose selected state by background color only — no aria-pressed
- `apps/expert/app/(browse)/requests/request-list.tsx:137` · a11y
- The 전체/major tabs and mid-category chips are plain buttons whose active state is conveyed solely by background/text color classes (bg-accent vs bg-surface). There is no aria-pressed/aria-current, so screen-reader users cannot tell which filter is active (WCAG 4.1.2 name/role/value), and the distinction is color-only for sighted users as well (WCAG 1.4.1).

#### P2-25. Action errors rendered without aria-live and far from the failing control
- `apps/expert/app/(browse)/requests/request-list.tsx:171` · a11y
- The single shared error paragraph appears above the list (not next to the card whose 관심 표현 button failed) and has no role="alert"/aria-live, so screen readers never announce it; with a long list the sighted user may not see it either since the failed button is below the fold while the error renders at the top. The same silent-error pattern recurs in opportunity-section.tsx line 64 and matching-actions.tsx line 47.

#### P2-26. Invitations hard-capped at 20 with no pagination — older invitations become unreachable
- `apps/expert/app/(main)/invitations/page.tsx:41` · states
- The list is the only navigation path to /invitations/[id], and it fetches limit(20) ordered by created_at desc with no pagination, 'more' link, or truncation notice. An expert with more than 20 invitations silently loses access to the older ones — including possibly still-pending ('invited') ones pushed out by newer traffic.

#### P2-27. Owner-sent messages are labeled '나' (me) in the expert's message thread
- `apps/expert/app/(main)/work/[id]/message-thread.tsx:77` · consistency/feedback (wrong author label)
- deal_message.sender_type enum is owner|expert|admin (packages/shared/lib/database.types.ts:1213) and the owner app writes sender_type:'owner' into the same deal's thread (apps/owner/lib/message/actions.ts:59). The expert view uses a binary label — admin gets '매니저', everything else gets '나' — so an owner's message renders left-aligned (correctly, since alignment checks sender_type==='expert') but captioned '나', telling the expert they wrote the client's message. The owner-side thread uses a proper SENDER_LABELS map with fallback; this side hand-rolls the binary and misattributes a whole sender class. Label-map lens: enums must go through a shared label map with safe fallback.

#### P2-28. Query errors on mypage silently render as empty states
- `apps/expert/app/(main)/mypage/page.tsx:96` · states (error rendered as empty)
- All six parallel queries discard their error field — `dealsRes.data || []` etc. If any query fails (network, RLS, schema drift), the user sees the empty-state copy '진행 중인 작업이 없습니다' / '매칭 제안이 없습니다' as if they genuinely have no work, with no error surface and no retry. States lens requires loading/empty/error/data as distinct states; here error is indistinguishable from empty. Same pattern in work/page.tsx:43 (`(deals || [])`). This is the silent-degradation class honest-implementation.md forbids: the page pretends success on failure.

#### P2-29. Dispute-reason textarea has no label or aria-label, and no length limit
- `apps/expert/app/(main)/work/[id]/dispute-button.tsx:70` · a11y (unlabeled input)
- The textarea relies solely on placeholder text — no <label htmlFor>, no aria-label — failing WCAG 2.1 AA (1.3.1/3.3.2): screen readers announce an unnamed multiline field, and the placeholder disappears once typing starts. The adjacent h3 is not programmatically associated. Additionally there is no maxLength on the field and the POST /api/disputes route accepts unbounded reason text (only presence is checked at route line 33), so a paste of arbitrary size is silently accepted. Compare message-thread.tsx which does both (aria-label + maxLength 1000).

#### P2-30. Message list opens scrolled to the oldest message and never scrolls to new ones
- `apps/expert/app/(main)/work/[id]/message-thread.tsx:60` · effort/feedback (latest content hidden)
- Messages render oldest-first inside a max-h-64 overflow-y-auto container with no scroll anchoring (no ref/scrollIntoView, no flex-col-reverse, no CSS overflow-anchor strategy). Once the thread exceeds ~5 messages, the newest admin reply — the thing the expert came to read — is below the fold, and even the user's own just-sent optimistic message appends out of view, making the send feel like it did nothing. Feedback lens: the action's visible confirmation is hidden by default. Same defect class exists in the owner-side thread.

#### P2-31. DB fetch errors are silently rendered as empty states
- `apps/expert/app/expert-dashboard.tsx:67` · states
- All four Promise.all queries (plus the workingCount query) ignore the error field; on failure `matchings`/`serviceOrdersData`/`openRequests` are null and the `|| []` fallback renders the polished empty-state copy ('아직 매칭 제안이 없습니다') as if the expert genuinely has no matchings. A senior with 5 pending proposals who hits a transient DB error is told they have none, with no error state and no retry affordance. The error state is collapsed into the empty state — the two must be distinguishable.

#### P2-32. Kakao login without an email dead-ends signup with a generic error
- `apps/owner/app/(auth)/callback/route.ts:40` · states
- user.email! assumes every OAuth account has an email. Kakao accounts frequently have no email (email consent is optional / requires business verification), so the insert violates NOT NULL and the user is bounced to /?error=profile_create — a generic failure toast for a situation the user cannot fix by retrying. The owner-signup flow silently cannot onboard email-less Kakao users, and the copy gives no actionable next step.

#### P2-33. GRADE_LABEL map duplicated in two files instead of shared labels module
- `apps/owner/app/(browse)/experts/expert-card.tsx:14` · consistency
- The identical grade→Korean-label map is declared here and again in apps/owner/app/(browse)/experts/[id]/page.tsx line 15, while the codebase already has a shared label module (@jisane/shared/labels, used by expert-dashboard.tsx for MATCHING_STATUS_LABELS/ORDER_STATUS_LABELS). Two copies of one label map with no pinning test is scheduled drift: adding a grade or renaming '시니어지식인' will update one surface and not the other, so the list card and the detail page will disagree about the same expert.

#### P2-34. Expert list query errors are swallowed — DB failure renders as an empty list
- `apps/owner/app/(browse)/experts/page.tsx:77` · states
- Both the expert_category fetch (line 20-25) and the main expert query destructure only .data and ignore .error. On any query failure the page renders ExpertList with [], indistinguishable from "no experts exist". The states lens requires error to be first-class, not disguised as empty; there is no retry or error message anywhere on this page.

#### P2-35. All six mypage section queries swallow errors — failures display as "no data" empty states
- `apps/owner/app/(main)/mypage/page.tsx:99` · states
- requests/orders/deals/interests/invitations/reviews all coerce with `.data || []` and never inspect .error. A Supabase outage or RLS/schema failure shows the owner "등록된 의뢰가 없습니다", "진행 중인 매칭이 없습니다" etc. across the whole dashboard — an alarming false signal (looks like their history vanished) with no error state or retry offered on any of the six sections.

#### P2-36. Category tabs drift from the category label map — 'education' packages are unreachable by browsing
- `apps/owner/app/(browse)/services/services-view.tsx:8` · consistency
- CATEGORY_TABS hardcodes only ax_consulting and biz_consulting while CATEGORY_LABELS (line 13-17) also knows 'education'. Any owner-audience package with category 'education' can never appear in browse mode (filtered on p.category === activeCategory), only via text search — yet the provider card's "서비스 N개" count (line 210) includes it, so the count disagrees with what the tabs can show. Two definitions of the category set in one file with no pinning between them; adding a category to the label map will silently not surface in the UI.

#### P2-37. Provider accordion toggle has no aria-expanded and signals state with raw ▼/▶ text glyphs
- `apps/owner/app/(browse)/services/services-view.tsx:218` · a11y
- The expandable provider button carries no aria-expanded/aria-controls, so screen readers get no open/closed state; the only visual state cue is a ▼/▶ character (not aria-hidden — announced as "black down-pointing triangle"), while every other icon in the file is an SVG line icon. Additionally the price-filter buttons (line 150-159) and category tabs (line 228-238) lack aria-pressed/tab semantics, even though the same chip pattern in request-form.tsx correctly sets aria-pressed — inconsistent application of the project's own pattern.

#### P2-38. Dispute reason textarea has no label and uses off-system focus/border tokens
- `apps/owner/app/(main)/status/[id]/dispute-button.tsx:70` · a11y/consistency
- The textarea's only identification is its placeholder — no <label> or aria-label, failing WCAG labeling (placeholder disappears on input and is not a reliable accessible name). It also styles with `border-border` and `focus:border-accent` while every sibling form field in this flow (inspection-section, request-form, profile form) uses the border-border-light + focus:border-primary + focus ring primitive — a visibly different input on the same screen.

#### P2-39. Revision and issue-report textareas are placeholder-labeled only
- `apps/owner/app/(main)/status/[id]/inspection-section.tsx:78` · a11y
- Both the 수정요청 textarea (line 74-80) and the 문제신고 textarea (line 125-131) have no <label htmlFor> or aria-label — the placeholder is the sole name. Screen-reader users reach an unnamed multiline field inside a section with three different actions (검수완료/수정요청/문제신고), making it unclear which action the field belongs to.

#### P2-40. Logged-out users are bounced to home with no explanation and no return path
- `apps/owner/app/(main)/layout.tsx:11` · feedback/effort
- An unauthenticated hit on /mypage, /request or /status redirects bare to '/' — no message, no way back to the intended page after login. The global ErrorToast already whitelists an `unauthorized` code ('로그인이 필요합니다'), so `redirect('/?error=unauthorized')` would explain the bounce for free; a returnTo param would restore the destination. As shipped, a user tapping a shared /status/… link while logged out lands on the marketing home wondering what happened.

#### P2-41. Raw internal error text (English, vendor internals) surfaced verbatim in the payment UI
- `apps/owner/app/api/payments/checkout/route.ts:79` · copy/feedback
- On failure the route returns err.message from createCheckoutSession (Toss API/network internals) — and its other errors are English developer strings ('Deal is not in quoted status', 'Deal not found', 'Forbidden'). payment-button.tsx line 59 renders data.error verbatim via onError, so a Korean-speaking business user at the money moment sees untranslated internal jargon or vendor error dumps. This bypasses the whitelist principle the shared toast component documents ('원본 쿼리값을 그대로 띄우면... 내부 코드도 노출된다'). Map failures to whitelisted Korean user copy; log the internal message server-side only.

#### P2-42. Raw Supabase insert error message shown to the user in the dispute form
- `apps/owner/app/api/disputes/route.ts:76` · copy/feedback
- insertErr.message is a Postgres/Supabase internal string (English, may include constraint/column names). dispute-button.tsx (verified: line 43/75) renders data.error verbatim inline, so a failed dispute submission shows DB internals to the business owner instead of a plain-language Korean message. Return a fixed user-facing message and log the internal error server-side.

#### P2-43. Review comment textarea has no accessible label — placeholder only
- `apps/owner/app/(main)/status/[id]/review-section.tsx:89` · a11y
- The comment textarea relies solely on placeholder text; it has no <label>, aria-label, or aria-labelledby. Screen readers announce an unnamed edit field, and the placeholder disappears once typing starts (WCAG 2.1 AA 1.3.1/3.3.2). The sibling forms in this same flow do it right (quote-section's textarea has an htmlFor label; message-thread's input has aria-label) — this one is the odd one out. Add aria-label="작업 소감" or a visible label.

#### P2-44. Counter rounds to integer — landing '만족도' metric displays the wrong number
- `packages/ui/components/animated-counter.tsx:31` · craft/copy
- AnimatedCounter renders Math.round(easeOutExpo(progress) * end), so the final frame is Math.round(end). landing-stats.ts computes avgSatisfaction to one decimal place (Math.round(avg * 10) / 10, e.g. 4.6), and apps/owner/app/page.tsx:46 feeds it in as a metric — so a real 4.4 displays as '4' and a real 4.6 displays as '5'. A trust metric on the landing page is silently misstated (inflated or deflated). Compounding it, the tile shows just the bare number with label '만족도' and empty suffix — no scale (out of 5? percent?), so even a correct value is uninterpretable. Support fractional ends (fixed decimals) and add a scale suffix like '/5'.

#### P2-45. Login dropdown cannot be dismissed by keyboard (no Escape, no focus handling)
- `packages/ui/components/login-dropdown.tsx:24` · a11y/WCAG AA
- The dropdown closes only via a document mousedown listener (click outside). A keyboard-only user who opens it with Enter has no way to dismiss it except activating a login button or clicking with a mouse: Escape is not handled, and focus is not returned to the trigger on close. aria-expanded is wired (good), but WCAG 2.1 keyboard operability expects Escape-to-close and focus restoration for disclosure menus. Add a keydown handler for Escape that closes and refocuses the trigger.

#### P2-46. Status-to-color map hand-copied in 10+ files while labels are centralized
- `apps/owner/app/owner-dashboard.tsx:9` · consistency
- REQUEST_STATUS_LABELS / DEAL_STATUS_LABELS come from @jisane/shared/labels (single source), but the matching status→badge-color map is redeclared locally here and in at least 9 other files across owner/expert/admin apps (owner mypage, owner status, expert-dashboard, expert work/mypage/invitations, admin dispute-tab/invitation-tab, ...). Drift is already visible between copies (?? vs || fallbacks, px-2 vs px-2.5 badge padding). One new status added to the shared labels will render as the grey fallback in some screens and colored in others. Move the color map next to the label map in @jisane/shared/labels (well past the 3rd repetition).

#### P2-47. Success toast white text on bg-success is 3.30:1 — fails AA (needs 4.5:1)
- `packages/ui/components/toast.tsx:70` · a11y/WCAG AA
- The success toast renders text-sm (14px) white text on --success #16a34a. Computed contrast is 3.30:1, below the 4.5:1 AA requirement for normal-size text. The error toast (white on #dc2626, 4.83:1) passes, so this is specifically the green token being too light to carry white text. Fix at the token level (darker success surface for filled components, e.g. ~#15803d or darker) so every bg-success + text-white pairing passes.

#### P2-48. Status badge text fails AA: warning-on-warning-light 3.07:1, success-on-success-light 3.15:1 at 12px
- `packages/ui/components/workflow-checklist.tsx:6` · a11y/WCAG AA
- The '진행 중' badge is text-xs (12px) #d97706 on #fffbeb = 3.07:1 and the '완료' badge is #16a34a on #f0fdf4 = 3.15:1 — both well under the 4.5:1 AA minimum for small text. This is a token-level pairing (--warning/--success used as text on their *-light surfaces) reused across the apps (e.g. STATUS_COLORS maps in owner-dashboard.tsx and expert-dashboard.tsx use the identical bg-*-light text-* pairs), so fixing the token fixes every badge. The info pair (#2563eb on #eff6ff, 4.75:1) shows the pattern can pass — darken the warning/success text tokens or add dedicated *-text tokens.

#### P2-49. Infinite auto-rotator is an aria-live region with no pause control
- `packages/ui/components/text-rotator.tsx:34` · a11y/WCAG AA
- TextRotator rotates words every 2.5s forever and marks the container aria-live="polite" aria-atomic, so screen readers announce each rotation indefinitely — a persistent interruption. WCAG 2.2.2 (Pause, Stop, Hide) requires auto-updating content lasting over 5s to be pausable by the user; only prefers-reduced-motion users are exempted (line 24-25, that part is handled). For sighted-AT users and keyboard users there is no pause/stop affordance. A decorative headline rotator should drop aria-live entirely (announce only the first word or a static combined phrase) and/or pause on hover/focus.

#### P2-50. Critical payment-error toast self-destructs after 4 seconds and cannot be recovered
- `packages/ui/components/toast.tsx:130` · feedback
- ErrorToast auto-dismisses all errors after 4000ms, including the 'payment' code whose copy is long and action-critical: '결제 처리 중 문제가 발생했습니다. 결제내역 확인이 필요하니 고객센터로 문의해주세요.' (~40 chars + an instruction to contact support). Average reading plus comprehension of that instruction exceeds 4s, and because the effect at line 122-124 already stripped ?error= from the URL, a user who missed it cannot re-surface the message even with a refresh. Payment-severity errors should persist until explicitly dismissed (the close button already exists); transient auto-dismiss is fine only for low-stakes codes.

#### P2-51. Search input seeded from defaultValue once — goes stale against the URL on back/forward or filter navigation
- `packages/ui/components/search-box.tsx:26` · consistency
- The input state is initialized from defaultValue and never re-synced when the URL's query param changes underneath it. Callers (e.g. apps/expert/app/(browse)/requests/request-list.tsx:107-113 passes defaultValue={query} with no key prop) keep the same client component instance across router navigations, so after the user searches then hits Back — or taps a category filter link that drops q — the results reflect the URL while the box still shows the old query. Screen contradicts itself (stored value that should be derived from the URL). Fix: key the component by the param, or sync state from useSearchParams.

## 3. P3 — 85건

#### P3-1. Success state is a 3-second client timer racing server revalidation
- `apps/admin/app/(protected)/dashboard/matching-tab.tsx:113` · states
- After a successful assign, the request card is hidden by filtering on assignedId, then a setTimeout clears it after 3s. If revalidatePath's refresh hasn't landed within 3s (slow network, heavy dashboard query — this page runs ~15 queries), the already-matched request card reappears with a live '후보 보기'/'배정' button; clicking it yields the confusing error '이미 매칭 진행 중인 의뢰입니다.' The hidden/shown state is stored (timer) rather than derived from the server data.

#### P3-2. Rank badges use raw Tailwind palette colors instead of design tokens
- `apps/admin/app/(protected)/dashboard/matching-tab.tsx:238` · craft
- The 1/2/3-rank badges hardcode bg-yellow-100/text-yellow-700, bg-gray-100/text-gray-600, bg-orange-100/text-orange-600 while every other badge in the dashboard uses semantic tokens (bg-warning-light, bg-info-light, text-accent...). These fixed light-palette values sit outside the token system, so they won't follow theme changes (dark mode) and visually drift from the rest of the badge family.

#### P3-3. Non-admin login is rejected silently — no error param on redirect
- `apps/admin/app/(auth)/callback/route.ts:31` · feedback
- Every other failure path in this callback carries an explanation (?error=no_code, exchange_failed, no_user), but the most common real-world case — a user who authenticated successfully yet is not in ADMIN_EMAILS — is bounced to '/' with no parameter at all. The user just lands back on the login page with a session and no message, and cannot distinguish 'wrong account' from 'login broken'.

#### P3-4. Estimate line guarded only by cap_amount — null est_hours renders as bare 'h ·'
- `apps/admin/app/(protected)/dashboard/invitation-tab.tsx:95` · states
- The block is gated on inv.cap_amount != null but interpolates inv.est_hours unguarded; est_hours is typed number | null, so a row with a cap but no hour estimate renders 'h · 500,000원' (React renders null as empty string). Guard should cover both fields or format them independently.

#### P3-5. Loading skeleton doesn't match the real page shape — 7 cards/6 tabs vs 9/9
- `apps/admin/app/(protected)/dashboard/loading.tsx:9` · craft
- The skeleton renders 7 summary cards on a grid-cols-2/sm:3/md:4/lg:7 grid and 6 equal-width tab blocks, but the real page (page.tsx:278) renders 9 cards on grid-cols-3/md:5/lg:9 and 9 grouped tabs. When data arrives the layout visibly reflows, defeating the purpose of a shape-matched skeleton. The two counts have already drifted once and will drift again without a shared source.

#### P3-6. Numeric && guard — budget_hope of 0 renders a stray '0' in the card
- `apps/admin/app/(protected)/dashboard/matching-tab.tsx:154` · states
- {req.budget_hope && (...)} with budget_hope typed number | null renders the literal 0 into the JSX when the value is 0 (falsy number, not skipped by React). Should be a != null (or > 0) check, as done elsewhere in this file for interestCounts ('(interestCounts[req.id] || 0) > 0').

#### P3-7. Single error line pinned to top of a three-section tab — off-screen for lower actions
- `apps/admin/app/(protected)/dashboard/partner-tab.tsx:81` · feedback
- All provider/package status actions in this long tab share one error string rendered at the very top. A failed '보관' or '중지' on an item in the second or third section shows its error above the fold where the admin isn't looking; there is also no auto-scroll or per-item error placement. matching-tab.tsx:130 has the same top-of-list pattern inside expandable cards.

#### P3-8. Summary grid: redundant breakpoint class and 9-across cards at lg
- `apps/admin/app/(protected)/dashboard/page.tsx:278` · craft
- 'grid-cols-3 sm:grid-cols-3' is a no-op duplicate, and lg:grid-cols-9 squeezes nine cards — including '적립금 잔액' with a full formatted currency value — into one row, leaving each card roughly 110-120px wide on a typical 1280px laptop where the amount can overflow or wrap awkwardly. A 5+4 or auto-fit layout would keep the currency card readable.

#### P3-9. Order cancellation fires instantly from a bare select with no confirmation
- `apps/admin/app/(protected)/dashboard/service-tab.tsx:190` · hierarchy
- The status dropdown includes '취소' (cancelled) alongside routine transitions, and onChange PATCHes immediately. A mis-click or arrow-key slip on the select cancels a paid customer order with no confirm step and no undo affordance beyond re-selecting a status. The settlement tab in the same dashboard confirms far less destructive money actions via confirm(); here the danger option is not separated from routine ones nor gated.

#### P3-10. Settlement action errors render far from the triggering button and without aria-live
- `apps/admin/app/(protected)/dashboard/settlement-tab.tsx:104` · a11y
- The single error string is rendered above the whole settlement list, so after clicking 정산 실행 on the third card the failure message appears off-context (possibly above the viewport), and it lacks role="alert"/aria-live so screen readers get no announcement. review-form.tsx in the same group does this correctly (role="alert" aria-live="polite"); service-tab.tsx and progress-tab.tsx share the same silent pattern.

#### P3-11. Owner email is fetched but never rendered, unlike every sibling tab
- `apps/admin/app/(protected)/dashboard/proposed-tab.tsx:65` · consistency
- The ProposedItem interface carries request.owner.email, and the progress and settlement tabs render the owner's email as a mailto link next to the phone link, but this tab renders only the owner's tel link — the expert row right below it does get a mailto link. An admin chasing a stalled matching from this tab cannot email the owner without switching tabs.

#### P3-12. expert.field rendered without null guard, unlike proposed-tab
- `apps/admin/app/(protected)/dashboard/progress-tab.tsx:130` · consistency
- field is typed string | null; when null this renders an empty <span> in the flex row (a stray gap between name and contact). proposed-tab.tsx guards the same value ({m.expert.field && <span>...}). Cosmetic inconsistency between two tabs showing the same entity.

#### P3-13. Star radiogroup uses role=radio without the radio keyboard pattern
- `apps/admin/app/(protected)/review-input/[id]/review-form.tsx:39` · a11y
- Each star is a native button with role="radio" inside role="radiogroup", but there is no roving tabindex or arrow-key navigation, so assistive tech announces a radio group whose expected arrow-key interaction does not work (each star is a separate tab stop instead). Functionally reachable, but the ARIA pattern promises behavior the widget doesn't implement.

#### P3-14. Raw Tailwind palette colors bypass the design-token system
- `apps/admin/app/(protected)/review-input/[id]/review-form.tsx:44` · craft
- text-yellow-400 / text-gray-300 are the only raw palette classes in this group — everything else uses semantic tokens (text-text, bg-surface, text-accent, border-border). If the token palette is ever retuned, the rating stars won't follow.

#### P3-15. Hardcoded bg-white amid token-based surfaces
- `apps/admin/app/(public)/ax/page.tsx:84` · craft
- The 적용축 cards (line 84) and 사다리 cards (line 108) use bg-white while adjacent sections on the same page use bg-surface-warm/bg-surface tokens. globals.css defines no dark scheme today, so this is token-consistency debt rather than a contrast break — but it is exactly the surface that silently stays white if a dark theme or palette shift lands.

#### P3-16. Message count badge goes stale after the admin replies
- `apps/admin/app/(protected)/dashboard/progress-tab.tsx:110` · states
- msgCount comes from the server-provided messageCounts prop; the optimistic append on send updates the thread but never the badge, so after replying the badge (e.g. '3') visibly disagrees with the thread (4 bubbles) until a full refresh. Stored value diverging from derived truth on the same card.

#### P3-17. Guarantee-fund accrual base contradicts the standards index page (중개 수수료 vs 매칭비)
- `apps/admin/app/(public)/standard/guarantee/page.tsx:46` · copy
- 제2조 states the fund accrues from '각 거래의 중개 수수료 중 10%', while standard/page.tsx principle 5 says '매칭비의 10%를 책임적립금으로 쌓아둡니다' — two trust-critical documents disagree on the fee base of the same 10%, and the service page separately insists '기업 수수료 0% — 매칭비만 별도' (i.e. there IS no 중개 수수료 besides 매칭비). Same drift on the fee's name across scope page ('중개 수수료율 · 7단계 등급표') vs standard page ('매칭비 7구간 단가'). Pick one term (매칭비) and one base, and use it in every document — for pages whose whole pitch is precision ('형용사 금지, 동사와 숫자로만'), a shifting noun is a product defect.

#### P3-18. Live legal-standard pages label themselves '초안 v1.0' (draft)
- `apps/admin/app/(public)/standard/scope/page.tsx:14` · copy
- Both trade-standard documents (scope and guarantee — guarantee/page.tsx line 14 has the same pattern) display '초안 v1.0' in the hero subtitle plus internal-jargon framing ('오해 제거 5대 메커니즘 ② 범위 실물화'). Telling users the binding contract template they are asked to sign is a 'draft' contradicts the reliability message; the mechanism numbering is internal strategy vocabulary that means nothing to a 발주자. Drop '초안' once these are in force and translate the eyebrow/subtitle into user language.

#### P3-19. Raw Supabase error.message leaked to the client; error copy language inconsistent across admin APIs
- `apps/admin/app/api/admin/deals/route.ts:24` · consistency
- Several admin routes (deals, matching, requests, service-orders, expert-activity line 48) return the raw DB driver message (English, internals — table/constraint names) straight to the UI, while expert-activity's authz error is polished Korean ('관리자 권한이 필요합니다.') and others return bare 'Forbidden'. Whatever surface renders these will show untranslated internals to the operator. Map to stable Korean error copy at the boundary and log the raw message server-side.

#### P3-20. 'AX 무료 진단 신청하기' CTA drops the user on a generic services list in another app
- `apps/admin/app/(public)/ax-process/page.tsx:210` · effort
- The primary CTA promises a free-diagnosis application but links to `${ownerUrl}/services` — a cross-subdomain generic catalog where the user must find the AX product themselves (recall over recognition, extra step at the highest-intent moment). Deep-link to the specific AX diagnosis service/slug, or to a request form pre-tagged with the AX intent.

#### P3-21. Header nav: '거래 표준' bypasses the /standard index and no link carries a current-page indicator
- `apps/admin/app/(public)/layout.tsx:14` · hierarchy
- The header labels the link '거래 표준' but sends users to /standard/scope (one specific document) instead of the /standard overview built to introduce all five principles — and neither scope nor guarantee pages offer a back link to /standard (ax-process demonstrates the PageHero `back` pattern; these two omit it), so the index is reachable only from the admin home page. None of the three nav links marks the active route (no aria-current or visual state), and the text-xs text-only links are small tap targets on the sticky mobile header.

#### P3-22. Footer lacks mandatory e-commerce disclosures for a paid escrow service
- `apps/admin/app/(public)/layout.tsx:35` · copy
- The footer shows only 사업자등록번호 and an email (a personal gmail address at that). For a service taking payments (토스페이먼츠 escrow), Korean 전자상거래법 disclosure conventions expect 상호·대표자명·주소·전화번호·통신판매업신고번호. Noting as P3 since legal copy is a project-policy decision per the project's own handbook — but for a brand whose pitch is '먼저 꺼내놓는 것이 표준', the thinnest footer disclosure in the category is also a trust defect.

#### P3-23. Quote/statement not-found state is a dead end with no action
- `apps/admin/app/docs/quote/[dealId]/page.tsx:25` · states
- When the deal is missing the page renders a single centered sentence with no link home, no PrintButton chrome, and no guidance — the user is stranded on a blank document route (same pattern in statement/[dealId]/page.tsx line 25). Add a link back to the status/dashboard page.

#### P3-24. as="style" attribute on a rel="stylesheet" link is invalid
- `apps/admin/app/layout.tsx:51` · craft
- The `as` attribute only applies to rel="preload"/"modulepreload"; on a plain stylesheet link it is ignored (and fails HTML validation). Either drop `as` or make it a real preload pair if the intent was to prioritize the Pretendard font CSS.

#### P3-25. Price field is marked required (*) but has no required attribute — validation only after server roundtrip
- `apps/admin/app/partner/dashboard/services/package-form.tsx:96` · effort
- name/description/category/target all use the native required attribute for inline validation, but the price input (marked with a red * when not free) omits it. A paid service with an empty price submits, and the user only learns of the problem from the server action's '가격을 입력하거나 무료로 지정해주세요.' error after a full roundtrip — inconsistent with the form's other required fields.

#### P3-26. Edit mode hides the immutable category/target values instead of showing them read-only
- `apps/admin/app/partner/dashboard/services/package-form.tsx:45` · effort
- In edit mode the category and target-audience fields are removed entirely, and the edit page's subtitle only says they cannot be changed — nowhere on the edit screen can the partner see WHICH category/target the service currently has. Recognition over recall: show the current values as disabled/read-only text rather than hiding them.

#### P3-27. Long service-registration form has no dirty-guard — navigating away silently discards input
- `apps/admin/app/partner/dashboard/services/package-form.tsx:35` · feedback
- The package form has 8 fields including two textareas, but no beforeunload/dirty-state guard. A partner who fills the form and clicks a nav link (e.g. '대시보드' in the partner layout header) loses everything with no warning. The shorter profile form shares the same gap but the package form is where real writing is lost.

#### P3-28. Orders list silently caps at 100 rows with no pagination or truncation notice
- `apps/admin/app/partner/dashboard/orders/page.tsx:23` · states
- The query takes the newest 100 orders and renders them as if complete. Once a partner exceeds 100 orders, older ones become unreachable from the UI with no 'more' affordance and no indication anything was cut off.

#### P3-29. Provider accordion toggles lack aria-expanded; state conveyed only by ▼/▶ glyphs
- `apps/expert/app/(browse)/education/education-view.tsx:64` · a11y
- The expand/collapse buttons expose no aria-expanded state, and the raw ▼/▶ text characters (not aria-hidden) are read literally by screen readers ('black down-pointing triangle'). Assistive-tech users cannot tell whether a provider's course list is open.

#### P3-30. '관심 표현 완료' button withdraws interest on click with no affordance or confirm
- `apps/expert/app/(browse)/requests/[id]/interest-button.tsx:52` · feedback
- After expressing interest the button label reads as a completed status ('관심 표현 완료'), but clicking it immediately withdraws the interest — there is no '취소' wording, hover hint, or confirmation. A user tapping the status-looking button accidentally undoes their expression of interest (interest slots are limited per the action's quota error, making an accidental withdraw+re-add non-free).

#### P3-31. Hardcoded production URL as silent env fallback for cross-app links
- `apps/expert/app/(browse)/education/[slug]/education-detail-view.tsx:13` · craft
- NEXT_PUBLIC_ADMIN_URL falls back to a hardcoded 'https://jisane.cloud' (same pattern repeated inline in education-view.tsx line 134). In any environment where the env var is unset (preview/staging), '관련 자료 보기'/'자세히' links silently point at production with no comment documenting the fallback — an undocumented silent fallback and a duplicated magic constant.

#### P3-32. Rejected/suspended partner state offers only a hardcoded personal Gmail as recourse
- `apps/admin/app/partner/page.tsx:118` · copy
- For pending/rejected/suspended partners the only next step shown is '문의: iamblackwhite86@gmail.com' — a personal Gmail address hardcoded in the page body (not a mailto: link, not a support alias, not sourced from config). It is also not tappable, so mobile users must copy it by hand.

#### P3-33. Missing/unauthorized matching silently double-redirects to home with no explanation
- `apps/expert/app/(main)/matching/[id]/page.tsx:39` · consistency
- A stale or unauthorized matching id redirects to /matching, which itself is a hard redirect to / (matching/page.tsx). The user clicking an old link lands on the home dashboard with zero explanation of why. The sibling invitations detail page uses notFound() for the same case — two different behaviors for the same situation.

#### P3-34. Truthy check on numeric budget — renders a literal '0' and hides zero budgets
- `apps/expert/app/(browse)/requests/request-list.tsx:211` · craft
- `{req.budgetHope && (...)}` is the classic React number-truthiness gotcha: if budget_hope is 0 the expression evaluates to 0 and React renders a stray '0' text node in the card meta row. The same pattern is at opportunity-section.tsx line 82 and matching/[id]/page.tsx line 71, while invitations/[id]/page.tsx line 108 correctly uses `budget_hope != null` — inconsistent primitives for the same field.

#### P3-35. Empty state ignores an active search query and offers no clear-search affordance
- `apps/expert/app/(browse)/requests/request-list.tsx:179` · copy
- When a search query returns 0 results without a category filter, the empty card says '현재 열린 의뢰가 없습니다' — implying the platform has no open requests at all rather than 'no matches for your search'. The clear-filter shortcut ('전체 의뢰 보기') is only offered for the category case; there is no equivalent to clear the search term.

#### P3-36. Search silently strips % and , and leaves _ as a wildcard; result count hides the 50-row cap
- `apps/expert/app/(browse)/requests/page.tsx:75` · effort
- Sanitizing by deleting % and , means a user searching for '50%' or '홍보,마케팅' silently searches something else, and `_` is not escaped so it acts as an ilike single-char wildcard. Separately, the query is capped at limit(50) while the client summary claims '${requests.length}건의 열린 의뢰' as if it were the total — at exactly 50 the count is a truncation, not a total, with no indicator.

#### P3-37. In-flight interest toggle shows only '...' — meaningless to screen readers
- `apps/expert/app/(browse)/requests/request-list.tsx:236` · feedback
- The pending label is a bare ellipsis with no aria-busy or descriptive text ('처리 중'), so assistive tech announces '...' and sighted users get a label that no longer describes the action. Same pattern in opportunity-section.tsx lines 100-104. Elsewhere the app uses proper pending copy ('수락 처리 중...', '거절 중...').

#### P3-38. Irreversible 거절 styled identically to the neutral 조건 상의 button
- `apps/expert/app/(main)/matching/[id]/matching-actions.tsx:104` · hierarchy
- 수락 is correctly the single primary action, but the destructive 거절 (its own confirm dialog says it cannot be undone) uses the exact same neutral border/muted-text styling as the harmless 조건 상의 button above it — danger is not visually separated. The invitation flow does distinguish its decline path with error tokens once confirming.

#### P3-39. Non-null-asserted division can render 'NaN원' if est fields are absent while cap_amount is set
- `apps/expert/app/(main)/invitations/[id]/page.tsx:126` · craft
- The 수락 정보 card is guarded only by cap_amount != null, then divides est_amount! by est_hours!. If the invariant (all three set together on accept) is ever broken by admin edits or data migration, the UI renders 'NaN원' or 'Infinity원' as the hourly rate with no guard. Deriving is right per state discipline, but the derivation needs the same null guard as its trigger.

#### P3-40. Dead import: Suspense imported but never used
- `apps/expert/app/(main)/invitations/page.tsx:1` · craft
- Suspense is imported and never referenced anywhere in the file — dead code left behind (the page also has no loading.tsx fallback, unlike the matching route which ships a skeleton, so the invitations list navigation shows nothing while streaming).

#### P3-41. Dispute duplicate-check is SELECT-then-INSERT with no unique constraint — concurrent submits create duplicate disputes
- `apps/expert/app/api/disputes/route.ts:54` · feedback/data-integrity (duplicate submissions)
- The route checks for an existing open dispute and then inserts. Two concurrent requests (double-tap before the disabled state paints, retry after a slow response, two tabs) both see no existing row and both insert, producing duplicate open disputes for the same review that the admin dispute queue then shows twice. quality-gates.md names this exact pattern: 'SELECT-then-INSERT is itself the race' — a retryable write needs an idempotency key enforced by a UNIQUE constraint (e.g. partial unique index on (target_type, target_id) where status='open').

#### P3-42. Tapping a 6th specialty chip is silently ignored at the 5-field cap
- `apps/expert/app/(main)/register/page.tsx:33` · feedback (silent no-op)
- toggleField returns prev unchanged when 5 fields are selected, so tapping another chip produces no visual change and no message — the chip just doesn't select. The '선택: n/5개' counter exists but sits below seven chip groups, far from the tapped chip. For the senior-citizen target audience this reads as a broken button. Either disable/dim unselected chips at the cap or show an inline notice ('최대 5개까지 선택할 수 있어요') at the point of interaction.

#### P3-43. Required '전문 분야' has no client-side validation — submit with zero fields round-trips to a bottom-of-form server error
- `apps/expert/app/(main)/register/page.tsx:98` · effort (no inline validation on required field)
- The field is marked required (*) but the submit button stays enabled with selectedFields empty; the hidden input posts an empty string and the user only learns of the failure via state.error rendered at the bottom of the form after a server round-trip, with no focus management or field-level association. Inline validation lens: block or message at the field before submit (e.g. disable submit until >=1 field, or validate on click with the message next to the chip group).

#### P3-44. Marking a workflow step '완료' is one tap and irreversible, with no confirmation or undo
- `apps/expert/app/(main)/work/[id]/workflow-form.tsx:76` · feedback (irreversible action without confirm)
- handleToggle promotes in_progress->done on a single tap of a small pill button; once done, the UI offers no path back (done branch renders a static badge) and the API route's transition table defines done as terminal (done: []). A misclick permanently completes a step, and completing the 5th step triggers the '모든 단계 완료' banner telling the manager the work is ready for client inspection. An irreversible state-advancing action should get a lightweight confirm or a brief undo window.

#### P3-45. No loading.tsx for /mypage (or /work) despite six serial-after-auth DB queries
- `apps/expert/app/(main)/mypage/page.tsx:52` · states (no loading state)
- MyPage awaits auth, an expert lookup, then six parallel queries before rendering anything; the (main) route group ships a loading.tsx only for /matching (per the route tree). Navigating to /mypage or /work shows no skeleton or pending UI — the previous screen just sits there until the server responds, which on a slow connection reads as an unresponsive tap. States lens: loading is one of the four mandatory states; a skeleton matching the section layout (as /matching already has) is the established pattern in this app.

#### P3-46. FIELD_GROUPS hardcodes a copy of the category table with no pinning test — drift silently breaks field matching
- `apps/expert/app/(main)/register/page.tsx:7` · consistency (unpinned mirror of DB data)
- The comment acknowledges FIELD_GROUPS must stay in sync with the category table, but nothing enforces it. mypage matches expert.field strings against request.req_type (`.in('req_type', expertFields)`, mypage:90) to build '내 분야 공개 의뢰' — if a category is renamed or added on the DB side, experts registered via this hardcoded list silently stop matching new requests (the section just doesn't render; no error). coding-style.md: a duplicated shape needs a pinning test that fails on divergence.

#### P3-47. budget_hope of 0 renders a stray literal '0' in the matching card
- `apps/expert/app/expert-dashboard.tsx:155` · states
- In React, `{0 && <p>...</p>}` evaluates to 0 and renders the text '0'. budget_hope is number | null; if a request is ever saved with budget 0, the card shows a bare '0' where the price line goes. Use `budget_hope != null && budget_hope > 0` (the pattern already used for activity_points elsewhere) or decide how a 0 budget should display.

#### P3-48. Clicking a 6th field chip is silently ignored
- `apps/expert/components/profile-editor.tsx:51` · feedback
- toggleField returns prev unchanged once 5 chips are selected: the 6th tap produces no visual change, no toast, no shake — the chip simply doesn't select. The '(최대 5개)' hint exists, but a silent no-op on an interactive control reads as a broken button, especially for the senior audience. Either disable the unselected chips at the limit (visibly) or show inline feedback when the cap is hit.

#### P3-49. Server-side validation error is not announced (no role=alert) and only appears after round-trip
- `apps/expert/components/profile-editor.tsx:158` · a11y
- The only validation surface for the required 전문 분야 field is state.error rendered at the bottom of the form after a full server action round-trip, in a plain <p> with no role="alert"/aria-live — screen readers get no announcement, and sighted users must find the message below the fold of a 5-section form. The empty-selection case is knowable client-side (selectedFields.length === 0) and could block/inline-validate before submit.

#### P3-50. Chip-group label not programmatically associated with the controls
- `apps/expert/components/profile-editor.tsx:62` · a11y
- The '전문 분야 *' label is a bare <label> with no htmlFor/fieldset relationship; the actual controls are toggle buttons and the value lives in a hidden input. Screen reader users hear thirteen aria-pressed buttons ('창업코칭', '디자인'...) with no group name or required-ness. Wrap the chips in <fieldset><legend> or use role="group" + aria-labelledby.

#### P3-51. career_years values outside {3,7,15} silently display as '선택 안함' and are lost on save
- `apps/expert/components/profile-editor.tsx:94` · states
- The select's options only cover values '', '3', '7', '15'. If profile.career_years holds any other value (older schema, admin edit, future granularity), defaultValue matches no option, the browser falls back to '선택 안함', and the next save silently overwrites the real career with null. A stored fact is misrepresented on screen and then destroyed by an unrelated edit.

#### P3-52. Two visually identical summary cards, but only one is clickable
- `apps/expert/app/expert-dashboard.tsx:122` · hierarchy
- '진행 중 작업' is a Link to /work with a hover state; '새 매칭 제안' is an inert div styled identically. Users who learn one card navigates will tap the other and get nothing. Either make the proposals card link to the matching list (an anchor/section exists right below) or visually differentiate the non-interactive card.

#### P3-53. OAuth sign-in buttons have no pending state and allow double-submit
- `apps/expert/app/page.tsx:72` · feedback
- Both Kakao/Google forms (hero and repeated CTA) use plain <button type="submit"> with no useFormStatus/SubmitButton, even though the shared SubmitButton primitive exists and is used elsewhere (profile-editor). After tapping, nothing indicates the redirect is in flight; on a slow connection seniors will tap again. Consistency lens too: the app's own submit primitive is bypassed here.

#### P3-54. Three-column trust-badge grid at all viewports cramps 320-375px phones
- `apps/expert/app/page.tsx:223` · craft
- The trust badges use grid-cols-3 with no mobile breakpoint; each card holds a bold title plus a two-line description ('작업료 전액 · 당신 통장으로 직접 정산'), which at ~100px column width on small phones wraps into a tall ragged stack. The metrics section above got the responsive treatment (grid-cols-2 md:grid-cols-4); this section did not. Same concern applies to the always-2-col comparison grid at line 162.

#### P3-55. No tab is highlighted on /matching/* and /requests/* routes
- `apps/expert/components/expert-nav.tsx:84` · consistency
- The active-tab match covers only the five tab hrefs, but core flows reached from the home dashboard live at /matching/[id] and /requests — on those pages the bottom nav shows no active tab at all, dropping the user's sense of place. Map matching/requests routes to the 홈 (or 작업) tab so location is always indicated.

#### P3-56. Landing promises '6항목 점수 투명 공개' but the visible profile shows at most 4-5 figures
- `apps/expert/app/page.tsx:227` · copy
- The trust badge (and the comparison row at line 160) claims six matching-score items are transparently disclosed, yet the owner-facing expert detail page renders 종합점수 + 3 sub-scores (경력/리뷰/완료율) + an unlabeled activity bonus. A number-specific transparency claim that the product surface doesn't match undermines exactly the trust it sells; also this badge has href: '' so the claim can't be verified anywhere.

#### P3-57. Bare '+N' in warning color with no label on the expert card
- `apps/owner/app/(browse)/experts/expert-card.tsx:54` · copy
- Activity points render as a naked '+3' in text-warning next to the score and career — no label, no unit, no tooltip. The detail page at least says '활동 +N'; on the card an owner has no way to know what the orange number means (recognition over recall), and warning color implies a problem rather than a bonus.

#### P3-58. Two-column score grid leaves an empty half when career is absent
- `apps/owner/app/(browse)/experts/[id]/page.tsx:133` · craft
- The grid is fixed at grid-cols-2 but the 경력 card is conditional (career_years > 0); for experts without career data the 종합점수 card sits in a half-width cell next to blank space instead of spanning the row. New/incomplete profiles — the common case early on — look broken rather than minimal.

#### P3-59. Landing metrics render literal zeros ('0+ 참여 기업', '열린 의뢰 0건을 확인하세요')
- `apps/expert/app/page.tsx:45` · copy
- The code documents hiding 완료 거래 when 0 ('완료거래 0이면 3칸' — intentional), but the same zero-state logic is not applied to 참여 기업 and 열린 의뢰: at low/empty data the hero metrics animate to '0+' and the CTA at line 253 says '등록 후 열린 의뢰 0건을 확인하세요', which argues against signing up. Apply the same hide-or-reword rule to all count-based claims.

#### P3-60. Invalid or stale ?category param silently shows the full unfiltered list
- `apps/owner/app/(browse)/experts/page.tsx:43` · states
- If the category id in the URL no longer exists (or a depth-1 category has no children), filterCatIds stays empty, targetExpertIds stays null, and ALL experts render while the URL still claims a filter — the screen looks filtered but isn't (the 8-lens 'reset a filter whose selected value disappeared' case). No notice that the requested category was not applied.

#### P3-61. Free price rendered as "₩0" here but "무료" everywhere else, next to a redundant 무료 badge
- `apps/owner/app/(browse)/services/services-view.tsx:48` · consistency/copy
- The card shows '₩0' for free packages while the detail view (service-detail-view.tsx line 38) and mypage render '무료'; the same card row already carries a 무료 badge (line 42), so '₩0' is both inconsistent and redundant. '₩0' reads like a price glitch rather than 'free'.

#### P3-62. Profile form forces two columns at every viewport width
- `apps/owner/app/(main)/mypage/owner-profile-form.tsx:32` · effort
- `grid-cols-2` has no responsive prefix, so on a 320-360px phone 대표자명/연락처 and 지역/업종 are squeezed into ~140px inputs — the '예: 010-1234-5678' placeholder truncates and typing a phone number is cramped. Form guidance (and the rest of the app's forms) is single-column on mobile; this should be grid-cols-1 sm:grid-cols-2.

#### P3-63. Category chips give no required/optional cue while every other field is explicitly marked
- `apps/owner/app/(main)/request/request-form.tsx:31` · copy/effort
- 제목 and 상세 내용 carry a red *, 희망 예산 carries '(선택)', but the category field ('어떤 일을 맡기시나요?') is marked neither — and it silently submits empty (hidden input `value={selectedChip || ''}`, server accepts null req_type). Users can't tell whether skipping it blocks submission or hurts matching quality; the inconsistent marking undermines the convention the form itself establishes.

#### P3-64. Expert list hard-capped at 50 with no pagination or truncation indicator
- `apps/owner/app/(browse)/experts/page.tsx:60` · effort
- `.limit(50)` silently drops everyone past the 50th by score; there is no 'more' affordance, page cursor, or count, so lower-ranked (typically newer) experts become undiscoverable through browsing and nobody can tell the list is truncated.

#### P3-65. Production URL hardcoded as a silent env fallback, duplicated in two files
- `apps/owner/app/(browse)/services/[slug]/service-detail-view.tsx:13` · craft
- `|| 'https://jisane.cloud'` appears here and again as ADMIN_URL in services-view.tsx line 25. A missing NEXT_PUBLIC_ADMIN_URL silently points staging/preview builds at production (honest-implementation: silent fallback, undocumented at one of the two sites), and the duplicate constants can drift independently.

#### P3-66. Collapsing the open provider hides the entire package list with no message
- `apps/owner/app/(browse)/services/services-view.tsx:224` · states
- The tabs and package list render only when a provider is expanded, so toggling the open provider closed leaves just provider cards plus a now-inert price filter — no hint that packages are hidden or how to see them. The filter predicate even contains an `expandedProvider === null ||` branch (line 121) that would show all packages, but the render guard makes that branch unreachable — the code contradicts itself about what the collapsed state should show.

#### P3-67. Star rating uses role=radio without the ARIA radiogroup keyboard pattern
- `apps/owner/app/(main)/status/[id]/review-section.tsx:68` · a11y
- The five stars are role="radio" buttons inside role="radiogroup", but there is no arrow-key navigation or roving tabindex — every star sits in the tab order and arrows do nothing, which contradicts what the radio role announces to assistive tech. It remains operable via Tab+Enter, so this is a pattern deviation rather than a blocker; either implement arrow-key roving focus or drop the radio roles and keep them plain labeled buttons.

#### P3-68. Grammar error in empty-state copy: '시니어지식인나' (missing particle)
- `apps/owner/app/(main)/status/[id]/message-thread.tsx:62` · copy
- The empty-state message reads '시니어지식인나 매니저에게' — should be '시니어지식인이나 매니저에게'. A typo in the first message a paying customer sees in the communication channel undercuts the trust the escrow flow is trying to build.

#### P3-69. Financial confirmation uses browser-native confirm() instead of the app's styled dialog
- `apps/owner/app/(main)/status/[id]/payment-button.tsx:41` · consistency/craft
- The highest-stakes confirmation in the product (approving and paying the quote) is a native confirm() popup — unstyled, shows the browser origin chrome, ignores the design system, and renders the multi-line escrow explanation as plain OS dialog text. The rest of the flow uses branded components. The confirm itself is correct UX (destructive-action guard is present); the vehicle is the defect. A styled in-page confirmation with the amount emphasized would match the product's trust framing.

#### P3-70. Done state: message composer available only to owners who already have messages
- `apps/owner/app/(main)/status/[id]/page.tsx:260` · states
- In the done branch the MessageThread (which includes the send form) renders only when messages.length > 0. An owner who never messaged during the work has no in-product way to contact the expert/manager about the completed deliverable, while an owner with one prior message keeps a full composer — inconsistent affordance for the same state. Either always render the thread in done state, or render a read-only variant for everyone.

#### P3-71. User-cancelled payment is reported as a failure
- `apps/owner/app/api/payments/fail/route.ts:28` · feedback
- All Toss fail codes — including USER_CANCEL, a deliberate user action — funnel into ?error=payment_failed, so an owner who intentionally backs out of the payment window gets the error toast '결제에 실패했습니다'. Cancel is not failure; branch on the code (which is already read into `code` at line 13) and either skip the toast or show neutral copy for user cancellation.

#### P3-72. Mixed-language validation errors in the same endpoint
- `apps/owner/app/api/requests/route.ts:61` · copy
- The required-fields error is English developer text ('title and detail are required') while the length errors four lines below are polished user-facing Korean ('제목은 200자 이내로...'). If client-side validation is ever bypassed or drifts, the user sees the English string. Make all validation messages in the endpoint user-facing Korean.

#### P3-73. Status skeleton does not mirror the loaded page's PageHero, causing layout shift
- `apps/owner/app/(main)/status/loading.tsx:4` · craft
- The real /status page opens with a full-width PageHero band followed by a responsive-container; the skeleton renders a plain padded text-bar header with different padding (px-4 py-5 vs hero + responsive-container px-4 py-6). When data arrives the page visibly restructures — the skeleton's purpose (stable perceived layout) is undermined. Mirror the hero block in the skeleton.

#### P3-74. Logged-in user without an owner row sees the logged-out landing with '시작하기' CTAs
- `apps/owner/app/page.tsx:29` · states
- When auth succeeds but no owner row matches (a lazily-created row — lib/invitation/actions.ts:36 explicitly handles 'owner 조회 (없으면 자동 생성)', proving this state is reachable), the page silently falls through to the marketing landing whose CTA section is two sign-in forms ('카카오로 시작하기'), while the AppHeader simultaneously shows the user's email and a 로그아웃 button. The screen contradicts itself: logged in above, 'start by logging in' below. Auto-create the owner row here (as the invitation action already does) or route the user to onboarding instead of the anonymous landing.

#### P3-75. Category tabs expose no selected state to assistive tech
- `packages/ui/components/category-browse.tsx:57` · a11y/WCAG AA
- The major-category filter buttons convey the selected tab only via background color (colors.bg vs bg-surface). There is no aria-pressed / aria-selected / aria-current, so screen-reader users cannot tell which category's counts the grid below is showing. Add aria-pressed={selectedIdx === idx} (toggle-button pattern) or proper tablist semantics.

#### P3-76. DB failure and genuine not-found both report 'Settlement not found'
- `apps/owner/app/api/settlements/[id]/route.ts:30` · feedback
- The .single() result's error object is discarded; any query failure (RLS/connection/transient) is reported to the caller as a 404 'Settlement not found'. The user-facing consequence is a misleading terminal message ('doesn't exist') for what may be a retryable server fault — the wrong recovery guidance. Distinguish PGRST116 (no rows → 404) from other errors (→ 500).

#### P3-77. Refund endpoint mixes English and Korean operator-facing error copy
- `apps/owner/app/api/settlements/[id]/refund/route.ts:38` · consistency/copy
- Validation errors in the same endpoint are inconsistently localized: 'Invalid refund amount', 'Refund reason is required', 'Cannot refund: current status is ...' in English, but the over-refund guard (line 71) and the ledger warning (line 133) in Korean. Whatever admin surface displays these shows the operator a mixed-language experience. Project convention (per commit history and the rest of the UI) is Korean — align the English strings.

#### P3-78. Hardcoded Tailwind gray instead of design token in Google login hover
- `packages/ui/components/login-dropdown.tsx:54` · craft
- The Google option uses hover:bg-gray-100 while every comparable hover surface in this component set uses tokens (bg-surface, bg-surface-warm, border-border-light). The Kakao option's #FEE500-derived hover is brand-mandated and fine; the gray one is not. If the surface palette is ever tuned, this one row drifts. Use hover:bg-surface.

#### P3-79. as="style" on a rel=stylesheet link is a no-op copied from preload snippet
- `apps/owner/app/layout.tsx:52` · craft
- The as attribute is only meaningful on rel="preload" links; on rel="stylesheet" it does nothing. The Pretendard CDN snippet was pasted without adapting it (the original pattern is rel="preload" as="style" + onload swap). Harmless at runtime but it signals unreviewed copy-paste, and the render-blocking third-party CSS (jsdelivr + Google Fonts) also forgoes next/font's self-hosting/preload benefits used elsewhere in the ecosystem.

#### P3-80. Current step not exposed to assistive tech — conveyed only by color and font weight
- `packages/ui/components/progress-bar.tsx:41` · a11y/WCAG AA
- The 4-step progress bar marks the active step visually (bg-accent bar, font-semibold label) but has no semantic signal: no aria-current="step", no role/aria-valuenow, no sr-only '현재 단계' text. A screen-reader user hears '접수 매칭 작업 완료' as four equal words with no way to know which stage the request is in — the component's entire purpose is lost for AT users. Note also the accent-vs-inactive bar contrast is 3.24:1, just above the 3:1 non-text minimum, so the weight cue on labels is doing real work.

#### P3-81. Toast close button target is ~20px — below the 24px WCAG 2.5.8 minimum
- `packages/ui/components/toast.tsx:76` · a11y/WCAG AA
- The dismiss button is px-1 (4px horizontal padding) around a single ✕ glyph at text-sm, yielding a hit area of roughly 16-22px in both axes — under the WCAG 2.2 AA 24x24 target-size minimum and far under the project's own 44px .touch-target convention (globals.css:433). On mobile, missing the ✕ taps whatever sits behind the toast. Same markup repeats in ErrorToast at line 148. Give it p-2 / min 24px box.

#### P3-82. Literal 'undefined' class emitted when className is omitted; spinner color hard-codes border-accent
- `packages/ui/components/submit-button.tsx:17` · craft
- With className undefined the template yields class="btn-press focus-ring undefined" in the DOM — an AI-generated tell visible in every inspector. Separately, the pending spinner is always border-accent (amber): on a bg-accent styled submit button the spinner becomes accent-on-accent and effectively invisible, so the pending state (the component's one job) can silently disappear depending on the caller's className. Use `${className ?? ''}` and currentColor/border-current for the spinner.

#### P3-83. Highlight split drops title text when the highlight string occurs more than once
- `packages/ui/components/page-hero.tsx:36` · states
- title.split(highlight) can produce 3+ segments, but only [before, after] are destructured — every segment past the second is silently discarded, so a title like '전문가와 전문가답게' with highlight '전문가' renders with the trailing text truncated. Use split with a limit-aware join (const [before, ...rest] and rest.join(highlight)) or indexOf/slice for first-occurrence-only highlighting.

#### P3-84. Dead keyframes: text-rotate-in/out and shimmer are defined but never referenced
- `packages/ui/styles/globals.css:101` · consistency
- @keyframes text-rotate-in / text-rotate-out duplicate the rotation motion that text-rotator.tsx actually implements with inline styles + transition classes (transform/opacity at lines 40-43), and @keyframes shimmer (line 96) has no utility class using it anywhere in the package. The rotator keyframes are a duplicated definition of behavior owned elsewhere — the CSS copy will silently drift from the JS implementation (it already differs: keyframe enters from +100%, JS snaps in with no from-state). Delete the dead keyframes or move the rotator onto them.

#### P3-85. --text-subtle on --background sits at 4.49:1 — hair under the AA line for its text uses
- `packages/ui/styles/globals.css:18` · a11y/WCAG AA
- #726d62 on #f3efe6 computes to 4.49:1, marginally below the 4.5:1 AA threshold. text-text-subtle is used for real content text, e.g. the search-box placeholder and its magnifier icon (search-box.tsx:47,58) and pending-step labels in workflow-checklist. One shade darker (~#6d6559 or lower) clears the bar without visibly changing the palette. Also noting under the same lens: the token sheet defines light values only — no dark-scheme variants or color-scheme declaration exists; if light-only is the brand decision it deserves a comment at the token block so the omission reads as intentional.

## 처리 우선순위
1. **P1-2 service-orders 인가**(로그인만으로 전 고객 주문 열람·변경 — P0급) 2. **P1-5 신규자 게이트 데드코드** 3. **P1-3·P1-8 소유권/수수료 노출** 4. **P1-6·P1-10 핀치줌 차단**(1줄×2) 5. P1 나머지 + V1·V2 6. P2 51건(a11y 14·feedback 11·states 10·consistency 6·effort 5·copy 3·craft 1)

*방법: git ls-files 160파일 12그룹 → fable 리더 → 적대적 검증(6 기각) → 커버리지 크리틱(미독 0) → quote-back 146/146 → 시각 스윕(browse 데몬). 진단 전용, 코드 변경 없음. code 축은 docs/11.*